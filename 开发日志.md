# 开发日志

记录每次改动的**来龙去脉**：为什么改、怎么改、量到了什么数据、踩了哪些坑、哪些东西故意没做。

与 `CHANGELOG.md` 的分工：

- `CHANGELOG.md` 面向用户，只写这个版本"有什么变化"，按发布版本组织。
- 本文件面向开发者（也包括未来的自己和 AI 协作者），写"当时是怎么想的"，按改动组织。

> 约定：**每次代码改动都要在本文件顶部补一条**，与代码同一个 PR 提交，不要等发布时再补。
> 结论请带上可复现的数据；被否掉的方案也值得写一句，省得下次重复踩。

---

## 2026-09-26 · 发布 v1.4.0

版本号 1.4.0 / 构建 140（`project.pbxproj` 三处 × 两个字段，加 `AppVersion.swift` 的兜底值）。
包含 #11（截图识别准确度）、#12（译文回贴原图）、#13（钉图 / 完成用译文图）、#14（选区翻译按进程号确认前台应用）。
截图翻译是整块重做，按 1.3.0（新增微软翻译）的惯例升次版本号。

签名没动，还是 1.3.1 起用的那张自签名证书，所以这版不用再让用户授权，也没有重复 1.3.1 那次 Sparkle 升级实测。
CHANGELOG 面向用户写，识别部分只写用户能感知到的（乱码、被改坏的字、折行、行序、崩溃），不写实现。

### 发版前验证

- 单测：main（3b0e28c）上 `Test run with 341 tests in 12 suites passed`，退出码 0，没有 `Restarting after`（#13 变基后跑的就是这份代码）。
- 本地 Release 构建：按 CI 的参数分别编 arm64、x86_64（`ARCHS=<arch> ONLY_ACTIVE_ARCH=YES`，`CODE_SIGNING_ALLOWED=NO`），
  两个都 `BUILD SUCCEEDED`、退出码 0；Info.plist 都是 1.4.0 (140)，`lipo -archs` 分别是 arm64、x86_64。

---

## 2026-09-26 · 截图翻译：「钉到屏幕」「完成」用贴了译文的图（feature/screenshot-export-translated）

### 为什么

#12 之后，翻完了选区里显示的是贴了译文的图，但「钉到屏幕」「完成」拿的一直是框选时截的原图（#12 之前也是）：
选区里看着是译文，钉上去、贴出来却是原文。#12 的「故意没做」里留了这一条等用户定，用户同意两个都换。

规则只有一条：**选区里显示的是哪张，就钉哪张、复制哪张。**

### 改了什么

- `ScreenshotOCRSession.imageForExport()`：翻完了（`.translated`）、回贴图画好了给回贴图；没翻、提取原文之后、翻译失败、
  换了源语言给原截图；翻完了但回贴图没画出来（界面退回文字卡片）也给原截图，标成 `originalBecauseOverlayFailed`，调用方提示一声。
  段落本来就都是目标语言、根本没有要画的，也是原截图，不提示「没画出来」。对照模式只是换个方式看文字，拿的还是回贴图。
- `canExport`：识别、翻译进行中，或者回贴图还在画（新加的 `overlayRenderPending`），两个按钮是灰的，跟「翻译」「提取原文」一样。
  钉上去的图不会再更新，这时候拿走的是翻了一半、或者上一批的图。「钉到屏幕」是 plain 样式加了前景色，禁用时不会自己变淡，手动降了不透明度。
- `overlayRenderPending` 由协调器的 `scheduleOverlayRender` 维护：排重画时置 true，渲染循环没活了（`overlayRenderTask` 清掉之后）置 false。
  翻完到最后一张图画好之间还要一次重画（#12 实测 2800×1800、40 行：首次约 277ms、之后约 25ms），这段时间 `translatedImage` 还是上一批的或者 nil——不等的话，刚翻完就点「完成」拿到的是原图。
  置位和清零都在主线程上、中间没有挂起点：排重画要么赶在循环最后一次检查之前（循环多跑一轮），要么在清零之后（新开一个任务、重新置位），不会卡在 true。
- 「完成」按钮是灰的时候真点进来也不往下走：原来的回调是「复制 → 等 0.65 秒 → 退出」，什么都没复制就退出，用户会以为复制好了。
- 提示分开写：「译文截图已复制到剪贴板」「截图已复制到剪贴板」（原来是「已保存到剪贴板」，实际是复制），
  没画出来时「译文图没画出来，复制的是原截图」/「……钉的是原截图」。「提取原文」之后「完成」不覆盖剪贴板，照旧。

### 踩到的坑：翻译全部失败后，回贴图会被一张原图的拷贝写回来

一开始 `imageForExport` 想写成「有回贴图就给回贴图」，不看阶段。变异时发现「只在 `.translated` 才给回贴图」这条没有用例区分得出来，
查下去它是真要的：分批翻译每批结束都会回调进度（失败的那批也回调，译文先填原文），协调器照常排一次重画；
全部失败时失败处理把 `translations`、`translatedImage` 清掉、阶段改成 `.failed`，**之后**排着队的那次重画才在主线程上跑——
按清空了的译文画，没有要重画的段落，渲染器原样返回原图，于是 `translatedImage` 又成了一张原图的拷贝。
不看阶段的话，「完成」会把它当译文图复制，还提示「译文截图已复制」。用例 `renderLandingAfterAFailedTranslationIsNotTreatedAsTheTranslation`。
（界面上这时显示的是出错卡片，不受影响；渲染循环要不要在 `.failed` 时丢掉结果，这次没动。）

### 怎么验证的

- 新增 7 个用例（`截图翻译：回贴图的状态` 这一组）：翻完给译文图（含对照模式）、没翻 / 提取原文 / 失败给原图、换源语言回到原图、
  识别中 / 翻译中 / 还在重画时不能拿、没画出来给原图并标出来、没有要翻的字给原图、失败后迟到的重画不算译文图。
- 变异（每次只改一处，跑这一组 8 个用例）：不看是否在重画、识别翻译中也能拿、不看是否翻完、一律给原图、没画出来也不提示、
  没有译文图就当没画出来——6 个变异各有用例挂，改回来 8 个全过。
- 全部单测：`Test run with 339 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。
  变基到 #14 之后再跑：`Test run with 341 tests in 12 suites passed`，退出码 0。

### 故意没做

- 翻完以后再拿原图的入口：真要的话右键清掉选区重框一次。以后可以在钉图窗口的右键菜单里加「显示原图」，这次先保持小。
- `overlayRenderPending` 的置位、清零在协调器里，没有单测（协调器要开全屏选区窗口、真截屏）；靠上面那段时序分析，和 `canExport` 本身的用例。
- 剪贴板里不同时放译文文字：不同应用挑的类型不一样，有的会贴成文字，反而把「贴图」弄坏。

---

## 2026-09-26 · 选区翻译：替换前确认前台应用改按进程号比（fix/input-assist-frontmost-pid）

### 为什么

跑全部单测时 `frontmostApplicationCheckIsAvailableRightBeforePosting` 偶尔会挂（截图翻译 #12 那几轮碰到过好几次，单独跑、重跑都过）。
挂的那句是 `#expect(!isFrontmostApplication(nil))`：判定写的是「前台应用的 bundle ID == 取词时的 bundle ID」，
测试读的是整台机器的前台应用，那一刻前台应用拿不到 bundle ID（为 nil）时，两边都是 nil，判成「前台没变」。

这不只是测试的问题。生产里有同样的洞：名单检查放行没有 bundle ID 的应用（全局模式下只要认得出是个应用就行——
直接运行的可执行文件、一些 Java / Wine 应用），会话记下的 bundle ID 是 nil；替换前如果前台换成了另一个同样没有 bundle ID 的应用，
或者此刻根本没有前台应用，这道闸都放行。后面还有「焦点控件是不是同一个」「选区是不是原文」两道闸，真写错地方的概率很小，
但第一道闸形同虚设，和它的注释（「最后一道闸」）对不上。

### 改了什么

- 三处「前台还是取词时那个应用吗」——精确替换前的 `invalidTargetReason`、`evaluate` 里给安全闸的 `isFrontmostApplicationUnchanged`、
  粘贴引擎的 `targetIsUnchanged`——都改成比**进程号**，统一走 `InputAssistReplacementSafetyGuard.isSameProcess(expected:current:)`：
  任何一边拿不到进程号都当作变了。进程号对没有 bundle ID 的应用也有，所以这类应用照样能替换，只是换了应用会被拦下。
  同一个应用退出重开（bundle ID 一样、进程号变了）也会拦下，这本来就该拦：原来的控件已经不在了。
- 进程号记在 `InputAssistAppIdentity` 里，和 bundle ID、名字取自同一个 `NSRunningApplication`，就是通过名单检查的那个进程；不参与名单匹配。
  `CandidateSession` 改为直接接收身份、自己取进程号（`appProcessIdentifier`），不再存 bundle ID（只有这三处在用）。
  这样「身份 → 会话」这一步也能单测：它断了的话会话里是 nil，每次替换都判成「应用变了」，选区翻译就再也替换不了。
- 进程号取前台应用的，不取焦点控件所属进程（`AXUIElementGetPid`）：焦点控件可能由别的进程提供（比如沙盒应用的打开 / 存储面板），
  取控件的进程号有可能和前台应用对不上，把原来能替换的场景拦掉；取前台应用的，和原来比 bundle ID 的口径一致。

### 怎么验证的

- 新增 2 个用例：`frontmostCheckTreatsAMissingProcessAsChanged`（纯函数，五种组合，不读全局状态）、
  `sessionRemembersTheProcessThatPassedTheFilter`（当前进程的身份带着自己的进程号，会话里存的就是它；没有身份时为 nil）。
  原来那条用例改成用到不了的进程号（`Int32.max`）和 nil 两种输入，结果不再取决于测试那一刻谁在前台。
- 变异（每次只改一处，跑 `InputAssistTests` 113 个用例）：两边都是 nil 也算同一个进程、身份不记进程号、会话不存进程号、
  没进程号的会话直接放行——4 个变异各有用例挂，改回来全过。
- 全部单测：`Test run with 334 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 故意没做

- 自动取词去重用的 `Fingerprint` 也是按 bundle ID 比、两边 nil 算相同，没改：它同时要求 `CFEqual(element, …)`，
  不同进程的控件不会相等，nil 相等不会让两个应用的选区被当成同一个。
- 没有复现「前台应用没有 bundle ID」的真机场景来跑整条替换流程；靠判定函数的单测和上面的分析。

---

## 2026-09-26 · 截图翻译：译文回贴（feature/screenshot-overlay-inpaint）

### 为什么

用户反馈的第二个问题：译文没放回原位、字有大有小。方案用户已拍板：像微信截图翻译那样**保留原图、抹字重绘**，
不再用卡片盖住整个选区。识别准确度是上一个 PR（#11）。

### 以前的问题

- 一张 96% 不透明的卡片盖住整个选区，原图的背景、按钮、配色全没了，所有字一律半粗、系统主色。
- 字号按行框高度算（`h≥28 ? h×0.45 : h×0.82`，再夹到 11～22）：同是 13pt 的几行画成 11.0 / 13.3 / 11.3 / 12.6，
  22pt 的标题画成 15.4，34pt 的画成 17.8。#11 里临时改成按字符宽度估，但仍夹在 11～22、仍是卡片。

### 改了什么

- **`ScreenshotTranslationRenderer`**（纯计算，后台跑）：
  1. 按原图像素量每段的样式：背景色（行框外一圈的中位色）、文字颜色（和背景反差最大那批像素的中位色）、
     墨迹上下边（从行框中心往外扩、碰到空行停）、墨迹左右端、**字号**、**粗细**、对齐。
     - 字号：量出墨迹实际宽度，用 Core Text 算系统字体排同样的字的墨迹宽度反推，按 pt 量、迭代两次。
       量墨迹上下边时，在文字框左右各放宽一个字宽的范围里几乎整行都是墨迹色的（白字按钮外面的白底）不算字。
     - 粗细：笔画宽度（墨迹横向连续的平均长度）和「同样字号、同样屏幕倍率和颜色的常规体」比，≥1.12 倍画半粗，≥1.33 倍画粗体。
     - 对齐：多行看行首 / 行尾 / 中心哪个对得齐；单行只有左右都碰到实实在在的边、而且离两边一样远才算居中（按钮、居中的单元格）。
  2. 找空白：从原文往右、往左（最多 40 个字宽）、往下（最多 20 个字宽）扫，碰到「东西」为止——这一列和两列之前比突然变色
     （文字、图标、分隔线、色块的边），或者和起点的背景差得太多（渐变走远了）。阈值按背景本身的噪点放大。
     往下碰到下一段时给它留出至少一半原有的间距；扫描起点前面紧贴着字的分隔线单独查。
  3. 排版（`ScreenshotTranslationLayout`）：相差 12% 以内的字号归成一档；单行先一行放下（往空白延伸、最多缩到 82%），
     再折行往下（最多缩到 70%），最后截断；段落在原宽度里重排、行距跟原文，最多缩到 62% 再截断；
     下限都不超过原字号。段落外框里、往下长时碰到的零星东西用 TextKit 的 exclusionPaths 绕开。
  4. 抹字：只抹原文各行（外扩一点盖住抗锯齿的边，外扩不越过紧挨着的分隔线和别的东西，也不抹到没翻的相邻段落），
     译文延伸出去的地方本来就是空白、不动。
     用四周 3 像素宽的一圈拟合平面（纯色、线性渐变都能还原），迭代三次剔除离群像素；
     背景有颗粒时，从四周相对平面的残差里随机抽来加上。
  5. 用截图自己的色彩空间画译文，颜色不偏。
- 没翻的段、译文和原文一样的段、翻译失败的段，一个像素都不动。
- **协调器**：每翻完一批就在后台重画一次；画的时候又来的请求合并成画完后的一次；画好时选区、源语言、识别结果变了就丢掉。
  量样式只看原图，同一批识别结果的多次重画共用一份缓存。
- **界面**：选区里直接显示回贴好的图（圆角和选框一致），不加卡片；第一批还没画出来时什么都不盖，看到的就是原图。
  「提取原文」之后显示识别到的文字卡片，方便核对复制的内容。回贴图画不出来（极少见）时退回卡片列出译文。
  删掉 #11 的过渡版覆盖层。

### 实测

- 字号：Vision 的行框宽度其实很准（比真实排版宽度多 −0.06～+0.26em），但按钮上的两字短标签会宽出一成多；
  最终按墨迹宽度反推，英、中、日、韩 × 11～28pt × 四种字重 × 深浅色 240 组，都在 ±2.5% 以内。
- 粗细：笔画宽度的绝对值随书写系统、字号、深浅色变化很大，中文常规体（0.17～0.23 个字号）比英文粗体（0.19～0.22）还粗，
  定一个绝对阈值分不开；和同条件常规体比之后：常规 0.97～1.10、中等 1.08～1.22、半粗 1.17～1.32、粗体 1.24～1.56，
  240 组里常规体一组都没判成粗体。（这是 2 倍屏、旧量法的数字；第六轮后自查改成按覆盖率算、补测了 1 倍屏，新数字见审核第六轮。）
- 性能：优化编译的命令行程序，2800×1800 像素。40 行界面文字：首次渲染 234ms，带缓存的后续重画 24ms（识别本身 859ms）；
  20 段三行段落、长译文要缩小重排：首次 102ms，重画 54ms（识别 1077ms）。（审核前第一版是首次 96ms / 重画 21ms，
  后来加了按竖条往下扫、TextKit 排版和墨迹测量。第六轮后自查加了分隔线排除、按覆盖率算笔画、逐像素取文字颜色，
  40 行那组首次渲染变成 263～269ms，重画还是 23～25ms；段落那组没重测。）
- 逐张看过渲染结果：英文界面、英文文章、深色界面、彩色标签和按钮、中译英（译文变长往右延伸）、日译中、俄译中、
  带图标和开关的列表、窄按钮（缩小后截断）、紧凑段落（给下一段留间距）、渐变背景、噪点背景。
- 单测 `Test run with 300 tests in 11 suites passed`，退出码 0（#11 的 284 + 新 16；审核第一轮后 304，第二轮后 307，第三轮后 310，第四轮后 314，第五轮后 316，第六轮后 321，第七轮后 323，第八轮后 326，第九轮后 328，第十轮后 329，第十一轮后 330，第十二轮后 330，第十三轮后 331，第十四轮后 332，第十五轮后 332）。回贴的端到端用例用真实 Vision：
  回贴后再识别一遍，原文没了、译文在原位（横竖都在 3pt 以内）、字号在 6% 以内、粗细一致、颜色和背景保留、
  没翻的段和按钮外面一个像素都不变、渐变上抹过的地方和四周差不到 8 个色阶、长译文停在开关前面。

### 踩的坑

- 在像素单位里量字宽：2 倍屏上 13pt 被当成 26pt，SF 在 20pt 以上换成更紧凑的字形，反推出来的字号大了一成。
- 判断「碰到东西」的容差按文字反差放大：浅色标签和白底只差三十几个色阶，被当成空白，标签里的字没居中。
- 渐变、噪点背景上左右两边都「碰到东西」、距离还差不多，被误判成居中；改成区分突变和慢慢变、阈值按噪点放大。
- 颗粒按四周的标准差均匀加：强度偏弱、质感也不对，噪点背景上能看出一块；改成从四周的残差里抽。
- 段落往下占满了两段之间的空隙，和下一段贴在一起分不清。
- Release 配置跑单测时测试宿主一启动就崩（加固运行时不让注入测试包），性能改用 `swiftc -O` 单独编的程序测。

### 审核第一轮（#12）

1. **1 倍屏上的小字把自己当成障碍**：往外扫的起点只比墨迹多 0.15 个字宽，11pt 在 1 倍屏上才 1.65 像素；
   而「突变」是拿这一列和两列之前比的，那一列还在字里。以竖画结尾的「Install」一开扫就「碰到东西」，
   译文不肯往右边的空白长，被缩到 8pt。现在起点保证这一列和两列之前都在墨迹外至少 2 像素（往下扫同理）。
   用例在 1 倍屏上画 11pt 的「Install」：修复前右边只能长到 50pt、字缩到 8pt，修复后长到选区边、字号不变。
2. **两段译文抢同一块空白**：每段能长到哪是各自从原图量的，带边框的一行里两个等距的标签都判成居中，
   都把中间的空隙算成自己的，译文都变长时叠在一起（修复前的用例里，回贴图两段原文正中间那一列上有字）。
   排版完再两两查：字身撞上了，左右相邻的从两段原文正中间劈开，上下相邻的让上面那段停在下面那段的译文之上，
   重排后再查。限制只会越收越紧、最多收到原文自己的范围，几轮之内一定停得下来。

单测 `Test run with 304 tests in 11 suites passed`，退出码 0，没有 `Restarting after`；新增的 4 个用例撤掉修复后全挂。

### 审核第二轮（#12）

1. **段落外框里的东西没查**：第一行长、第二行短时，短行旁边的图标或别的标签在段落外框里面，
   按整个外框宽度重排会画到它上面（只查了段落下方，外框里原文重叠的两块又分不开）。现在：
   - 逐行查外框里每行左右的空当，碰到东西就记成「绕开的地方」；
   - 往下扫改成按竖条（每条两个字宽）各自扫：多数竖条碰到东西的地方才是底线，少数竖条早早碰到的
     （段落右下角的图标）只绕开它实际占的那几行（找到下沿为止），不再把整段都挡住——
     之前那个图标只比第二行低 4pt，整段就没法往下长，字被缩到 62%；
   - 排版和绘制统一改用 TextKit（`TextLayout`：`NSLayoutManager` 关掉后台排版后可以在后台线程用），
     用 exclusionPaths 绕开，截断也由它在末行加省略号。TextKit 按整个行框（连行距）判断碰撞，
     交给它之前把绕开的地方上下各收进行距多出来的那一半，不然擦着行距 0.3pt 的图标也会把下一行劈成两截。
2. **小于 8pt 的原文被放大**：缩小的下限是 `max(8, 原字号 × 比例)`，原文 6pt 时下限 8pt 反而比原字号大。
   现在每个下限都封顶在原字号：原文本来就很小时宁可截断也不放大。

用例：图标在段落外框里（真实截图）——图标一个像素都不变、字号不缩、没有哪一行被劈成两截；
绕开的地方只到图标下沿；TextKit 逐行查不压到障碍；6pt 的原文不被放大。撤掉修复时图标被盖住、6pt 被放大成 8pt。
单测 `Test run with 307 tests in 11 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第三轮（#12）

1. **外框叠在一起的两段还会撞**：段落短行旁边的标签落在段落外框里，两段外框重叠，「从中间劈开」用不上，
   之前直接不管。标签也译长、放不下只能折行往下长时，和段落往下长的第三行抢同一块地方。现在这种情况让行数多的那段
   （段落）把另一段的译文当成障碍绕开排；判断撞没撞时，已经在绕开对方的不算撞。单行也照顾到：
   同一行里的障碍收紧它左右能长到的范围。
2. **单行的行框高度不对**：按「Ag字」量，而且 `NSAttributedString.size()` 只按主字体算行高。实测系统字体 14pt 下
   `size()` 对各种文字都给 17pt，TextKit 实际排出来缅甸文 31pt、高棉文 28.6pt、藏文 24.2pt、天城文 21pt——
   按 17pt 定行框，字溢出、竖直位置偏下 7pt。现在不折行也用 TextKit 量（画的时候用的就是它），行高按译文本身。
   参照常规体的位图上下也多留了余量，免得这类字被切掉。

用例：段落外框里的标签（真实截图，选区收窄到标签只能折行）——先断言各排各的会撞，排完段落的每一行都不压到标签的译文；
缅甸文单行——行框高度够、这一行居中在原文那一行上、字全排进去了。撤掉修复时三个用例都挂。
单测 `Test run with 310 tests in 11 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第四轮（#12）

上一轮把单行的行框改成按译文本身量之后，顺着暴露出同一类问题的另外两处：

1. **段落的行高压不住后备字体**：段落的固定行高跟原文行距、封顶 1.9 倍字号（14pt 时 26.6pt），缅甸文一行要 31pt，
   字压到上下行、伸出排版范围；让字居中的基线偏移也是按主字体算的。现在行高不小于译文本身（TextKit 实测）的自然行高，
   基线偏移也按译文本身算好、随排版结果交给绘制。
2. **单行竖着放不放得下没查**：只查了横向，高的文字会伸出选区上下边、按钮上下沿，撞车处理收紧下边界对它也不起作用。
   现在：
   - 边界多了「上边界」（往上扫到的第一处东西）和「本行下界」（下方最近的边，只留 0.1 个字宽）——「往下长」的界限
     要给下一段留一半间距，拿来限制本行会让按钮里下边比上边多留一大截；
   - 单行的**墨迹**要落在上下边界之间，放不下就上下挪，挪了也不行就缩小，70% 还放不下接着缩（最小 8pt、不超过原字号）；
   - 墨迹用 Core Text 量字形轮廓、基线取 TextKit 排出来的位置——缅甸文的元音符号叠在上面，墨迹在行框里不居中，
     「行框上下各去 15%」的估法一试就伸出了按钮上沿；竖直对齐也改成译文墨迹的中心对准原文墨迹的中心；
   - 撞车处理上下相邻时，上面那段的两个下界和下面那段的上边界一起收紧。
3. **顺带修了一个量样式的真 bug**：白字蓝按钮放在白底上，按钮外面的白底和字同色，量墨迹上下边时被当成了字——
   20pt 高的按钮上，墨迹带伸到按钮外面、字号量成 18.7pt（真值 15pt）。现在几乎整行都是「墨迹色」的行不算字，量到这里就停。
   之前按钮用例的按钮够高，没暴露出来。

用例：多行缅甸文段落（行高不小于自然行高，画出来的墨迹不出排版范围）；单行缅甸文上下都紧时缩小、只有下面有地方时往下挪不缩
（用画出来的真实墨迹判断）；紧凑按钮（20pt 高、15pt 字）里译成缅甸文，按钮上下外面一个像素都不变；
白字按钮的字号和墨迹带。撤掉修复时 4 个用例都挂。
单测 `Test run with 314 tests in 11 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第五轮（#12）

1. **单个汉字的一横被当成按钮外的底色**：第四轮「几乎整行都是墨迹色的行不算字」只在文字框里数，「工」「王」的一横
   能占满文字框，被当成底色，墨迹带只量到半个字——抹字抹一半（「工」抹完还剩 40 个深色像素）、译文位置也偏。
   现在「整行同色」在文字框左右各放宽一个字宽的范围里数：按钮外的底色是一整片，字的笔画不会伸到字外面。
2. **紧挨着字的分隔线被跳过**：扫描起点在墨迹外几个像素（1 倍屏 14pt 约 5 像素），紧贴着字 1～2 像素的分隔线整个被跳过，
   长译文越过它伸进隔壁单元格。现在检查跳过的那一小段：分隔线是一整条，在字的上方和下方都连着；字自己的抗锯齿只贴在
   笔画边上、不会伸到字外面——上下两头都连着颜色的列才算分隔线（往下、往上跳过的几行同理，看字的左右两头）。

用例：「工」「王」（墨迹带盖住整个字，译成「.」后原字的地方一个深色像素都不剩）；1 倍屏「Install」后面 2 像素处的
1 像素分隔线（能长到的范围和排出来的译文都不越过它）。撤掉修复时两个都挂。
单测 `Test run with 316 tests in 11 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第六轮（#12）

1. **抹字的外扩把紧挨着的分隔线抹掉了**：抹字范围是各行墨迹上下左右统一外扩（左右 0.12 个字宽），24pt 的字离浅灰
   表格分隔线 1pt 时，外扩 2.88pt 把分隔线抹掉了——扫描已经不让译文越过它，抹字却不看。现在抹字范围在量样式时一起量好、
   交给排版：左右的外扩碰到紧挨着的分隔线（第五轮那个判法：字的上方和下方都连着）就停；隔着一列干净的背景之后又出现颜色，
   也停在那里（字的抗锯齿贴着笔画，中间不会隔着干净的背景，隔开了就是别的东西）；上下不越过首行上面、末行下面量到的边。
   没碰到东西时还是照原来外扩，抗锯齿的边照样盖住。只削外扩的那一圈，每行的墨迹带本身整条照抹。
2. **渲染失败时旧图还挂着**：前几批画出来了、后面一次渲染失败（比如内存紧张时位图分配不出来），旧图一直显示，
   界面优先显示图，退回卡片列出最新译文的那条路走不到。现在渲染结果统一交给会话：画出来换新图，画不出来清掉旧图、退回卡片。

用例：24pt「Install」后面 1pt 处的 1pt 浅灰分隔线，回贴后分隔线一个像素都不变、原文的地方换掉了（审核给的例子）；
第五轮的 1 倍屏分隔线用例也加上了渲染后查分隔线（那个场景离字 2 像素，原来的外扩碰不到，只当守护）；
渲染成功一次再失败，旧图清掉、退回卡片。撤掉修复时 24pt 分隔线和清旧图两个用例都挂。

提交后等审核的时候（机器人那边的模型接口一直 503）自查，又改了三处：

1. **段落相邻两行各抹到中线会削进字里**：第一版让段落相邻两行各抹到两行的中线。可行距紧时，两行的墨迹带会叠在一起——
   墨迹带往下扩时允许隔开 0.2 个字宽的空行，上一行的带扩进了下一行，下一行的也扩进了上一行——中线落在带里面，
   削进了上一行自己的字，它比下一行长出来的那一截，字的下沿就漏抹了。用手工构造的识别结果（第二行的行框往上伸进
   第一行 5pt，Vision 真会这样）试了几种紧行距，抹字范围全都削进了墨迹带，其中 g/y/p/q 的下伸部分确实被削到。
   现在去掉中线这条（段落里相邻两行本来都要抹，用不着互相让），每行的墨迹带整条照抹，只削外扩的那一圈。
   用例 `tightParagraphLinesAreErasedCompletely`：拉丁文和中文各一组，每行的抹字范围都盖住墨迹带，渲染后第一行长出来的
   那一截一个深色像素都不剩；改回中线时拉丁文那组还剩 204 个深色像素。
2. **紧挨着字的深色分隔线被当成了字**：审核那个例子是浅灰分隔线，过不了墨迹阈值；换成深色（0.1、0.4 灰）的，离字 1～3pt，
   量墨迹左右端时整条算进了字——回贴时被抹掉，往外扫从它外面开始、能长到选区边上（译文越过它伸进隔壁格子），
   它每一行都有颜色，还把墨迹带一路撑高（24pt 的字从 17.5pt 撑到 27pt），细线拉低了笔画宽度，字号跟着量偏。
   现在量墨迹左右端时，在字的上方和下方都连着的列不算字（同第五轮的分隔线判法）；量出左右端后，只在这个范围里重量一次
   墨迹带、再重量一次左右端；墨迹带和左右端的右边界改成不含（原来含 `Int(maxX)` 那一列，第二遍会把紧贴墨迹的下一列带进来）。
   用例 `darkDividerNextToTheTextIsNotTreatedAsText`：0.1、0.4 灰的分隔线在 2 倍屏 24pt 字后 1pt、1 倍屏 14pt 字后 2 像素，
   分隔线不算进墨迹、译文长不过去、有没有分隔线量出来的字号一样、回贴后分隔线一个像素都不变。撤掉修复时三组的四项断言全挂。
3. **1 倍屏上文字颜色量浅、粗细判错**：查第 2 条时发现，1 倍屏上 14pt 常规体的「Install」被判成半粗、字号量成 12.9pt。
   做了 256 张图的实验（4 段文字 × 4 个字号 × 4 个横向、2 个纵向亚像素位置 × 常规/半粗），1 倍屏粗细判错 8%，两个方向都有，
   判错了字号按错的粗细重算、偏 7%，同一张图里的译文就忽大忽小。两个原因：
   - 文字颜色原来隔一个像素取样、把反差七成以上的都算进来取中位色；1 倍屏上笔画才一个多像素，完全盖满的像素少，
     近黑的字（29）量成了 54～67 的深灰——译文画浅了，量粗细时覆盖率也按浅色折算、量粗了。现在逐像素取样，
     只取反差最大的那四分之一取中位色，1 倍屏 14pt 量到 29、11pt 拉丁文 38～41，2 倍屏不变。
   - 笔画宽度原来数「过阈值的像素个数」，一笔竖画落在像素格的不同位置会数成 1 个或 2 个，差出三成。现在按覆盖率算
     （段里各像素的覆盖率加上两边各一个抗锯齿像素的），各段取中位数（横画、弧线的顶和底是长长一段，平均会被拉偏）。
     试过参照渲染在四个亚像素位置各画一遍取平均，没有用（偏差来自颜色量浅，不在参照的位置），没留。
   - 按新量法重新标定（英、中、日、韩和短标签 × 11～28pt × 四种字重 × 深浅色，1 倍、2 倍屏各 200 组）：
     2 倍屏常规 0.99～1.02、中等 1.13～1.20、半粗 1.23～1.31（短词 28pt 一组 1.72）、粗体 1.34～1.93；
     1 倍屏常规 0.91～1.13、中等 1.09～1.24、半粗 1.17～1.36、粗体 1.26～1.91。常规和半粗的分界从 1.12 挪到 1.15，
     两种倍率下常规体都一组没判错；中等字重判成常规或半粗都有；半粗和粗体在 1 倍屏上偶尔互相认错（字号差 2% 左右）。
     另一组 768 张图的实验（上面 256 张再加粗体、加 2 倍屏）改前改后对比：改前 1 倍屏常规错 13、半粗错 45、粗体错 36，
     2 倍屏半粗错 4、粗体错 30；改后（阈值还是 1.12 时）只剩 1 倍屏 14 组半粗判成粗体，常规和非常规一次都没判反。
   用例 `fontSizeWeightAndColorAreMeasuredOn1xScreens`：深浅色 × 中英 × 11/14pt × 4 个亚像素位置 × 常规/半粗，64 组，
   常规和非常规判对、字号在 6% 以内、文字颜色离真实颜色不超过 25。撤掉这两处修复时挂 42 条（颜色 29、粗细 9、字号 4）。
   代价：首次渲染慢了一点（见「实测」的性能），带缓存的重画不变。

单测 `Test run with 321 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第七轮（#12）

这一轮只有一条 P2（机器人上游恢复后审的 84a9ffa）：**排不全的测量当成了放得下**。给的宽度比一个字还窄、又有要绕开的地方时，
TextKit 会半路停下。先写小程序实测了 TextKit 的行为：
- 没有要绕开的地方时，再窄它也照排（每行一个字，超出容器宽度），截断模式下留第一个字加省略号，字符都算排进去了；
- 宽 8pt、最上面 20pt 整宽被挡住：一个字都不排，`usedRect` 是零；
- 宽 8pt、第一行下面被挡住：「安装」只排进 1 个字，「Install now」只排进 1 个字母，量出来正好一行高；
- 宽度够一个字（30pt）时，它会挪到挡住的地方下面接着排完。

单行折行那一步是带着要绕开的地方量的，量出一行高（或者 0）就当成放得下：原文抹掉了，译文少了后半截、连省略号都没有，
或者整个没画。现在测量结果带上「字是不是都排进去了」（`Measurement.complete`，取 `TextLayout.laysOutEverything`），
折行那一步和段落排版都要排全才算放得下，排不全就接着试更小的字号。单行最后一步截断成一行，本来就不带绕开的地方，
TextKit 至少排出第一个字加省略号；段落缩到最小还绕着障碍排不全，就不绕了，宁可压到障碍，也不能让译文少一截。

用例：`narrowSpaceWithAnObstacleBelowStillShowsTheTranslation`（真实 TextKit：6pt 宽的原文、左右没地方长、下面紧挨着要绕开的图标，
译成「安装」，按渲染器画的方式排一遍，字都排进去了）；`paragraphThatCannotBeLaidOutAroundObstaclesStopsAvoidingThem`
（假的量字，一有要绕开的地方就排不全：最后不再绕开障碍）。撤掉修复时两个都挂。
单测 `Test run with 323 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第八轮（#12）

两条 P2：

1. **字伸出了允许的横向空间**：窄窄的「I」左右都长不出去、译成中文时，TextKit 照样每行排一个字、字伸出容器；
   而它给的 `usedRect`、`boundingRect` 都压在容器宽度以内（实测 24pt 的字放进 3pt 宽的容器，量出来还是 3pt），
   折行那一步只查排全和高度就收下了。渲染器画的时候又不裁，字压到旁边的分隔线、图标上；撞车检测看的也只是窄窄的框。改了三处：
   - 测量结果的宽度改用字本身的宽度：逐行用 Core Text 量这一行的字（不算行尾空格），取最宽的一行（`TextLayout.widestLine`）；
   - 单行折行和段落排版都要横着不伸出去才算放得下（量出来的宽度是向上取整的，跟可用宽度比也先取整，不然 100.2 放进 100.3 会被误判）；
   - 截断成一行时，第一个字加省略号要放得进框里，放不下接着缩；8pt 还放不下的，画的时候按排好的框横着裁（左右各多留一个像素给字形出头，
     竖着不裁——后备字体的字可能比行框高），怎么都画不到框外面。
2. **抹字抹到了没翻的相邻段落**：行距紧时墨迹带会隔着空白扩进上一行或下一行，那一行可能在别的段里（句末标点、
   下一行大写开头都会断开）；只翻这一段时，抹字连别人的字一起抹掉。改了两处：
   - 墨迹带隔着空白又碰到墨迹时先往外看一眼：连着高过 0.45 个字宽的是另一行字，停在空白这边；矮的（i 的点、声调、
     泰文缅甸文的元音符号、下划线）照旧算本行的。量样式也跟着准了（墨迹带不再把下一行算进来）；
   - 渲染时，把挨着要抹的地方的不重画段落（没有译文、译文和原文一样）各行的墨迹量出来（缓存起来），抹字范围从它们身上削掉，
     四周多留一个像素：横着不重叠的算并排、削左右，横着重叠的算上下叠着、削上下，都不削进自己这一行的墨迹。
     两行的字真碰在一起（没有空白，两边的墨迹带互相扩进对方）时，交界处分不清是谁的像素，按两行墨迹的中线各让一半。
     起初按「竖着重叠多少」分并排还是叠着，字碰在一起时两条墨迹带竖着叠了一大半，被当成了并排、一点没削，改成看横着重不重叠。

第六轮后自查的紧行距用例（`tightParagraphLinesAreErasedCompletely`）原来的场景行距 14、15pt，字之间隔着一两个像素的空白，
加了第 2 条的第一处之后两行的墨迹带不再叠在一起，改成字碰在一起的 13pt（改回中线规则时它照样挂）。

用例：`narrowLabelIsNotWrappedWithGlyphsWiderThanTheSpace`（排版：一个字都放不下时要么截断成一行，要么每行都不比框宽；
截断时第一个字加省略号放得进框里）；`translationNeverDrawsOverNeighborsWhenNotEvenOneGlyphFits`（24pt 的「I」夹在两条深色分隔线中间，
译成「安装」，两条分隔线一个像素都不变）；`erasingOneBlockKeepsTheUntranslatedBlockBelowIntact`（上下两段、只翻上面那段：
行距 15pt 中间隔着空白时下面那段一个像素都不变；13pt 字碰在一起时下面那段的字身不被抹）。逐项撤掉修复：去掉折行的宽度检查、
去掉截断时的横向检查，排版用例挂；去掉绘制时的裁剪，分隔线用例挂；去掉保护、去掉跳空白前的检查，上下两段的用例都挂。
性能：40 行那组首次渲染 267～278ms，重画 24～26ms（上一轮 263～269 / 23～25）。
单测 `Test run with 326 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第九轮（#12）

一条 P2：**折行后第一行没按实际排在第一行的字对齐**。译文前面是拉丁字母、后面是后备字体的高文字（缅甸文）时，折行后第一行
只剩「Settings」，行框和基线都跟整段排一行时不一样；折行那一步却按整段排一行的墨迹定位，第一行比原文那一行高了 5pt。
顺着查了同一类的另外两处：
- 段落排版也是按整段排一行的墨迹定位的。行高按整段里最高的字定（缅甸文 31pt），第一行要是只排到了拉丁字母，字沉在固定高的
  行框底部（多出来的空间堆在上面）——实测第一行画低了 10.5pt（泰文 2.5pt）。偏得比审核说的那处还多，一起改了；
- 截断成一行时（高的字被截掉）实测只差 0.25～1.25pt，没动。

改了四处：
- `firstLineInk` 只量排在第一行的字（原来整段当一行量、取所有字里最高最低的）；
- 折行那一步：先按整段一行估个位置，量完折行再按第一行实际的墨迹对齐原文那一行；位置变了，要绕开的东西跟着挪、再量一次（最多两次）；
- 段落排版同样量完再按第一行实际的墨迹对齐。要量得和画的一样，量字的函数加了 `baselineOffset` 参数（字在固定高的行里往上挪多少），
  段落传实际用的那个，其余传 nil；
- 查的时候发现 `firstLineInk` 把 `baselineOffset` 算了两遍：TextKit 给的基线位置里已经算上了，带着这个属性建的 Core Text 字形轮廓
  又挪了一次（实测偏移 3pt 时预测的墨迹挪了 6pt、画出来只挪了 3pt）。量字形轮廓时去掉这个属性。原来段落量出来的墨迹没用上，没暴露。

用例：`wrappedMixedScriptTranslationAlignsItsFirstLine`（单行折行：「Settings မြန်မာစာ」折成两行，画出来第一行墨迹的中心对准原文那一行，
差 1pt 以内）；`paragraphWithTallScriptLaterAlignsItsFirstLine`（段落：前面是英文、后面是缅甸文或泰文，第一行只排到英文，同样要对准）。
逐项撤掉修复：折行不重新对齐，折行用例挂（差 5pt）；段落不重新对齐、第一行墨迹还量整段、字形轮廓不去掉基线偏移、段落量的时候不传偏移，
段落用例都挂。性能：40 行那组首次渲染 262～266ms，重画 24～25ms。
单测 `Test run with 328 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第十轮（#12）

一条 P2：**选区紧紧框着单个字时，宽的笔画又被当成了底色**。第五轮让「整行同色」在文字框左右各放宽一个字宽里数，
可选区紧紧框着「工」「王」时，放宽的部分被截图边缘截掉了，剩下的差不多全是字，一横就能占满 85%，又被当成按钮外面的底色：
墨迹带停在横画前面、墨迹左右端缩到中间，大半个字抹不掉。复现了：左右只留半个 pt 时「工」抹完还剩 40 个深色像素、「王」224 个；
字正好贴着截图边缘时 39、254 个（左右留 2pt 时一横只占 84%，碰巧没事）。

改法按审核说的：字框外面取不到半个字宽就不判「整行同色」。取得到半个字宽时，一横（最多一个字宽）最多占三分之二，到不了 85%。
也试过再加一条「字框外面那部分也要几乎都是墨迹色」，但有了上面这条它在这些场景里不起作用、也没有用例盯着，没留。

用例 `tightlyCroppedSingleCharacterIsErasedCompletely`：「工」「王」× 左右留半个 pt / 正好贴边，手工给识别结果（这么小的图 Vision 不一定认得出来），
墨迹带和墨迹左右端都盖住整个字，译成一个很小的「.」渲染后，原来字的地方除了新画的那一点一个深色像素都不剩。撤掉修复时四组都挂。
单测 `Test run with 329 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第十一轮（#12）

一条 P2：**同一个字里隔得远的几笔没算进墨迹带**。「二」「三」的几横之间隔得比 0.2 个字宽远得多，墨迹带从离框中心最近的那一横往外扩，
碰到这段空白就停了。复现了：「二」的墨迹带只盖住一横（2.5pt 高），抹完还剩 164 个深色像素；「三」只盖住中间一横，剩 248 个。

改法：往外扩时空白超过 0.2 个字宽也不马上停，还在识别出的这一行的框（Vision 行框）里就接着找——几横之间隔得再远，都在这一个字的框里；
出了框才按 0.2 个字宽截断。框伸进了相邻的行也不怕：第八轮加的「隔着空白又碰到墨迹先看一眼」照样生效，那一行连成一大片的字身会被挡住。

用例 `characterWithWidelySeparatedStrokesIsErasedCompletely`：「二」「三」，手工给识别结果（这种字 Vision 可能认成「=」「ニ」），
墨迹带盖住整个字，译成一个很小的「.」渲染后，原来字的地方除了新画的那一点一个深色像素都不剩。修之前两个字都挂。
单测 `Test run with 330 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第十二轮（#12）

一条 P2：**排译文时没避开保留下来的原文**。第八轮只让抹字护住了不重画的段落；字碰在一起时，上面那段的墨迹带扩进了下面那段，
排版还按扩进去的墨迹带排、也不知道下面有保留的字，译文的中心偏下、画到了下面那段上。第八轮的用例在这种情况下只画了一个点
（当时在注释里写明了这个局限），没测出来。

改法：护住的逻辑从只削抹字范围，扩成一段要重画的段落处处都让开（`retain`）：
- 抹字范围照旧削（`trimmed`）；
- 墨迹带也照同样的分界削：字碰在一起时削到两行墨迹的中线，译文的中心就不会被扩进去的部分带偏；
- 在上面、下面的收紧上下边界（`minY`，`maxY` 和 `bodyMaxY`）：中间隔着空白就是对方墨迹的边，字碰在一起就是中线；
  只管横着够得着的（译文左右长不到的地方，上下有什么都碍不着）。
分界统一由 `boundary(below:)`、`boundary(above:)` 给。每次渲染重算，不进缓存：哪些段落不重画，每翻完一批都在变。

起初还把同一行左右挨着的保留原文加进要绕开的东西、另外跳过它们不参与上下收紧，写了用例才发现两条都用不上：
左右能长到哪由扫描管（扫到它的字就停），排版时上下边界又不会收进这一行自己的墨迹带，同一行的保留原文怎么都挤不动译文。
两条都删了，那个同一行的用例也删了（它区分不出任何东西）。

用例 `erasingOneBlockKeepsTheUntranslatedNeighborIntact`（改写第八轮的，四组）：行距 15pt 隔着空白、翻上面那段，下面那段一个像素都不变；
13pt 字碰在一起、翻上面那段（「短句」和缅甸文各一组），下面那段的字身不被抹、也不被画上字；13pt、翻下面那段成缅甸文，上面那段基线以上不动。
逐项撤掉：墨迹带不削，两组字碰在一起的挂；下边界不收紧，缅甸文那组挂；上边界不收紧，翻下面那段的挂；只护抹字（第八轮的状态），两组都挂。
单测 `Test run with 330 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第十三轮（#12）

一条 P2：**往下扫时看不见一个像素粗的线**。1 倍屏上，原文下面一条一像素宽的竖连接线，往下一行行扫时每一行只有一个像素变色，
「突变」（三个像素）、「走远了」（两个像素）都凑不够数，线再长也看不见；长译文折行往下长时就压在它上面。横着扫有同样的问题：
标题后面一像素高的横线，往右扫时每一列也只有一个像素变色。两个都复现了（竖线被压 22 个像素，横线 41 个）。

改法：扫的时候再记一下同一行（横着扫）、同一列（往下扫）上连着变色的像素，一连三个就是细线，停在它开头的地方、算碰到了边。
阈值和「走远了」一样按背景噪点放大；渐变会先被「走远了」拦下；噪点连着三个都超标的概率可以不计。
也改过找障碍下沿的函数（细线在它看来一碰就结束），撤掉时用例照样过——下一次往下扫又会碰到这条线，一段段接起来照样盖住整条线，就没留。

用例 `thinLinesNearTheTextAreNotPaintedOver`（1 倍屏）：「Install」右边有分隔线挡着、长不开，下面有一像素宽的竖连接线，长译文折行往下长，
连接线一个像素都不变；「Section」右边 4pt 起一像素高的横线，译文比原文长，横线一个像素都不变。撤掉横着扫的检测，横线那组挂；撤掉往下扫的，竖线那组挂。
性能：40 行那组首次渲染 276～278ms，重画 24～26ms（上一轮 262～266 / 24～25，扫描时每个像素多记一个计数）。
单测 `Test run with 331 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第十四轮（#12）

一条 P2：**两像素高的横线没被当成段落短行旁边的东西**。往右扫到一条两像素高（2 倍屏上 1pt）的深色横线时，第一列突变、变色各两个像素：
突变凑不够三个，变色够了两个，就按「走远了」停下（`hitEdge` 为 false，当成渐变）；段落查短行旁边的东西时只认「碰到了边」，线就漏了。
它在段落外框里，往下扫也管不到，长译文压上去。复现了。

改法：分清渐变和锋利的细东西——渐变是慢慢变的，变了色的像素不会同时是突变；两像素的线，两个像素既变了色、又是突变。
按「走远了」停下之前先看：既变了色又是突变的像素有两个，就算碰到了边。往下扫也照样认（两像素宽的竖线在那里本来按「走远了」停下、
停的地方也当成障碍，现在只是改标成碰到了边，行为上看不出差别，为了两个方向一样也加上了）。

用例 `twoPixelRuleBesideAShortParagraphLineIsAvoided`（2 倍屏）：两行的段落，短的第二行后面 8pt 起一条 1pt 高的深色横线（在段落外框里），
译文长得多，横线一个像素都不变。修之前挂。
单测 `Test run with 332 tests in 12 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第十五轮（#12）

一条 P2：**淡的细线认不出来**。2 倍屏上一条 1pt 高的浅灰线（#EEEEEE）在白底上：离白底只差 29，过了突变的门槛（12），
到不了「走远了」的 40；第十三、十四轮认细线的两条（一连三个变色、两个既变色又突变）都是按 40 数的，它一个像素都不算。
段落短行旁边、原文下面的这种线就漏了，译文压上去。1 倍屏上一像素的浅灰竖线、横线也一样。三种都复现了。

不能直接把门槛降到 12：渐变走远了所有行一起过 12，会被当成细线。细线和渐变的区别在横截面：细线是一道窄窄的脊，
和上下（竖线是左右）隔两个像素的地方明显不一样；渐变在那个方向上是平的。所以改成：离底色超过突变的门槛、而且和上下
（左右）隔两个像素的像素也不一样，这样的点顺着扫的方向一连三个，就是细线。深色的一像素线也满足，原来按 40 数的那条就不要了；
两个像素的深色线在第一列就认（第十四轮那条）照旧。

用例：第十三轮的 `thinLinesNearTheTextAreNotPaintedOver`（1 倍屏竖线、横线）和第十四轮的 `twoPixelRuleBesideAShortParagraphLineIsAvoided`
（2 倍屏段落短行旁边的横线）都加了浅灰（#EEEEEE）的一组。修之前三组浅灰的都挂，深色的照过。

跑全部单测时 InputAssist 的 `frontmostApplicationCheckIsAvailableRightBeforePosting` 挂了两次（`isFrontmostApplication(nil)` 为 true：
测试进程里拿不到前台 App 时两边都是 nil），单独跑、再跑一遍全部都过——跟环境时机有关，和这个 PR 无关，是之前就记下的那个不稳定的用例。
单测 `Test run with 332 tests in 12 suites passed`，退出码 0（第三遍）。

### 故意没做

- 「钉到屏幕」和「完成」仍然用原截图，没有换成回贴后的图：要不要换、两个都换还是只换一个，等用户定。
- 字体一律用系统字体：原文是衬线体时，译文是无衬线体。
- 竖排、旋转的文字不处理；照片上细节很多的地方，平面加颗粒的补法看得出来（不是按内容补全）。
- 截断成一行、被截掉的正好是后备字体的高文字时，竖着按整段的墨迹对齐，实测差 0.25～1.25pt，没专门处理。
- 每批都整张重画，不做增量：带缓存后二十毫秒左右，够快。
- CHANGELOG 等发版时再写。

---

## 2026-09-26 · 截图翻译：识别准确度（fix/screenshot-ocr-accuracy）

### 为什么

用户反馈截图翻译两个问题：识别不准；译文没放回原位、字有大有小。分两个 PR：这一个修识别，
下一个把覆盖层改成像微信截图翻译那样保留原图、抹字重绘（用户已拍板）。

### 怎么查的

写了离线探针：用 AppKit 按指定字号 / 颜色把文字画进 2x 位图当「截图」，标准答案就是画进去的字；
**原样调用**旧版 `VisionOCRService` 和几种改法对比字符错误率（忽略空白的编辑距离）；
译文用 macOS 26 的 `TranslationSession(installedSource:target:)` 在脚本里离线生成。21 张合成图覆盖
中日韩、法德俄西、深色、彩色标签、浅灰小字、非 Retina 小字、图片背景、双栏、代码网址。

### 找到的原因（都有复现）

1. **源语言被锁死**：工具栏只能选英 / 俄 / 西，主界面是「自动检测」时退回英语，Vision 只拿 `en-US` 去认。
   中日韩错误率 95%～100%：一段中文认成「2026 € 38」，日文认成「₫· ₫」。同一张图换对语言是 0。
   拉丁语系不受影响（rev3 的拉丁模型照样认出变音符号），法 / 德 / 意文被当成英语送进 Apple 翻译，结果也是对的。
2. **后处理把认对的字改坏**：
   - 西语规则把行首 I 换成「¿」，触发词按**子串**匹配，英文里的 trusted / adjusted / exhausted 含 "usted"、
     scholar 含 "hola"。一张带「Trusted devices」的英文截图，下面四行全变成「¿nstall…」「¿f a device…」。
   - 俄语规则把拉丁单词换成形近的西里尔字母：「Apple Music на iPhone」→「Аррле Миsис на Ирhопе」。
     这是给不支持俄语的旧 Vision 写的，rev3 直接认俄语是 0 错误。
   - 去重规则会吞行：上一行是下一行的前缀就被并掉，同一排两个相同的词只留一个（那个位置就没有译文）。
3. **折行段落没拼回整段**：拼接条件是「行尾贴近选区右边、行首贴近选区左边」，框宽一点就不拼。
   6 行英文全拆开逐行翻："in half" → 「在半小时内」，"some（鲸鱼）" → 「有些人」；整段翻就是通顺的。
   双栏文章更糟：右栏第一行和左栏第二行被拼成一句。
4. **高选区行序颠倒**：「同一行」按图片高度的 2% 判，框 1440pt 高时相距 22pt 的两行被当成同一行。
5. **预处理的灰度 / 对比度 / 锐化**会在浅灰字和图片背景上多出杂字（「notice. |」「shop now. at」）；
   放大本身对非 Retina 小字有用（9.5pt@1x：不放大 1.1%，放大 0%）。
6. **潜在崩溃**：旧代码在 `VNRecognizeTextRequest` 的 completionHandler 和 `perform` 的 catch 里各 resume 一次。
   实测 `perform` 抛错时 completionHandler **也会被调用**，于是 continuation 被 resume 两次直接崩。

### 改了什么

- **源语言**：「自动检测」+ 本机 Vision 实际支持的语言（运行时查 `supportedRecognitionLanguages()`，
  和应用语言表取交集，本机 25 种），默认跟随主界面设置。自动时开 `automaticallyDetectsLanguage`；
  手动指定时只用那一种、**关掉**自动识别——开着的话纯汉字日文照样被认成繁体（閲→閱，6.2% 错误）。
  换源语言会作废已有识别结果，下次点「翻译」重新识别（「未识别到文字」后换语言也能直接重试）。
  越南语代码修成 Vision 用的 `vi-VT`（写成 `vi-VN` 不报错，只会被悄悄忽略）。
- **删掉**西语、俄语两套修正和去重；预处理只保留缩放。
- **`OCRParagraphGrouper`**：按版面把行拼回段落，和选区大小无关。同一栏（水平有重叠）、字号相近、
  行距像正文、上一行写满、上一行不以句末标点结束、下一行不是列表项才拼；拉丁文开头两行还要求下一行小写开头
  （界面列表几乎都是大写开头），段落成形后不再要求，免得德语名词把段落拆碎。中日文拼接不加空格，韩文加。
  阅读顺序改成按行中心分行、行内从左到右。
- **按段翻译**，每段各自判断源语言（`ScreenshotTranslationSourceResolver`）：已经是目标语言的段、
  纯数字符号的段不翻；识别器在短文本上弃权时，先看和整张图主语言是否同一书写系统（法文页面里的
  「Annuler」按法语），不是再按书写系统兜底（中文页面里的「Sign in」按英语）。
  Apple 引擎逐段翻；在线引擎把相邻、同源语言的段用换行拼成一批（每批 ≤14 段、≤2200 字，沿用原 OpenAI 分块上限），
  走现有的换行标记机制，分不回原段数就逐段重来。每批翻完就先显示；第一段就失败直接报错，
  之后的失败保留原文并提示。
- 选区、源语言变化时 `generation` +1，异步回来的截图 / 识别 / 翻译结果对不上就丢弃。
- 过渡版覆盖层（下个 PR 整体替换）按段显示，字号改用下面的字符宽度估算。
- 设置里的 FAQ 补了「可以手动指定源语言」。

### 实测

- 单测 `Test run with 241 tests in 9 suites passed`，退出码 0（原 204 + 新 37；审核第一轮后 245，第二轮后 251，第三轮后 260，第四轮后 263，第五轮后 267，第六轮后 272，第七轮后 276，第八轮后 280，第九轮后 284）。新增的真实识别回归里，
  13 张基准图在 macOS 26.5 上字符错误率全是 0.0%。
- 离线端到端（新识别链路 + 系统离线翻译）：6 行英文段落拼成一段整体翻译；界面各行保持独立；
  中文截图（目标中文）整段跳过；日文正常识别翻译。
- 本地 Release 构建成功。

### 踩的坑

- **Vision 的行框高度和上下边都不可靠**。上方有 26pt 标题时，15pt 正文里某一行的框顶边往上多伸 9pt、
  有时底边也多出 10pt，框高是别的行的 1.9～2.4 倍（同一张图重复跑结果一致，换画布高度会变）。
  按框高判断「字号相近」直接把段落拆开。改用**宽度 ÷ 字数**估字号（小写 0.5、大写和数字 0.62、空格 0.28、
  汉字假名 1、谚文 0.86 个字号宽，按系统字体实测），同段各行估出 14.9～15.5，标题 27.2。
  行距取顶边、中心、底边三种量法任一落在 0.85～1.95 个字号内即可：框常常只往一头撑。
- **跑 Vision 的单测不能用 `CODE_SIGNING_ALLOWED=NO`**：宿主不带 entitlements、又被 XCTest 注入，
  打不开神经引擎（`ANEServicesDeviceOpen` 返回 `0xe00002c1`，kIOReturnNotPrivileged），Vision **不报错、直接返回空结果**。
  改用 ad-hoc 签名：`CODE_SIGN_IDENTITY=- CODE_SIGN_STYLE=Manual DEVELOPMENT_TEAM=`，Gatekeeper 没有弹框。
- 输入增强的 `frontmostApplicationCheckIsAvailableRightBeforePosting` 会偶发失败：它读全局的前台应用，
  而测试宿主会在运行约 4 秒时自己激活到前台。main 上整轮 0.6 秒跑完碰不到；加了真实识别用例后整轮 5～9 秒，
  本分支 8 次全量里失败 2 次。根子是 `前台bundleID == 期望值` 在两边都是 nil 时判成匹配（输入增强里有 3 处同样写法），
  属于输入增强的安全逻辑，这个 PR 不动。

### 审核第一轮（#11）：逐段判断源语言的三处漏洞，都已复现并修掉

1. 没列出的语言一律当拉丁文：阿拉伯文页面上的「Sign in」被当成阿拉伯文。改成显式的拉丁语系列表；
   阿拉伯文、泰文这类 `TextScriptPresence` 认不出的书写系统，只有「有字母、又不属于任何认得出的书写系统」才算对得上。
2. 猜出来的语言被当成跳过的依据：整张图只有「Bonjour」、目标英语时，按拉丁字母猜成英语，判成「不用翻」。
   现在只有识别器认出来的、整页主语言且书写系统对得上的、或汉字 / 假名 / 谚文这类能确定语言的，才能决定跳过；
   拉丁、西里尔字母只能猜，一律交给引擎自动检测（`auto`）。
3. 书写系统对不上、兜底也认不出时，又把整页主语言塞回来：中文页面上的阿拉伯文标签被当成中文跳过。删掉这层兜底。

单测 `Test run with 245 tests in 9 suites passed`，退出码 0。

### 审核第二轮（#11）：书写系统比对和简繁判断，都已复现并修掉

1. 认不出的书写系统被当成彼此相同：阿拉伯文页面上的希腊文「Ναι」被当成阿拉伯文，目标是阿拉伯文时被跳过。
   `TextScriptPresence` 补上阿拉伯文、希伯来文、希腊文、泰文、天城文、孟加拉文、泰米尔文，以及「有字母不属于任何已知书写系统」；
   判断改成逐个显式比对，认不出的一律算对不上、交给引擎自动检测。只加字段，Mini 和 Apple 翻译的原有调用不受影响。
   注意 CJK 符号区里的「々」「〇」也算字母，要跟着汉字走，否则日文会被当成混了别的书写系统。
2. 简繁变体取自目标语言：单独一个「设置」、目标繁体时，兜底按目标猜成繁体，判成「不用翻」，永远转不成「設置」。
   改成按字形判断：用 ICU 的 `Hans-Hant` / `Hant-Hans` 转一下，转繁体会变说明含简体特有的字，反之亦然；
   简繁同形（「中文」）才按目标语言算不用转。简体页面上的「設置」也照样转换。不再调用 `LanguageScriptFallback`
   （它的简繁取自目标语言，这里正是要避开这一点）。

单测 `Test run with 251 tests in 9 suites passed`，退出码 0。

### 审核第三轮（#11）：跳过的依据和失败的处理，两条 P2 都修掉

1. 整页主语言被当成跳过的依据：英文页面上单独的法文「Bonjour」，按「拉丁字母 + 整页英语」判成英语后跳过。
   判断分成两档：**能确定**（识别器认出来的；汉字按字形、假名、谚文、希腊文、希伯来文、泰文、孟加拉文、
   泰米尔文这类书写系统本身就能确定语言）才能决定跳过；**只是提示**（拉丁、西里尔、阿拉伯、天城文这类
   多语言共用的书写系统和整页主语言对得上）只当翻译的源语言用，等于目标语言时交给引擎自动检测。
   顺带：阿拉伯文页面上的希腊文现在直接判成希腊语（上一轮是交给引擎检测）。
2. 第一段就失败就整体中止：自动检测下各段源语言可能不同，第一段恰好是 Apple 不支持的语言对时，
   后面支持的段也全不翻了。把调度抽成 `ScreenshotBlockTranslator`（翻译调用从外部传入，可以单测），
   失败分两类处理：整体性的（`URLError`、取消、HTTP 401/403/429/5xx、OpenAI 限流）立刻停下；
   和语言有关的只熔断这一门源语言（免得缺失的语言包一段一段反复弹下载框），别的语言照常翻，
   `auto` 的段不熔断。没翻成的段保留原文，全部失败才报错，部分失败用提示条说明原因。

单测 `Test run with 260 tests in 9 suites passed`，退出码 0。

### 审核第四轮（#11）：熔断和书写系统判定都收窄

1. 上一轮的熔断太宽：凡不是整体性失败都熔断该语言，OpenAI 一次空响应、某个请求 400 就让剩下的英文段全不翻。
   现在只有 Apple 明确报告「语言对不可用」（`unsupportedLanguagePair` / `unsupportedSourceLanguage` /
   `unsupportedTargetLanguage` / `unsupportedLanguagePairing`）才熔断；为此把 `AppleTranslationError`
   从 `private` 改成模块内可见，加了 `isLanguagePairUnavailable`。拒绝下载语言包时落到哪个错误拿不准，不算在内。
   其余错误只影响出错的那一段。
2. 混排短标签被单一书写系统定性：「下载 Save」因为有汉字就算确定是中文，目标中文时被跳过，英文没翻。
   书写系统只有在整段只用这一种时才算「能确定」（日文算假名加汉字，韩文允许夹汉字），混排的交给引擎自动检测。

单测 `Test run with 263 tests in 9 suites passed`，退出码 0。

### 审核第五轮（#11）：识别成目标语言的混排段落、OpenAI SDK 的错误

1. 识别器认出是目标语言就跳过，混排保护只在它弃权时才生效：中文段落里夹一句英文操作说明，
   整段判成中文、目标中文时跳过，英文永远没翻。现在判成目标语言准备跳过前，先把不属于目标语言书写系统的
   字母挑出来：够分量（拉丁等按词计 ≥3 个词或 ≥15 个字母；汉字、假名、泰文 ≥4 个字）就翻，而且**按这段外文
   自己的语言翻**（先让识别器认外文部分，认不出按书写系统猜）——交给引擎自动检测的话，引擎会把整段认成中文、
   原样返回，英文照样不翻。零星的品牌名、型号（「打开 Wi-Fi 设置」）不够分量，照旧跳过。
2. OpenAI SDK 的 HTTP 失败没算整体性失败：`OpenAICompatibleEngine` 对 429 以外的状态码会原样抛出 SDK 的错误。
   SDK 有三种形态——流式请求的 `OpenAIError.statusError`（带状态码）、普通请求能解码出错误体时的 `APIErrorResponse`
   （只有 type / code，如 `invalid_api_key`）、Gemini 格式的 `GeminiAPIErrorResponse`（code 就是状态码）——
   都按鉴权 / 权限 / 额度 / 限流 / 服务端错误归为整体性失败，用例用真实的错误类型构造。

单测 `Test run with 267 tests in 9 suites passed`，退出码 0。

### 审核第六轮（#11）：俄语清洗删汉字、Google 的 405、同一种字母的外语句子

1. **按俄语翻的混排段落，汉字会被删光**：`OpenAICompatibleEngine` 源语言是俄语（或自动检测时像俄语 OCR 噪声）时，
   会先跑一遍旧的 `fixRussianOCRNoise`——它假设 OCR 把西里尔字母认成了拉丁字母，拉丁字母一律映射回西里尔、
   汉字整个删掉。这是给「手动粘贴 OCR 文本到输入框」准备的；截图翻译现在可以逐段按俄语翻，
   「Нажмите 设置 чтобы продолжить」送出去就成了「Нажмите  чтобы продолжить」。
   改成请求上带一个开关 `cleansRussianOCRNoise`（`TranslationRequestContext` → `FrozenTranslationExecutor` → 引擎），
   默认开、主窗口行为不变；截图翻译统一走 `ScreenshotTranslationService.makeRequest`，固定关掉。
   用例用 mock 的 URLSession 截下真实的 chat completions 请求体：关掉时里面有「设置」，默认开时没有。
   `makeRequest` 改成显式传引擎、模型这些字段，不收 `AppSettings`——测试宿主就是 app 本身，
   `AppSettings()` 一初始化就会往真实的 UserDefaults 里写。
2. **Google 的 405/501 没算整体性失败**：`GoogleTranslateEngine` 把它包成了 `EngineError.methodNotAllowed`，
   分类只认 `HTTPError`，结果每一批失败后逐段重试一遍，后面的批照样打过去。顺手把其他引擎自己包过的也补上：
   微软的限流 `.rateLimited`、OpenAI 的缺 Key / 缺模型 / 地址不对、Responses 接口不兼容，都是换一段也一样；
   空响应、微软的 400（多半是语言不支持）、解析失败只算这一次。
3. **同一种字母的外语句子查不出来**：第五轮的「夹带外文」只挑书写系统不同的字母，英文段落里夹一句西班牙语，
   字母全是拉丁字母，照样整段跳过。现在书写系统查完，再用 `NLTokenizer` 逐句识别语种：
   够分量的句子认出别的语言，就按它翻。真实识别器实测：一段英文加一句「Abra la aplicación y toque el botón
   de inicio para continuar.」，整段认成英文置信度 1.00，那一句单独认是西班牙语 1.00，现在解析成 `es`；
   全英文的段落照旧跳过。

单测 `Test run with 272 tests in 9 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第七轮（#11）：自动检测的段合批、简繁混用、中文里的短英文

1. **源语言是 `auto` 的段也被合成一批**：它们不一定是同一种语言（中文截图上的一个阿拉伯文标签和一个英文标签
   都可能是 `auto`），合成一段请求时引擎只整体认一次语言，分回原段数也查不出译错。现在 `auto` 的段各自单独一批。
2. **简繁混用的短标签被跳过**：「发佈」只有两个字，识别器弃权；`chineseVariant` 对「简繁同形」和「简繁混用」
   都返回 nil，于是按目标语言算、直接跳过。现在目标是中文、这段也是中文时，不看它「是」简体还是繁体，
   只看「转成目标字形会不会变」：会变就按另一种字形翻（目标简体按繁体翻，反之亦然），不变才跳过。
   识别器把整段认成另一种变体、但其实没有要转的字时，也按字形跳过，不再白发一次请求。
3. **中文里夹的短英文按钮名被跳过**：第五轮只翻「够分量」的外文，「请点击 Save 按钮后继续操作」里的 Save
   不够长，整段跳过。审核说得对：长短分不出「Save」「Sign in」和「Wi-Fi」，第五轮「零星的品牌名、型号照旧跳过」
   这条规则去掉了——书写系统不同的外文哪怕只有一个词也翻，按那个词的语言翻，品牌名交给引擎原样保留
   （多一次请求，译文不变时下一个 PR 的回贴也不会重画）。认不出语言、又夹着目标语言的字的短标签（「下载 Save」）
   同样按外文那部分翻，不再交给引擎自动检测——引擎会整段认成目标语言、原样返回。
   「够分量」只留给逐句识别语种用：句子太短，识别器认不准。

单测 `Test run with 276 tests in 9 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第八轮（#11）：既要简繁转换、又夹着外文

第七轮的简繁判断提前返回了：「請點擊 Save 按鈕」译成简体时，直接按「繁体→简体」发出去，
而 Google、微软这类引擎对这个语言对只做字形转换，Save 原样留着。反过来只按英文翻，引擎又不动夹着的中文。
一次请求两头顾不全，所以拆成两步：

1. 解析器先查外文、再查简繁：有外文就按外文的语言翻，没有外文才按另一种字形翻（转换）。
2. 调度器（`ScreenshotBlockTranslator`）第一遍译完，看译文里是不是还留着另一种字形的字
   （`variantConversionSource`，同样用 ICU 判断），是的话按那种字形补翻一次。补翻失败保留第一遍的译文，
   不重复计入成功段数；第一遍本来就是按那种字形转换的不再补，免得 ICU 偶尔误报（「瞭望」）时来回转。

没有在本地直接用 ICU 转换译文：ICU 是按字转的，简转繁有一对多（「干擾」会被转成「乾擾」），
交给引擎按上下文转，ICU 只用来判断「要不要转」。实测 ICU 在繁体文本上误报「要转」的很少
（「周末」「干擾」），代价只是多一次请求。

用例 `blockNeedingBothTranslationAndConversionGetsBoth` 在同一段上把解析器和调度器串起来：
假引擎只按英文翻时不动中文，最终拿到「请点击 保存 按钮」，请求依次是 `en:請點擊 Save 按鈕`、`zh-TW:請點擊 保存 按鈕`。

单测 `Test run with 280 tests in 9 suites passed`，退出码 0，没有 `Restarting after`。

### 审核第九轮（#11）：超长段落、补翻失败没提示

1. **一段超过引擎的单次上限就整段失败**：折行拼回整段后，密集截图里的长段落可能超过 Google 的 8000 字
   （`textTooLong`），而合批的上限只管「不把段合进去」，不会拆开一段。现在单次请求统一以 2200 字为上限
   （`defaultMaxCharactersPerRequest`，原先 OpenAI 分块的大小，也在 Google 8000、微软 30000 以内，
   用例里连同合批的换行标记一起断言了）：合批不超过它，单独一段超过它就按句子拆开翻，一句本身太长再按空白、
   最后按字数硬拆；译完用段落拼接的规则（`OCRParagraphGrouper.joinLines`：中日文不加空格）拼回原来的段。
   有一块没翻成就整段保留原文，免得半段原文半段译文；拆开的段要等所有块都有结果才出现在进度里。
2. **补翻（简繁转换）失败没提示**：第一遍都成功时 `untranslated` 是 0，补翻超时也不提示，
   用户看到的是没转换完的繁体。`Outcome` 加了 `unconverted`，提示改由 `incompleteWarning` 统一生成：
   「有 N 段没转换成简体中文，已保留未转换的译文（原因）」，和没翻成功的段一起出现时合成一句。

单测 `Test run with 284 tests in 9 suites passed`，退出码 0，没有 `Restarting after`。

### 故意没做

- **macOS 26 的 `RecognizeDocumentsRequest`**：识别准确率和现在一样；单栏分段很好，但把双栏文章误判成表格，
  左栏逐行拆开、右栏还丢了字母（"be better" → "e better"）；而且只有 macOS 26 有，应用最低支持 15.1，
  CI 用的 Xcode 16.4（macOS 15.5 SDK）连编译都过不了。
- **大图分块识别**：5K 整屏框选（5120×2880）时 Vision 内部降采样，9.5pt 小字错字多、还会整行漏掉
  （整图逐字全对 6/12；切成 1600px 的块 12/12，耗时 0.5s→1.0s）。跨块的长行要拼接去重，留到后面单独做。
- 覆盖层渲染（字号、颜色、抹字重绘）在下一个 PR。CHANGELOG 等发版时再写。

---

## 2026-09-24 · 发布 v1.3.1

版本号 1.3.1 / 构建 131（`project.pbxproj` 三处 × 两个字段，加 `AppVersion.swift` 的兜底值）。
没有新功能，只有 #8、#9 两个修复和 #10 的签名改动，所以升补丁号。

这是第一个用固定自签名证书签的版本。CHANGELOG 把「升级到这一版要最后授权一次」放在第二条，
并按 macOS 26 系统设置的实际名称写（「隐私与安全性」「辅助功能」「录屏与系统录音」，
名称取自 `SecurityPrivacyExtension` 的 `Localizable.loctable`）。

### 发版前实测：ad-hoc 的 1.3.0 能否通过 Sparkle 升到自签名的 1.3.1

上一次改签名方式（1.2.3）就把更新搞坏过，见 1.2.4 的 CHANGELOG。这次同样是改签名，
所以不只靠读源码，而是在本机真实跑了一遍 Sparkle 更新：

- **旧版**：/Applications 里 1.3.0 正式包的拷贝。只改了 bundle ID（`…sparkleprobe`）、
  `SUFeedURL`（指向 `127.0.0.1:8765`）和 `SUPublicEDKey`（换成测试 Ed25519 公钥），
  重新 ad-hoc 签名，和真实 1.3.0 用户一样。
- **新版**：按 CI 参数编的 1.3.1 Release 包，同样改这三项。用一次性自签名证书，
  走和 CI 相同的临时钥匙串流程，按 SHA-1 `--deep` 签名；`ditto` 打 zip，
  再用 CI 同款 `generate_appcast --ed-key-file` 生成 appcast。
- 起 `python3 -m http.server`，启动旧版，用 AX 点「检查更新…」→「安装更新」。

结果：appcast、zip 各请求一次，旧进程退出，同一路径拉起新进程。磁盘上的版本变成 1.3.1 (131)，
签名 `flags=0x0(none)`、`Authority=` 为测试证书，全程没有校验报错。
测试证书、测试 EdDSA 密钥、探针应用、LaunchServices 登记和 defaults 事后都清掉了。

### 其他验证

- 单测：`Test run with 204 tests in 4 suites passed`，退出码 0。
- 本地 Release 构建（arm64，`CODE_SIGNING_ALLOWED=NO`，与 CI 同参数）：成功，Info.plist 为 1.3.1 / 131。

---

## 2026-09-24 · 改用自签名证书签发布包，更新后不再掉权限（feature/self-signed-signing）

### 为什么

每次更新都要重新授权辅助功能 / 屏幕录制，这是用户最大的痛点。原因在签名：
TCC 记住授权时，存的是应用的 designated requirement（DR），之后拿它比对。ad-hoc 签名的 DR 是
`cdhash H"…"`，也就是「必须是这一份二进制」，换版本必然对不上。

实测：1.3.0 正式包的 DR 是 `cdhash H"d864…"`，1.2.6 的构建满足不了它
（`codesign -v -R=<DR>` 失败）。

不考虑 Apple 开发者账号（Developer ID），用户明确不买。

### 验证：自签名证书能让授权跨版本保留

1. **DR 层面**：同一张自签名证书分别签 1.2.6 和 1.3.0 的 Release 包，两份 DR 完全相同：
   `identifier "achord.dazuofanyiguan" and certificate leaf = H"<证书 SHA-1>"`。
   1.3.0 满足 1.2.6 的 DR（exit 0），ad-hoc 对照组不满足（exit 3）。
2. **TCC 端到端**（用户手动授权一次）：独立 bundle ID 的探针 app，编了三个 cdhash 各不相同的版本，
   启动后轮询 `AXIsProcessTrusted()`：

   | 步骤 | 版本 | 签名 | 结果 |
   |---|---|---|---|
   | 用户授权 | v1 | 自签名 | ✅ |
   | 模拟升级 | v2（不同二进制） | 同一证书 | ✅ **授权保留** |
   | 对照 | v3 | ad-hoc | ❌ |
   | 换回 | v2 | 同一证书 | ✅（对照没破坏授权记录） |

   探针的授权条目事后用 `tccutil reset Accessibility achord.tcc-probe` 清掉了。
3. **Sparkle 2.9.4 不受影响**（读 `SUUpdateValidator.m` / `SUCodeSigningVerifier.m`）：
   - 应用包更新时，EdDSA 通过即放行，文档原话是它允许更换 Code Signing 身份。代码签名只要求结构合法，
     `SecStaticCodeCheckValidityWithErrors` 不做信任评估，自签名能过。
   - 旧版 ad-hoc 与新版自签名的 DR 不匹配，走的是和现在每次 ad-hoc 更新完全相同的放行路径。
   - XPC 连接校验只在有 Team ID 时才设要求；自签名和 ad-hoc 一样没有 Team ID。

**代价**：从 ad-hoc 版本升到第一个自签名版本时，用户还要最后授权一次。
Gatekeeper 首次安装的拦截和现在一样，不会变好也不会变坏。

### 证书生成脚本 `scripts/create_signing_certificate.sh`

用户在本机跑一次，私钥和密码不经过协作者、不打印到终端：

- RSA 3072、有效期 7300 天，EKU 只给 codeSigning。
- 导出 p12，用 OpenSSL 3 时加 `-legacy`，否则 `security import` 读不了。
- 用和 CI 相同的方式导入临时钥匙串试签名，校验 DR 是否等于预期，不等就中止，什么都不上传。
- 写桌面离线备份（700 权限，附恢复说明），然后 `gh secret set` 上传
  `MACOS_SIGNING_CERT_P12_BASE64` / `MACOS_SIGNING_CERT_PASSWORD`。
- 防误换证书：仓库里已有这两个 Secret 时拒绝覆盖。读不到 Secret 列表时也中止，
  不把「读不出来」当成「没有」。

踩过 / 预防的坑：

- **codesign 只在钥匙串搜索列表里找身份**，`--keychain` 指定文件也找不到（`no identity found`）。
  所以要临时把钥匙串加进搜索列表，退出时（包括失败退出）原样恢复。
- **密码文件不能带换行**：`openssl rand` 输出带 `\n`，OpenSSL 读 `file:` 会去掉它，
  但同一个文件上传成 Secret 后，CI 拿它解 p12 就是错密码。
- 吞掉 stderr 的 `2>/dev/null` 会把真实错误一起吞掉，改成失败时打印捕获的输出。

怎么验证的：

- `zsh -n` 语法检查通过。
- `--dry-run` 全流程通过，搜索列表前后 diff 为空，桌面没留下文件。
- 用假 `codesign` 注入失败：打印真实错误、退出码 1，搜索列表照样恢复。
- 只读查了仓库 Secret 名称：目前只有 `SPARKLE_PRIVATE_KEY`，脚本会继续生成。

用户随后在本机正式跑了一次：证书 SHA-1 `65333a6e8f985b5fa58e827d430c2cc811fe6a73`，
2046-09-19 到期，两个 Secret 已上传，备份在用户桌面，由用户自己转存。

### CI 改签名（`build-macos.yml`）

- **签名步骤**：解 p12 → 临时钥匙串 → 加进搜索列表 → `codesign --deep --sign <SHA-1>`。
  按 SHA-1 而不是名称指定身份，钥匙串里就算混进同名证书也只用这一张。
  `trap` 保证签完（包括失败）立刻删钥匙串、删 p12、恢复搜索列表。
- **缺 Secret 直接失败，绝不退回 ad-hoc**：退回去就等于悄悄把所有用户的授权作废。
- **校验步骤**：原来断言「必须是 ad-hoc」，改为断言「不能是 ad-hoc」、`Authority=` 是这张证书，
  并且 DR **逐字等于**写死在 workflow 里的 `identifier "achord.dazuofanyiguan" and certificate leaf = H"65333a6e…"`。
  证书一变，构建直接失败。hardened runtime 仍然关着：自签名同样没有 Team ID，库验证会拒绝加载 Sparkle。
- **新增 `pull_request` 触发**：以前 PR 上根本不跑 CI。现在 PR 也构建、签名、校验，
  但发布相关步骤（版本校验、appcast、Release）仍只在 `workflow_dispatch` 跑。
  从 fork 发起的 PR 拿不到 Secret，会停在签名步骤并说明原因。
- Release notes 里的签名说明、README 两处、权限引导 FAQ 文案同步改掉。
  FAQ 原文说「1.2.1 及更早用临时签名……后续更新会保持同一权限身份」，这和实际情况不符，
  现在改成「1.3.0 及更早每次更新签名都会变；之后改用固定证书，升级上来的这一次要重新授权」。

怎么验证的：用 PyYAML 从 workflow 里原样抽出签名、校验两步，只做两处本地化改动：
只编了 arm64，循环只跑 arm64；校验步骤截掉最后的「启动发布包」，因为本机正式版在跑，同 bundle ID 不能再起一个。
用 `/bin/bash` 3.2 跑；证书用同名、但指纹不同的一次性证书。Release 包按 CI 同样的参数编（`CODE_SIGNING_ALLOWED=NO`）。

| 场景 | 结果 |
|---|---|
| ① 正常签名 + 校验 | ✅ exit 0，DR = `identifier … and certificate leaf = H"<一次性证书>"` |
| ② 缺 Secret | ❌ exit 1，提示「不能退回 ad-hoc」 |
| ③ 写死的指纹不在 p12 里 | ❌ exit 1，`no identity found`，签不出错证书的包 |
| ④ 包的证书与写死的指纹不符 | ❌ exit 1，打印预期 / 实际 |
| ⑤ 混进 ad-hoc 包 | ❌ exit 1，「仍是 ad-hoc 签名」 |

每个场景之后，钥匙串搜索列表都与运行前一致；`$RUNNER_TEMP` 里没留下 p12 或钥匙串。
单测（`-skip-testing:大佐翻译官v1UITests`）：`Test run with 204 tests in 4 suites passed`，退出码 0。
真证书的端到端（含发布包启动冒烟）由这个 PR 自己的 CI 跑。

### 发版时要记得

- CHANGELOG 写清楚：**升到这个版本时要最后重新授权一次**（删旧条目 → 重新添加 → 开启），之后更新不用再授权。
- **不要换证书。** 私钥丢了或泄露了才换，换了所有用户都要再授权一次。

---

## 2026-09-24 · 权限引导的拖拽不再拷贝整个 .app（fix/permission-guide-drag-in-place，#9）

### 为什么

权限引导里「把图标拖进系统设置权限列表」用的是 SwiftUI `.onDrag { NSItemProvider(object: Bundle.main.bundleURL as NSURL) }`。
实测每拖一次，`~/Library/Caches/com.apple.SwiftUI.Drag-<UUID>/大佐翻译官v1.app` 就多一整份（31MB），从不清理：

- 本机 9/20 留下两份，ctime 分别是 13:28:50 和 13:28:58（birth / mtime 是原件的 13:28:18，拷贝时原样带过来了）。
- 两份都登记在 `achord.dazuofanyiguan` 下：`urlsForApplications(withBundleIdentifier:)` 返回 5 条，
  其中 2 条是这两份拷贝（另外 2 条是 DerivedData 的 Debug / Release）。`lsregister -dump` 里还有 14 条
  已不存在的 `~/Library/Caches/com.apple.SwiftUI.Drag-*` 历史登记、4 条沙盒时期 `Containers/…/Caches` 下的。
- 系统设置「退出并重新打开」按 bundle ID 找应用，同 ID 的拷贝越多越不可控；拖进列表的也可能是拷贝而不是 /Applications 里那份。

### 先查清：拷贝发生在哪一步

做了个探针：独立 bundle ID（`achord.dragprobe.source.*`，不碰本应用的登记）的 .app，直接编译仓库里的
`PermissionGuideView.swift`，塞 30MB 填充让拷贝耗时和真 .app 同量级；另起一个进程当接收方（NSView 登记
`.fileURL`，放下时按 `NSURL` 读）；CGEvent 驱动真实拖拽；每 5ms 轮询 `~/Library/Caches`、`$TMPDIR`、用户 Caches 目录。

| 场景 | 旧（SwiftUI `.onDrag`） | 新（AppKit 拖拽源） |
|---|---|---|
| 只在本应用里拖，原地松手取消 | 不拷 | 不拷 |
| 拖到 AppKit 接收方上悬停，再拖回来取消 | **拷**（对方 `draggingEntered` 前 50–120ms） | 不拷 |
| 放到 AppKit 接收方 | **拷**，对方读到的是 Caches 里拷贝的路径 | 不拷，读到原路径 |
| 本应用内拖完取消后，另一个进程去读拖拽剪贴板的 `public.file-url` | **拷**，读到拷贝的路径 | 不拷，原路径 |
| 悬停在真实系统设置「辅助功能」列表上（不松手） | 不拷；列表亮起蓝色可放下框 | 不拷；同样亮蓝框 |
| 悬停在「屏幕录制」列表上 | — | 亮蓝框 |
| 接收方在前台，从后面的引导窗口直接拖 | 能拖，接收方保持前台 | 同 |

结论：**拷贝既不在拖动开始，也不绑定「松手」这个动作，而是别的进程第一次读 `public.file-url` 数据时，
SwiftUI 的懒提供者把 .app 整个拷进 Caches 再交出拷贝的 URL。** `NSItemProvider(object: NSURL)` 本身只注册了
`public.file-url` / `public.url`，没有文件表示——拷贝是 SwiftUI 拖拽桥接自己加的。放下后拖拽剪贴板上
`public.file-url` 是拷贝、`public.url` 才是原路径。AppKit 接收方在进入时就读了数据，所以悬停也拷；
系统设置悬停时不读、松手才读，所以对它来说就是**放下那一刻**——9/20 那两份的 ctime 就是两次松手。

系统设置收到的路径：剪贴板上唯一的文件 URL 就是拷贝，按文件收应用只能收到它。旁证是探针的拷贝只被读了路径，
没进 LaunchServices，而本机那两份进了——是系统设置拿到路径去解析应用时登记的。**直接证据没拿到**：
TCC.db 没有完全磁盘访问权限读不出来；9/20 那段统一日志只剩 57 行，没有相关记录。
拷贝上 `kMDItemUseCount` 为空，没被启动过。

### 改了什么

- 去掉 `.onDrag` 和 `PermissionGuideDragItemProvider`。图标上盖一层 `PermissionGuideApplicationDragSource`
  （`NSViewRepresentable`，NSView 实现 `NSDraggingSource`），`mouseDragged` 里 `beginDraggingSession`，
  `NSDraggingItem` 的 writer 直接是 `applicationURL as NSURL`：只写 `public.file-url` 及其旧式别名
  （`NSFilenamesPboardType` 等），全是路径引用、立即写入，没有懒提供者。
- 操作只给 `copy`：旧版 SwiftUI 实测也只给 `copy`，系统设置照收。不给 move / delete，拖进废纸篓或 Finder
  时不会把正式安装的 .app 挪走；本应用内给空。`ignoreModifierKeys` 返回 true，否则按住 ⌘ / ⌃ 时
  操作被收窄成 generic / link，和 copy 一交就空，拖不进去。
- `acceptsFirstMouse` + `shouldDelayWindowOrdering` + `preventWindowOrdering`：系统设置在前台时可以
  直接从后面的引导窗口拖，引导窗口不会跳到前面挡住列表。
- 拖拽图像只剩应用图标（42pt，居中在按下的位置）；旧版是 SwiftUI 把整个 VStack 截下来（带手势角标和「拖动」）。
- `.help` 保留给 VoiceOver，NSView 另设 `toolTip`（它盖在上面，会挡住 SwiftUI 的悬停）。
  实测 AX help 仍是「将大佐翻译官拖到系统设置的权限列表」，悬停只出一个提示。

### 测试

- 改写 `permissionGuideProvidesDraggableApplicationFileURL`：把拖拽项写进私有剪贴板再读回来，
  按 `NSURL`（fileURLsOnly）和 `NSFilenamesPboardType` 两种读法都必须是原路径；不能有文件承诺类型；操作掩码。
  读回来这一步正好会触发懒提供者——将来谁在这里挂了个「先拷一份」，读回来的路径就对不上。
- 新增 `permissionGuideDragsTheRunningApplicationInPlace`：把 `PermissionGuideView` 放进 `NSHostingView`，
  两项都未授权时必须找到 2 个 `DragSourceView`、URL 是 `Bundle.main.bundleURL`；都授权了一个都没有。
  变异检验：把视图改回 `.onDrag`（保留类型让它能编译），这条挂在 `sources.count → 0`。
- 全量 `xcodebuild test`（禁签名，独立 `-derivedDataPath`）：`Test run with 203 tests in 4 suites passed`
  （HEAD 声明 202 个 `@Test`，和上一条日志记的 202 一致，+1 新增），无 `Restarting after`、无 `Failing tests`；
  `** TEST FAILED **` 只来自 UI runner 未签名。
- 合入 #8 之后（带 `-skip-testing:大佐翻译官v1UITests`，不再拉起 UI runner）：
  `Test run with 204 tests in 4 suites passed`、`** TEST SUCCEEDED **`，退出码 0，没有 Gatekeeper 弹窗。

### 踩过的坑

- **模拟接收方和真系统设置读数据的时机不一样。** 只看 AppKit 接收方会得出「悬停就拷」，
  拿真实系统设置对照才发现它悬停时不读。结论要以真实接收方为准。
- 驱动脚本替别的进程 `activate()` 会被 macOS 26 的协作式激活拒掉，得像用户一样真点一下窗口。
- 第一次对系统设置的悬停没生效：它的窗口被别的窗口挡着，拖拽其实落在别的应用上。之后每次拖之前
  先查「目标点最上层的窗口是不是系统设置」，不是就中止。
- 实验时旧版探针产生了 5 份拷贝（`achord.dragprobe.source.old`），已按实验前的清单逐个核对后删除并注销登记；
  实验前就在的两份没动。

### 故意没做的事

- **没在系统设置里真的松手添加。** 那是在改安全设置，还要管理员密码；只做了悬停验证（两个列表都亮可放下框）。
  松手添加留给发版出来的安装包验证：拖进去之后 `ls ~/Library/Caches | grep SwiftUI.Drag` 不该变多。
  别拿 DerivedData 的 Debug 包去试——签名和安装版不同，而 TCC 按 bundle ID 记，可能把 /Applications 那份的授权顶掉。
- **没清理本机已有的两份拷贝，也没在应用里做自动清扫。** 前者是用户数据，要用户点头；后者的问题是非沙盒应用
  共用 `~/Library/Caches`，`com.apple.SwiftUI.Drag-*` 这个前缀别的 SwiftUI 应用也会用，按前缀删会误伤，
  要删也得按里面的 bundle ID 筛，而且老用户的拷贝还散在沙盒时期的 `Containers` 里。先止血，要不要清扫另议。
- **没用「`NSItemProvider` 只注册 file-url 的 data representation」这条路。** 它仍然走 SwiftUI 的拖拽桥接，
  拷不拷取决于 SwiftUI 的私有实现——这次就栽在这里；AppKit 拖拽源是公开、确定的行为，测试也能直接断言剪贴板内容。
- 没给 `.link` / `.generic`：旧版只给 `copy` 系统设置照收，没必要扩大。
- 从 DMG 或被 App Translocation 的位置运行时，拖进去的仍是「当前运行的这一份」，和改前语义一致（改前更糟，是它的拷贝）。
- 探针没进仓库：它要接管鼠标、要辅助功能权限，当不了回归；回归靠上面两条单测。

---

## 2026-09-24 · 系统设置「退出并重新打开」不生效（fix/permission-guide-quit-reopen，#8）

### 为什么

授权屏幕录制后，系统设置会弹「将无法录制屏幕内容，直至退出」，点「退出并重新打开」没反应，
只能自己退出再开。怀疑和 ad-hoc 签名有关。用户的要求是：先查原生为什么不生效，
确定做不到再自己加一个「立即重启」。

### 系统设置到底做了什么

反汇编 `/System/Library/ExtensionKit/Extensions/SecurityPrivacyExtension.appex`
（macOS 26，`otool -tV` + 自己解析 `__objc_stubs` 还原 selector），
「退出并重新打开」的实现是：

1. `NSRunningApplication.runningApplications(withBundleIdentifier:)` 找正在跑的实例；
2. 给每个实例发 `aevt/quit`，退出原因 `why?` = `rlgo`（really log out），**`noReply`**——不等应用真的退出；
3. **固定等 3 秒**，然后 `NSWorkspace.openURL(LSApplicationRecord(bundleIdentifier:).URL)`。

所以只要 3 秒内没退干净，那次 `openURL` 就只是把原进程激活一下，看起来就是按钮没反应。
另外：`Localizable.loctable` 里只有屏幕录制、输入监控、麦克风等的 `*_QUIT_*` 文案，
**没有辅助功能的**——辅助功能授权本来就不弹这个框，应用里靠 `AXIsProcessTrusted()` 实时刷新。

### 复现：拦住退出的是 sheet，不是签名

照上面三步写了个命令行逐条复刻，对 1.3.0 正式包的拷贝（只改 bundle ID、重新 ad-hoc 签名）跑：

| 条件 | 结果 |
|---|---|
| 没开任何 sheet | 收到 quit 后 **25ms** 退出，3 秒后拉起新进程 ✅ |
| 开着「完善权限」引导（sheet） | 日志 `App termination blocked by modal sheet`；3 秒后 `openURL` 返回**同一个 pid** ❌ |
| 首次启动的「选区翻译」推荐（sheet） | 同上 ❌ |

同一个 ad-hoc 二进制，差别只在有没有 sheet——**和签名无关**。而用户去系统设置授权，
恰恰是开着权限引导 sheet 去的（引导里的「打开屏幕录制设置」+ 拖图标）。
AppKit 默认窗口挂着 sheet 时拦下一切退出请求，连 `applicationShouldTerminate` 都不问。

### 改了什么

- `WindowAccessor.swift` 加 `allowsAppTerminationWhilePresented()`：
  拿到 sheet 所在窗口，设 `preventsApplicationTerminationWhenModal = false`。
  这是 AppKit 专门给这个场景留的开关，文档原意就是「有些模态窗口不该拦退出」。
- 用在权限引导（`PermissionGuideView`）和选区翻译推荐 sheet 上。两者都是纯展示，没有未保存内容。
- 单测 1 个：把真实的 `PermissionGuideView` 放进窗口，断言默认是 `true`、挂上视图后变 `false`。

### 故意没做

- **没加应用内「立即重启」。** 原生路径修好就能用，不满足「确定做不到」这个前提。
  而且自己做重启同样要先退出，一样会被 sheet 拦——根因不修，重启按钮也会失灵。
- **没全局放开所有 sheet。** `fileExporter` 的保存面板这类 sheet 拦住退出是合理的默认行为，只放开确认无害的两个。
- **否掉的方案：** 自己接管 `aevt/quit` 的 Apple Event handler——会连带接管注销/关机时
  loginwindow 等回复的语义，风险远大于收益；把引导改成独立窗口——改动大，而且推荐 sheet 有同样的问题。

### 顺带发现（本次未处理）

- 引导里「拖动图标」用的是 SwiftUI `.onDrag` + `NSItemProvider(object: NSURL)`，
  每拖一次都会在 `~/Library/Caches/com.apple.SwiftUI.Drag-<UUID>/` 留下一整份 `.app` 拷贝（31MB），
  而且被 LaunchServices 登记在同一个 bundle ID 下。系统设置重开时选的是
  `LSApplicationRecord(bundleIdentifier:)` 认定的那一份，本机目前仍是 `/Applications`，但同 ID 的拷贝越多越不可控。
  已在 #9 单独修掉。
- ad-hoc 签名下 TCC 授权绑的是 cdhash。本机 9/20 13:28:18 升级到 1.3.0，13:28:50、13:28:58 就有两次拖图标授权，
  这是「每次升级都要重新授权」的旁证。引导里「1.2.1 及更早版本使用临时签名……后续更新会保持同一权限身份」
  和现状（CI 仍是 ad-hoc）对不上。签名方案是用户的决定，这里只记下。

### 怎么验证的

- 全量 `xcodebuild test`（禁签名）：`Test run with 203 tests in 4 suites passed`（202 + 新增 1），
  没有 `Restarting after`、没有 `Failing tests`；`** TEST FAILED **` 只来自 UI runner 未签名无法 bootstrap。
- **坑：** 共享 DerivedData 里的 `大佐翻译官v1UITests-Runner.app` 带 `com.apple.macl`，当前进程写不进去，
  报 `ld: can't write output file`，**单测一条都没跑**（输出里没有 `Test run with` 行）。
  换 `-derivedDataPath` 到临时目录后正常。
- 变异检查：去掉引导上的修饰符，新测试失败（`preventsApplicationTerminationWhenModal → true`），恢复后通过。
- 用修复后的 Debug 构建做拷贝，AX 点开「查看权限」确认挂上 1 个 sheet，再跑复刻脚本：
  日志 `App termination approved`，38ms 退出，3 秒后拉起新进程；首次启动推荐 sheet 开着时同样 87ms 退出并重开。
- 复刻用的拷贝、LaunchServices 登记和 defaults 事后都清掉了；正在跑的正式版全程没动。
- 与 #9（拖拽不再拷贝 .app）合在一起试过：代码自动合并，只有本文件顶部冲突；
  合并结果全量 `Test run with 204 tests in 4 suites passed`。

### 本地跑测试以后要加 `-skip-testing:大佐翻译官v1UITests`

以前「UI runner 未签名必挂」被当成无害的噪音，其实它每跑一次，都会在用户屏幕上弹一个
Gatekeeper「已损坏，无法打开」对话框（syspolicyd 日志里是 `Prompt shown`，
对象是 `achord.dazuofanyiguanUITests.xctrunner`），用户专门来问过。

跳过 UI 目标后：`Test run with 204 tests … passed`，`** TEST EXECUTE SUCCEEDED **`，
退出码 0，也不再弹框。以前说 `-only-testing` 用不了，其实是 target 名字写错了，
真名是 `大佐翻译官v1Tests` / `大佐翻译官v1UITests`。

---

## 2026-09-20 · 发布 v1.3.0

版本号 1.3.0 / 构建 130（`project.pbxproj` 三处 × 两个字段，加 `AppVersion.swift` 的兜底值）。
次版本号进位而不是 1.2.7：新增了一个用户可见的翻译渠道，不是修 bug。

**这个仓库的 tag 是 workflow 建的，不要自己推。** `build-macos.yml` 的
`workflow_dispatch` 里 `Validate release version` 有一步
`git ls-remote --exit-code --tags origin refs/tags/$TAG`，tag 已存在就直接失败；
真正建 tag 的是最后 `gh release create "v$VERSION" --target "$GITHUB_SHA"`。
所以流程是：改版本号 + 写 CHANGELOG → 推 main → `workflow_dispatch` 传版本号。
（push 到 main 也会触发同一个 workflow，但只构建、不生成 appcast、不发 Release。）

它还会校验：`MARKETING_VERSION` 必须等于传入的版本号；构建号必须是正整数且
**大于线上 appcast 里的构建号**；`CHANGELOG.md` 里必须有 `## 1.3.0` 这一节且非空
（release notes 直接从那一节抠出来）。所以 CHANGELOG 不是可选的，漏了会在
生成 appcast 那步才炸，白等一轮构建。

发版前本地补跑了一次 Release 配置的构建（平时只跑 Debug）：`BUILD SUCCEEDED`。

---

## 2026-09-20 · 新增翻译渠道：微软翻译（feat/microsoft-translate）

### 为什么

Apple 本地翻译要下模型、Google 走的是非官方 gtx 接口，多一个免密在线渠道给用户兜底。
用户点名要微软翻译。

### 调研：免密的路只剩一条

| 路径 | 2026-09-20 实测 | 结论 |
|---|---|---|
| Azure Translator 官方 v3 | 正常，但要 Key + Region | 本轮不做，见下 |
| `edge.microsoft.com/translate/auth` 换 JWT → `api.cognitive.microsofttranslator.com` | **auth 返回 404、空 body**；换 Edge UA / 加 Referer 都一样 | 8 月起就死了（bing-translate-api #47、AnyDoor #111 都有记录），别再走 |
| `edge.microsoft.com/translate/translatetext` | 正常；无 token，body 是字符串数组，响应与 Azure v3 同构 | **采用**（bing-translate-api PR #49 同一选择） |
| bing.com 网页 `ttranslatev3` | 正常，但要先抓 `params_AbusePreventionHelper` 里的 token/key 和 `IG`/`IID` | 能用但脆，不采用 |

`translatetext` 接口的几条实测行为，代码里的常量都是从这里来的：

- 不带浏览器 UA → 400 `Client Browser Version not supported`；带 Edge 桌面 UA 即可，不校验 Origin / Referer。
- `from=auto` → 400。自动检测要**省略** `from`。`from=zh-CN` 可以。
- `to=zh-CN` / `zh-TW` / `no` 服务端自行归一成 `zh-Hans` / `zh-Hant` / `nb`；不认识的 `to=xx` → 400 空 body。
- 检测回来的源语言是微软码（`zh-Hans` / `zh-Hant` / `nb`），必须映射回应用的 `zh-CN` / `zh-TW` / `no`，否则页脚语种显示成代码。
- 字符串里的 `\n` 原样保留（已用三段中文验证），不需要 `[[DAZUO_NL]]` 那套。
- 空数组 → 400；空字符串元素正常返回空译文。
- 单请求 5000 / 10000 / 20000 字都 200（1.3s 左右）；50000 字挂 20s 后 500；60000 字直接 400 `Request exceeds the maximum allowed translation size`。
- 120 个元素一次请求正常。GET → 405。

### 改了什么

- 新增 `Translation/MicrosoftTranslateEngine.swift`：单块 ≤ 5000 字，按段落 / 空白切；
  多块攒进一个请求（单请求 ≤ 15000 字），少走几趟；总量 > 30000 字本地直接拒。
  原文只在 JSON body，URL 上只有语言参数（和 Google 引擎同一条隐私原则）。
- `TranslationEngineType` 加 `microsoft`（图标 `m.circle.fill`），六处 `switch` 补齐：
  执行器、剪贴板 toast、设置页说明与摘要、截图翻译日志、选区翻译缓存指纹。
  缓存指纹和 Google 一样只用 rawValue——引擎无配置，同一原文同一结果。
- 400 → 「拒绝了本次请求」并带上服务端文字；429 → 「请求过于频繁」；其它沿用 `HTTPClient` 的通用文案。
  没有接 `FrozenTranslationExecutor` 的限流重试——那条只认 OpenAI 的 `RateLimitError`，
  微软这边还没观察到 429，先不为没见过的情况写重试。
- 单测 8 个：URL 不含原文、auto 省略 from、UA 非空、语言码双向映射、`LanguagePreset.common`
  每个码都能映射、真实响应解析、畸形响应拒绝、超长本地拒绝、切块 / 攒批保序、HTTP 状态映射。
- 文档：两个 README、`PRIVACY.md` 加微软一行。`CHANGELOG.md` 按约定先不动，发版时补。

### PR #7 第一轮：一条 P1，成立

achord-review 指出我把检测回来的 `yue`（粤语）/ `lzh`（文言）折叠成 `zh-TW`。
问题不在显示，在 `TranslatorViewModel.reverseTranslate`：源语言是 auto 时它拿检测码当
反向翻译的**目标语言**，折叠等于把用户的原语种偷换掉。顺着看，我写的 `pt-pt→pt`、
`sr-Latn→sr`、`mn-Mong→mn`、`fr-ca→fr` 全是同一类错——我当时想的是"界面只有 zh-CN/zh-TW
这几个名字"，但这个值不只用来显示。

改法：`appLanguageCode(fromMicrosoft:)` 只留**同一种语言换写法**的三条
（`zh-Hans→zh-CN`、`zh-Hant→zh-TW`、`nb→no`），其余透传；`requestLanguageCode(forTarget:)`
里对应的 `sr→sr-Cyrl`、`mn→mn-Cyrl`、`nn→nb` 也删掉，透传码再送回服务端就是原码。
单测加了一组：七个不同语种的码必须透传，且透传后再映射回请求码仍是自身。

### 故意没做

- **Azure 官方 Key 配置**。免密接口 8 月刚断过一次，接官方 Key 做兜底是有价值的，
  但要加设置项、钥匙串、区域字段，本轮先把免密路打通；再断的话再加。
- 没把 Google 和微软的 `chunkText` 抽成公共函数。两份一样的代码、都只有二十行，
  等第三个渠道需要时再抽（PR #5 的教训是"同一套判断有几份就漂几次"，这里先记下）。
- 没做 `textType=html`、音译、多目标语言，应用用不到。

### 怎么验证的

- `xcodebuild … build-for-testing`：`TEST BUILD SUCCEEDED`。
- 全量 `xcodebuild test`（禁签名）：`Test run with 202 tests in 4 suites passed`（含新增 8 个），
  没有 `Restarting after`、没有 `Failing tests`；`** TEST FAILED **` 只来自 UI runner
  未签名无法 bootstrap，main 上一样。
- 把 `MicrosoftTranslateEngine.swift` + `HTTPClient.swift` + `TranslationEngine.swift` 用 swiftc
  单独编成命令行跑真网络（单测里不联网）：中→英三段文本换行原样、检测回 `zh-CN`；
  `繁體中文測試` 检测回 `zh-TW`；`en→no` 得 `Hallo`；18000 字英文切 4 块攒 2 个请求、
  6785 字译文顺序正确；`to=xx` 得到「微软翻译拒绝了本次请求，请检查所选语言是否受支持」。
- Debug 包起了第二个实例看设置页：四张引擎卡一行排开不换行，微软卡片说明文案正常。
  共享的 `engineType` 默认值事后仍是 `apple`，没动到正在跑的正式版。
- 接口行为全部来自本轮 curl 实测，命令和返回都在上表。

---

## 2026-08-25 · PR #5 审核循环收尾

Codex 在第十七轮明确通过：**「Didn't find any major issues」**，
审的提交 `def9192e76` 就是当时的 HEAD。已合并（`57db217`）。

### 十六轮，26 个问题

按性质归类，比按轮次更有用：

| 类别 | 数量 | 典型 |
|---|---|---|
| 判定本身错了 | 6 | 韩文含汉字被压成中文；注音被当成简体；假名判定依赖了识别器 |
| 某条保护反而帮倒忙 | 7 | 复制兜底把粘贴引擎刚保护住的剪贴板清掉；瞬时失败被当成永久结论 |
| 「读不到」被当成「成功」 | 3 | AX 写入无法验证却报成功；粘贴后读不回 `kAXValue` 仍报 `.replaced` |
| 我自己修法之间的相互作用 | 5 | 第七轮的 cancel × 第六轮的缓存 → 连按快捷键反而更慢；180ms × 200ms 开两次会话 |
| 安全 / 隐私 | 1 | **Chromium 重试能绕过应用黑名单** |
| 误导性提示 / 死路 | 4 | 只读 `AXTextArea` 走进「按了没反应」；被黑名单挡掉时静默无反馈 |

### 三条值得记住的

**1. 同一套判断在仓库里有几份，就会漂移几次。** 第一轮那条韩文 bug 的根因是
字符区间在三个文件里各写了一份。抽成 `TextScriptPresence` 之后，
第十轮的注音问题只需要改两处、并且能用一条「断言两处结论一致」的单测钉住。

**2. 二值返回表达不了「不知道」。** 「AX 写入是否生效」「粘贴是否被接受」
都被写成了 `Bool` 语义，于是「读不到」只能挤进 true 或 false，
两次都挤进了「成功」。改成三态之后这类问题自然消失。

**3. 打补丁不如改原则。** 页脚语种那条我第一轮修过一次（按引擎类型分流），
第九轮的改动又从另一个方向把它带回来了。第十四轮改成
「源语言是 auto 时一律等引擎回报」之后，第一轮那个特例也随之删掉——
一条正确的原则比三个特例便宜。

### 还没被任何测试覆盖的

单测覆盖不了真实 AX 行为。以下几条只能靠人工验证，已写进提醒任务：

- `InputAssistChromiumAccessibility.settleNanoseconds = 200ms` **没有实测依据**
- Chromium / Electron 首次触发是否真的需要按两次
- 第十一轮放行 `ja` / `ko` 之后，中文专有名词、型号、短标题会不会被误判成日文

---

## 2026-08-25 · 第十六轮 Codex 审核：重试能绕过黑名单（PR #5）

两条 P2，第一条是**安全问题**。

### 1. Chromium 重试可以绕过应用范围检查

```
在 App A（允许）按快捷键 → 取词失败 → 排一次重试，identity = App A
        ↓ 用户切到 App B（黑名单里，比如终端 / 钥匙串 / 1Password）
重试读 pid → 拿到的是 App B 的 → enableIfNeeded(App B) → 睡 200ms
        ↓
睡醒后的 pid 校验：前台还是 App B，**通过**（它比的是"没再换过"，不是"还是原来那个"）
        ↓
在 App B 里取词成功 → startSession(identity: App A) → 送去翻译
```

黑名单默认就含 `com.apple.Terminal` / `com.googlecode.iterm2` / 钥匙串 /
1Password / Bitwarden——**这条能把这些应用里选中的文本送到在线翻译引擎。**

三处都补上：

- 在**排任务之前**就把 pid 钉下来，作为参数传进去
- 重试一开始先校验前台仍是那个 pid（不只是"没再换过"）
- 睡醒后**重新走一遍应用范围检查**，并且用现在读到的 identity 去开会话，
  不再用触发时那份

最后一条不只是补漏：用当时的身份给现在的窗口背书，本身就是错的记账方式。

### 2. 期望值应该在粘贴前重算，而不是用取词时的快照

浮层开着的这段时间里，编辑器完全可能在选区**之外**改动文本
（异步追加的后缀、聊天窗口新到的消息）。那时选区和原文都没变，
`targetIsUnchanged` 放行、粘贴也确实成功了——
但拿取词时的旧快照算出来的期望值和现状对不上，于是：

一次**成功**的替换被报成失败 → 提示"编辑器未接受译文替换" →
再用多余的复制兜底把刚还原好的剪贴板覆盖一遍。

改成在合成 ⌘V **之前**重读一次 `kAXValue` 作为基准，读不到才退回快照。

---

## 2026-08-25 · 第十五轮 Codex 审核：上一轮的口子开太宽了（PR #5）

一条 P2，是我上一轮修法直接造成的。

第十四轮为了让「关掉浮层后重新划选同一段文字」能再次触发，在 `.leftMouseDown`
上无条件清掉去重指纹。**口子开得太宽**：点窗口标题栏、点工具栏、点另一个非文本
控件都是左键按下，而它们**不改变**焦点元素的选区。于是——

浮层被点击关掉（面板自己的 mouse-down 监听）→ 指纹被清 → mouseUp 取到**没变过**的
选区 → 没有指纹可去重 → 180ms 后浮层又弹回来。用户点一下窗口边缘，它就回来一次。

比上一轮要修的那个问题更烦人。

### 权衡过程

想过在 mouseDown 时读一次 AX 判断选区到底变没变——那能精确区分两种情况，
但要在**全系统每一次左键按下**时发起 AX 调用，而同步 AX 往返是会卡住的
（`MiniAssistCapture` 那边为此专门加了消息超时）。
为一个默认关闭的实验开关，把这个风险加到最高频的事件上，不划算。

### 收窄成只认多击

`event.clickCount >= 2` 才清指纹。双击选词是关掉浮层后重新选中同一段文字**最常见**
的手势，而单击不是。这条判断是免费的，不需要任何 AX 调用。

**残留的两个情况，明确记下来不装作没有：**

- **单击拖选**重新选中完全相同的一段文字，仍然会被去重抑制。
  绕过办法：按快捷键，或先选别的再选回来。
- **双击窗口标题栏**（缩放窗口）也会清掉指纹，此时若选区没变，浮层可能弹回来。
  比"任意单击都弹回来"罕见得多。

---

## 2026-08-25 · 第十四轮 Codex 审核：两个修法的余波（PR #5）

两条 P2，都是我前面修法的副作用。

### 1. 关掉浮层后，原样重选同一段文字不再触发

第四轮为了让 Esc 能关掉浮层，加了「记下已处理的选区」。副作用是：
用户关掉浮层后**故意重新划选同一段文字**，指纹仍然匹配，自动显示被静默抑制。

关键在于监听的是 mouse-**up**：拖选过程中只有 mouseDown 和 mouseUp，
**中间不会有一次「空选区」的取词**来自动清掉指纹。所以那段选区会一直处于
被抑制状态，直到发生某个不相干的事件。

修法：把 `.leftMouseDown` 加进监听，按下左键就清指纹——那正是
「用户要重新划选」的信号。只认 `leftMouseDown`：Esc 关闭浮层不产生鼠标事件，
所以第四轮那条抑制路径完全不受影响，两个需求同时满足。

### 2. 页脚仍可能显示一个并没有送出去的语言

第九轮让 `translationContext` 在猜出的语言对被 `LanguageAvailability` 判成
`.unsupported` 时**弃用**兜底、改走自动识别。但 Mini 页脚那边照旧按兜底算标签，
于是又出现了「页脚说英语、引擎收到的是 auto」——**正是这行页脚要避免的事，
我自己在第一轮修过一次，第九轮的改动又把它带回来了。**

这次不再打补丁，直接改原则：**源语言是 `auto` 时一律留空，等引擎回报。**

任何在请求前做的推断都可能和实际使用的语言对不上——脚本兜底只有 Apple 会用，
而就算是 Apple 也可能因为不支持而弃用它。这行页脚的全部价值就在于它是真的，
宁可晚一两秒也不能先显示一个可能是错的。

副产品：第一轮加的那个「按引擎类型分流」的判断随之不需要了，
`sourceLanguageName` 从四个参数缩到一个。**一条正确的原则比三个特例便宜。**

---

## 2026-08-25 · 第十三轮 Codex 审核：两条延迟路径为同一段选区各开一次（PR #5）

一条 P2。又是两条路径撞车，只不过这次撞的是**时间**：

- 自动显示：选区完成后等 `180ms` 再取词
- 快捷键的 Chromium 重试：等 `200ms` 再取词

Chromium 的树在这 20ms 中间建好的话，自动显示先取到、开出浮层；
20ms 后快捷键重试也取到了，然后**无条件** `startSession`——
它会 `dismiss(.replacedByNewSession)` 再重开。

后果不只是浮层闪一下：**同一段文字被翻译两遍**。
OpenAI 那条路会在被取消之前多发一次请求，那是要计费的。

### 修法

给 `CandidateSession` 加 `matchesSelection(of:)`（元素 + 原文 + 范围），
重试路径在 `startSession` 之前先看一眼当前会话是不是就为这段选区开的，是就让开。

谁先到谁开，另一个站住——不需要在两条路径之间排优先级，也就不用担心
"优先的那条失败了另一条也不补位"。

单测把三种情况都钉住：同一段选区要让开；内容变了是新选区；
**内容相同但位置不同**（同一控件里的另一处相同文字）也是新选区。
最后这条和粘贴引擎里那个"两处相同文字"的坑是同一类问题。

---

## 2026-08-25 · 第十二轮 Codex 审核：连按快捷键反而更慢（PR #5）

一条 P2。这是第七轮和第六轮两个修法**互相绊了一下**：

1. 第一次按快捷键 → 取词失败 → 建 `chromiumRetryTask`，
   `enableIfNeeded` 返回 true（**并把这个进程记成"已打开"**），开始睡 200ms
2. 用户等不及，在这 200ms 里又按了一次 → 取词照样失败（树还没建好）→
   第七轮加的 `chromiumRetryTask?.cancel()` **把第一个任务掐掉**，新建第二个
3. 第二个任务里 `enableIfNeeded` 发现进程已记录，返回 false →
   它既不等待也不重试就退出了
4. 结果：浮层永远不会出现，用户得按**第三次**（那时树建好了，走的是快路径）

**连按反而比按一次更慢。**

### 修法：让在途的那次跑完

加 `isChromiumRetryInFlight`，已有重试在等时直接 return，不去掐它。

**没有选另一个方向（"已打开的情况也照样等一次再重试"）。** 那样每一次取词失败
都要先等 200ms 才出提示，而最常见的失败原因是"用户根本没选中文字"——
等于给最高频的错误路径统一加 200ms 延迟。

### 顺带修一句误导的提示

已经打开过 AX 树、却仍然读不到选中文本时，原来提示的是「请先选择要翻译的文字」。
用户明明选了，这句话是错的。改成「当前应用没有提供选中文本，可能需要重新选择一次」，
靠 `hasSettledResult(pid:)` 区分这两种情况。

---

## 2026-08-25 · 第十一轮 Codex 审核：纯汉字的日韩文（PR #5）

一条 P2，**动的是一条有意为之的既有决策**，所以单独记一下判断过程。

`selectionSourceLanguageCode` 的注释写得很清楚，它存在就是为了**不盲信**通用语言识别：

> 数字、型号、多个标点和中英混排会让 NLLanguageRecognizer 返回非中文或 nil，
> Apple Translation 随后检查错误的语言对，已安装的中文包也会被误报成"需要下载"。

所以「汉字文本一律按中文」是刻意的，不是疏漏。原来的代码只在**有假名**时才保留 `ja`。

审核指出：`東京大学` 这种纯汉字日文没有假名可依，识别器给出的 `ja` 被覆盖成 `zh-CN`。
（顺带更正审核的一处措辞：这不是本 PR 引入的回归，改动前后行为一致，是既有行为。）

### 为什么这次接受

关键在于那个 bug 的**失败模式**是「返回非中文或 nil」，而具体是 `en` 和 nil，
**不是 `ja`/`ko`**。只放行这两个语言，不会让它复发。

再加一层：`LanguageDetectionService` 要求置信度 ≥0.60 且与第二名相差 ≥0.15。
能在汉字文本上给出 `ja`，说明它确实明显领先于中文，不是勉强过线。

所以例外划得很窄：**只有 `ja` 和 `ko`**，其余一切（尤其 `en`）仍按中文处理，
并且原有那条 `en` 的单测保留着，注释里写明"不能扩大到 en"。

### 需要人工确认

这条改的是产品判断而不是纯粹的对错，值得在人工测试时留意：
选中中文的专有名词、型号、短标题时，会不会有被识别成 `ja` 的情况。
如果有，说明这个例外还是开大了。

---

## 2026-08-25 · 第十轮 Codex 审核：注音被当成简体（PR #5）

一条 P2。注音符号（ㄅㄆㄇㄈ）原本和汉字合并在同一个分支里：

```swift
if scripts.containsHan || scripts.containsBopomofo {
    return normalizedChineseVariant(preferredChineseVariant)
}
```

目标语言是英文时 `normalizedChineseVariant("en")` 退到 `zh-CN`，
于是一段**明确的繁体信号**被交成简体源语言。

**注音自己就说明了简繁：台湾用注音，大陆用拼音。** 汉字才是简繁通用、
只能靠目标语言去猜的那一类。把两者合并，等于用"猜"覆盖了一个确定的信号——
而且猜错会让 Apple 选错语言模型。拆开，注音直接返回 `zh-TW`。

同一条规则补进了 `InputAssistLanguagePolicy.selectionSourceLanguageCode`——
那边原本压根没处理注音。跨文件不一致正是第一轮就点过的问题（韩文含汉字那条），
不想再犯第二次，所以配了一条专门断言两处结论一致的单测。

---

## 2026-08-25 · 第九轮 Codex 审核：兜底越界，以及"读不到"被当成"成功"（PR #5）

三条 P2。

### 1. 脚本兜底越过了它自己的适用边界

`translationContext` 里，识别器**识别成功但那个语言对不被支持**时，
执行仍然会落到脚本兜底。一段被高置信度识别出的加泰罗尼亚语（`Configuración` → es 0.98
是同一类），脚本兜底只看书写系统，会给出 `en`——于是它被当成英语翻译。

**用错误的源语言翻出来的结果，比一句「不支持」糟得多，因为用户看不出它是错的。**

而且这违反了 `LanguageScriptFallback` 自己文档写明的契约：它是给「识别器弃权」用的。
加上 `detection == nil` 的前置条件。

配的单测把边界钉在调用点：同一段 `Configuración`，识别器给 `es`、兜底函数给 `en`，
两者本来就不同——所以「什么时候调用兜底」必须由调用点严格把关，
不能指望兜底函数自己聪明。

### 2. 粘贴后读不回 `kAXValue` 时报了成功

能算出精确期望值，说明取词那一刻 `kAXValue` 是读得到的
（`expectedValueAfterPaste` 依赖 `elementValueAtCapture`）。粘贴后读不到，
那是瞬时故障或焦点变了——原来的 `if let ... , currentValue != expected` 写法
会把这种情况跳过并报 `.replaced`。目标要是拒绝了粘贴、或根本没收到，
我们记一次成功，还不给复制兜底，用户什么都拿不到。

抽出 `verifyPaste` 返回三态（`applied` / `rejected` / `unverifiable`），
读不回来时返回 `.aborted(reason: .writeVerificationUnavailable)`。

「什么都读不到」那一档（连选区都拿不到）仍然保持宽松——否则这类应用里
粘贴永远算不成功，整条路径失效。三态让这两种情况能分开处理，
而原来的二值写法做不到。

### 3. settle 窗口里的剪贴板改动还是会被清掉

上一轮堵的是 `pressPaste()` **之前**的争用。`pressPaste()` 之后还有 250ms 的
settle 等待，这段时间里的写入 `restoreIfUntouched` 会静默地不还原（对的），
但接下来一旦报失败，协调器仍会走复制兜底把它清掉——同一个矛盾，窗口换了个位置。

在还原**之前**记下 `changeCount` 是否变过；只有在要报失败时才用它转成 `.clipboardBusy`。
顺序很重要：粘贴确实生效时不能报争用——替换已经成功，协调器根本不会去碰剪贴板。

### 顺带：提示语跟上

粘贴这一步现在也可能返回「无法验证」，那时说「编辑器拒绝替换」是错的。
两个分支都改成看**最后一次尝试**的结果来选措辞。

---

## 2026-08-25 · 第八轮 Codex 审核：兜底把引擎刚保护住的东西毁掉了（PR #5）

一条 P2，是个自相矛盾。

`InputAssistEditorPasteEngine` 有两处剪贴板争用的失败返回：快照时 `changeCount`
对不上、以及放上译文之后又被别人改写。这两处**刻意不覆盖也不还原**——
那份新内容是用户或剪贴板管理器刚写进去的。

然后协调器接住这个失败，无条件走 `copyTranslatedText`，
第一句就是 `NSPasteboard.general.clearContents()`。

**引擎前脚保护，协调器后脚清掉，比一开始就不保护还糟**：
不保护至少行为是一致的，现在是白白多绕一圈还是丢了。

### 修法：让失败带上原因，而不是靠比对提示语

两处从 `.failed(message: "剪贴板...")` 改成 `.aborted(reason: .clipboardBusy)`。
用结构化原因而不是匹配中文字符串——后者改一个字就静默失效。

协调器上加 `preservesForeignClipboard` 判定，命中时不复制，
只提示「剪贴板正被其它应用使用，本次未替换，请稍后重试」。

**取舍写在这里**：这样用户这次拿不到译文。但他自己刚复制的东西还在——
译文重来一次就有，被覆盖掉的剪贴板不可恢复。

单测把两个方向都钉住：`.clipboardBusy` 必须挡掉复制兜底，
其余每一种结果都**不能**挡（免得以后有人收得过宽，把最常见的兜底路径废掉）。

---

## 2026-08-25 · 第七轮 Codex 审核：延迟任务与测试窗口的误导（PR #5）

### 1. 禁用功能后，睡着的重试任务还会把浮层弹出来

Chromium 打开 AX 树后的重试是个**没有留句柄**的 `Task`。
它睡着的那 200ms 里，用户完全可能从菜单栏把选区翻译关掉——
`applyEnabledState()` 收摊之后，这个任务醒过来仍然会 `startSession`，
弹出浮层并发出翻译请求。

睡醒后的检查原本只有 `isCommitting` 和前台 pid，两条都跟"功能还开着吗"无关。

两头都补：留住句柄，在 `applyEnabledState()` 的禁用分支和 `deactivate()` 里取消；
睡醒后再查一次 `settings.isEnabled`（取消和检查之间仍有窗口，两条都要）。

### 2. 测试窗口把「选中自动」显示成可用，但它在那儿不会触发

`NSEvent.addGlobalMonitorForEvents` **收不到本应用自己的**鼠标键盘事件，
所以选区自动显示在 `InputAssistTestView` 里永远不会触发。
而状态栏把「选中自动」显示成 OK——它报的是"设置开没开"，
读起来却像"这里可用"。

**没有为此加本地监听。** 那需要给生产路径（全局键鼠监听 + AX 取词 + CGEvent tap）
再接一个事件源，纯粹为了一个调试界面；而且正如审核自己指出的，
进程级本地监听会连设置页里的选区一起响应，要"只限测试窗口"就得再插一层窗口身份判断。
为一个调试面板给生产路径加分支，得不偿失。

改成把话说清楚：状态项改名「选中自动（其它应用）」，并在说明里写明原因。
在这里试不出来的人本来会以为整个功能坏了，现在他知道该去别的应用试。

---

## 2026-08-25 · 第六轮 Codex 审核：pid 会被回收（PR #5）

一条 P2，和上一轮那条同类——都会让某个应用的取词**永久**不可用。

`InputAssistChromiumAccessibility` 用裸 pid 做缓存键。macOS 会回收 pid，
而翻译官是常驻的：被缓存的应用退出后，新启动的 Chromium 应用可能拿到同一个 pid，
于是被当成「已经处理过」，`AXManualAccessibility` 再也不会写下去，
那个进程的快捷键取词和自动显示就都失效了。

审核给了两个方向，两个都做了，各挡一处：

- **进程身份**：缓存键从 `pid` 换成 `pid + launchDate`。这一层是确定性的，
  不依赖任何通知时序。
- **终止时驱逐**：监听 `NSWorkspace.didTerminateApplicationNotification` 清记录。
  这一层兜住启动时间取不到的情况——应用退出后 `NSRunningApplication` 就查不到了，
  新旧两个进程的 `launchDate` 都会是 nil 而"相等"，光靠身份区分不开。

观察者是首次 `enableIfNeeded` 时懒注册的，不需要在协调器的生命周期里额外接线。

---

## 2026-08-25 · 第五轮 Codex 审核：两处"在最不该依赖的地方依赖了"（PR #5）

两条 P2，本质是同一个毛病的两种形态。

### 1. 假名 / 谚文的判定依赖了识别器

`selectionSourceLanguageCode("私の", detected: nil)` 原本返回 `zh-CN`：
`私` 命中 `containsHan`，而保留日文的分支要求 `detected == "ja"`——
可 `私の` 只有两个 compact 字符，过不了 `LanguageDetectionService` 的字数门槛，
识别器弃权返回 nil。

**假名和谚文是决定性的：中文里不会出现它们。** 要求识别器先给出 `ja` 才肯保留日文，
等于在识别器最不可靠的地方（短文本）去依赖它。改成脚本直接决定，
顺序与 `LanguageScriptFallback` 一致：假名 > 谚文 > 汉字。

「识别器明确认定是中文」那一条仍然排在最前面——简繁之分只有它能给，
而且中文句子里引用一小段日文时应该按中文处理。

### 2. 瞬时失败被当成了永久结论

`InputAssistChromiumAccessibility.enableIfNeeded` 原本在**知道结果之前**
就把 pid 插进集合：

```swift
guard pid > 0, !enabledProcessIdentifiers.contains(pid) else { return false }
enabledProcessIdentifiers.insert(pid)          // ← 还不知道成没成功
let result = AXUIElementSetAttributeValue(...)
```

`.cannotComplete`（目标应用正忙、或刚启动还没起来——Electron 应用启动慢，
这很常见）会被当成永久结论，**那个应用从此再也不会重试**，
直到它或翻译官重启。代价远大于"多试一次"。

拆成两个集合：成功的记 `enabled`，`.attributeUnsupported` / `.notImplemented`
记 `unsupported`（非 Chromium，再试也一样），其余一律不记。

`AXError → 要不要记住`这个判断抽成纯函数 `classify(_:)` 并配单测——
判错的代价是一个应用永久失效，这件事值得钉住。

原来那条 `isEnabled(pid:)` 改名 `hasSettledResult(pid:)`：
语义从"打开过吗"变成"有结论了吗"，瞬时失败不算。

---

## 2026-08-25 · 第四轮 Codex 审核：Esc 关掉的浮层会自己弹回来（PR #5）

一条 P2。触发链：

1. 自动显示开着，用户选中文字
2. 在 180ms 自动取词跑起来**之前**先按了快捷键 → 浮层由快捷键那条路打开，
   而 `SelectionMonitor.lastFingerprint` 还是空的（快捷键路径从不碰它）
3. 用户按 Esc。浮层在 key-**down** 上关掉——`CandidateKeyTap` 的 mask 只有
   `keyDown | flagsChanged`
4. Esc 的 key-**up** 漏到选区监听的全局监听器 → 排一次取词
5. 180ms 后：选区没变、浮层已经不可见（`isSelectionAllowed` 放行）、
   `lastFingerprint` 为空所以去重不生效 → **浮层立刻自己弹回来**

也就是 Esc 关不掉。

### 修法：记选区，而不是排除按键

`emit` 里的去重逻辑抽成 `registerSelection(_:)`，快捷键那条路在 `startSession`
里也调一次。

**刻意不选「排除关闭类按键的 key-up」那个方向**：那需要枚举所有关闭动作
（Esc、Enter 提交、⌘C 复制、⌘1–6 直选、点击外部…），漏一个就复发。
记选区是对**任何**关闭方式都一致生效的——浮层怎么关的无所谓，
那段选区已经处理过了就不再自动弹。

顺带发现 `resetSelection()` 从来没有被调用过。这次没删——
它和新的 `registerSelection` 正好构成一对（清除 / 记录），留着是合理的 API。

---

## 2026-08-25 · 第三轮 Codex 审核：上一轮没修干净（PR #5）

一条 P2，而且是第二轮第 3 条的**同一个问题**——我上一轮只修了一半。

上一轮我去掉了「`kAXSelectedTextRange` 可写 ⇒ 可粘贴」这条推断，
却把它上面的 `isEditableRole(role)` 短路留着了：

```swift
if InputAssistCommitPolicy.isEditableRole(role) { return true }   // ← 漏在这里
return isSettable(kAXValue) || isSettable(kAXSelectedText)
```

只读的 `NSTextView`（日志窗、只读代码预览、聊天记录区）**同样暴露 `AXTextArea`**，
所以那条死胡同原封不动地留着：⌘V 什么都改不了 → 拿不到完整 AX value 时
粘贴引擎无法证伪 → 报成功 → Enter 既没替换也不退到复制兜底 →
用户只看到「按了没反应」，没有任何线索。

### 修法一：role 是线索，不是证据

`allowsEditorPaste` 不再接收 role，只认 `kAXValue` / `kAXSelectedText` 可写——
这两个才真正表明文本可改。

`InputAssistCommitPolicy.isEditableRole` 随之成为死代码，**一并删掉**。
留着它是个陷阱：下一个人会看到一个叫 `isEditableRole` 的现成函数，
拿去做我们刚刚否掉的那个判断。它的三条断言也从测试里删了。

### 修法二：算不出精确期望值时，至少要能证伪

审核连着两轮都点到同一句话——「无法验证时报成功」。这才是让判错变成
死胡同的真正原因。但**不能简单改成失败关闭**：`expectedValueAfterPaste`
需要 `sourceRange` 和 `elementValueAtCapture` 同时存在，
而 `.editorPaste` 这一档**本来就常常**缺其中之一（`CommitPolicy` 正是缺了才选它）。
一律判失败会让整条粘贴路径彻底失效。

改成加一个便宜的证伪信号：**粘贴真的生效的话，选区里不可能还是原文。**
只读控件吃掉 ⌘V 之后选区原封不动，正好落在这里，于是能正确退到复制兜底。
译文与原文相同的情况在函数开头已按 `.alreadyMatching` 返回，不会误判。

---

## 2026-08-25 · 第二轮 Codex 审核：替换路径上的三个安全问题（PR #5）

三条全在替换路径上，两条 P1。这是整个功能里最不能出错的地方。

### 1. 不可验证的 AX 写入之后，不能再走粘贴兜底（P1）

把 `InputAssistTextReplaceEngine.replace` 的所有出口列一遍就清楚了：

| 出口 | 写了吗 | 能兜底吗 |
|---|---|---|
| 初始 verdict abort / focusLost | 没写 | ✅ |
| `.replaceSelection`（算不出精确位置） | 没写 | ✅ |
| selectRange 失败 | 没写 | ✅ |
| settle 后 abort（选区变 / 焦点变 / 应用切） | 没写 | ✅ |
| 算不出 expected / 不支持精确替换 | 没写 | ✅ |
| `.replaced(.axDirect)` | 写了，已验证生效 | — |
| `.failed("当前应用拒绝了原位替换")` | 写了，已验证**没**生效 | ✅ |
| **`.aborted(.writeVerificationUnavailable)`** | **写了，不知道生没生效** | ❌ |

只有最后一行会出事：AX 写调用已经发出去了，读不回结果无法证伪。
此时再合成一次 ⌘V，万一那次写其实成功了，**译文会被插入两遍**——
而且是在用户明确要求「安全替换」的路径上。

加 `InputAssistReplacementOutcome.allowsPasteFallback` 收口，配了单测把
「除它之外每一种出口都仍然允许兜底」也钉住，防止以后收得过紧把兜底整个废掉。

顺带改了提示语：这种情况说「无法安全替换」是**错的**，它可能已经生效了，
改成「无法确认替换是否生效，译文已复制」。

### 2. 选区读不回来时必须失败关闭（P1）

`InputAssistEditorPasteEngine.targetIsUnchanged` 原本是：

```swift
if let expectedRange = ..., let currentRange = ..., currentRange != expectedRange {
    return false
}
```

`currentRange` 读不到时整个 `if` 被跳过，位置这一关就没了，只剩「选中文本是否等于原文」。
**同一个控件里出现两处相同文字时**（日志、表格、重复短语），用户把选区挪到另一处，
文本比对照样通过，⌘V 就打在错误的位置上。

改成：expectedRange 存在时，currentRange 必须读得到且相等，否则放弃。

### 3. 能移动选区 ≠ 能改文本（P2）

`allowsEditorPaste` 原本把 `kAXSelectedTextRange` 可写也算作「可粘贴」。
但只读的文本视图（日志窗、只读代码预览）**通常也允许**辅助功能客户端移动选区。

据此判成 `.editorPaste` 会走进一个死胡同：⌘V 什么也改不了，
而拿不到完整 AX value 时粘贴引擎又无法证伪、只好报成功，
于是 Enter 既没替换、也不会退到复制兜底——用户看到的是「按了没反应」。

改成只认 `kAXValue` 或 `kAXSelectedText` 可写，这两个才真正表明文本可改。

---

## 2026-08-25 · 第一轮 Codex 审核的三个问题（PR #5）

三条都成立，其中两条是我自己没看出来的。

### 1. 韩文含汉字被压成中文

`selectionSourceLanguageCode("한자漢字", detected: "ko")` 原本返回 `zh-CN`：
只看「有没有汉字」，而保留分支只覆盖了假名，谚文漏了。
候选请求的源语言因此是错的，还会连带把错误的目标行过滤掉。

审核指出的「与新的共享脚本兜底不一致」是根因：这个文件里还留着**第三份**
字符区间副本（`containsHan` / `containsJapaneseKana`）。
一并换成共享的 `TextScriptPresence`，覆盖范围是原来的超集
（多了汉字扩展区 U+20000+ 与半角片假名 U+FF65–FF9F）。

**这就是「同一处漏两次就去找现成的」那条教训的现场版本**：
同一套字符区间在仓库里存在三份时，修其中一份不会让另外两份跟着对。

### 2. 自动选区路径没有 Chromium 兜底

只有快捷键那条路调了 `enableIfNeeded`。开了「选中后自动显示候选」的用户
在 Chromium/Electron 里必须先手动按一次快捷键才能用——而且这条路是
**静默丢弃**的，连提示都没有，用户只会觉得这个开关没用。

自动路径补上同样的一次性 enable-and-retry。不会变成「每点一下都重试」：
`enableIfNeeded` 每个进程只返回一次 `true`，所以每个应用至多多花一次 settle 等待。

重试任务刻意复用 `pendingTask` 而不是另开句柄——`stop()` / `resetSelection()` /
应用切换观察者都只认它，挂在这里才能被一并取消。

### 3. 非 Apple 引擎显示了引擎并未收到的语种

这条最要害，它直接违反了我给这行页脚写的原则：**显示的必须是真正送去翻译的那个语言**。

脚本兜底**只有 `AppleTranslationCoordinator` 真的会用**，其它引擎收到的就是 `auto`。
在那些引擎上显示推断值，恰好就是这行页脚要避免的事——模型返回纯文本兜底、
没有可用 detected code 时，那个猜出来的标签会一直挂着，
把一个非英语的短 Latin 短语说成英语。

按引擎类型收口：`.apple` 才用脚本兜底，其它引擎等 `detectedSourceLanguageCode` 回报，
回报不了就不显示。

---

## 2026-08-25 · 选区翻译在 Chromium / Electron 应用里按快捷键没反应

现象：在 Claude、Slack、Discord、VS Code、Notion、Chrome 里选中文字按 ⌥⇧Space，
什么都不发生，或者提示「请先选择要翻译的文字」——但用户明明选了。

### 不是快捷键没触发，是取词拿不到东西

`Chromium` **默认不维护辅助功能树**。维护一棵完整的树对渲染进程有实打实的开销，
所以它做成按需构建：只有当某个客户端明确表示需要时才建。在那之前：

```
kAXSelectedText → 读不到
kAXValue        → 读不到
   ↓
capability(of:) == .unavailable
   ↓
captureSelectedText() 返回 nil
   ↓
toast「请先选择要翻译的文字」
```

Electron 应用全部继承这个行为，所以是"很多地方"而不是某一个应用。
`/Applications/Claude.app` 里有 `Electron Framework.framework`，正是这一类。

### 修法：需要时才请对方把树建起来

新增 `InputAssistChromiumAccessibility`：给应用的 AX 对象设 `AXManualAccessibility = true`。

**刻意不用 `AXEnhancedUserInterface`。** 那是 VoiceOver 的总开关，对非 Chromium 应用
同样生效，已知会让部分应用窗口尺寸错乱、Java 应用行为异常。
`AXManualAccessibility` 是 Chromium 自己提供的入口，作用范围正好只有它。

**只在取词失败时才去开，不对每个切换到的应用都动手。** 这是在让别人的进程多干活，
不该默认施加。同一个进程只设一次，之后按快捷键直接走快路径。

Chromium 是异步建树的，设完要等一下。目前取 200ms——**这个值没有实测依据**，
真实需要多久取决于页面复杂度。等完仍然读不到时会明确提示「已为当前应用开启辅助功能读取，
请重新选中一次文字」，不静默失败，所以即使 200ms 不够也不会变成一个查不出原因的 bug。

等待期间前台应用可能已经切走，重试前会比对 pid，不同就放弃这次快照。

### 顺带：被黑名单挡掉时不再静默

`handleShortcutTrigger` 命中应用范围排除时原本是**静默 return** 的——
用户在终端里按快捷键，什么都不会发生，也无从知道是被自己的设置挡掉的。
（默认黑名单里有 `com.apple.Terminal` 和 `com.googlecode.iterm2`，
在终端里跑东西的人很容易撞上。）现在会明确提示。

### 没做：⌘C 兜底取词

还有一类应用即使打开了 AX 树也不暴露选中文本（部分 PDF 阅读器、自绘控件、Java 应用）。
通用解法是合成一次 ⌘C 取词、再用 `InputAssistPasteboardSnapshot` 把剪贴板还原回去。

**这次没做**：它会动用户的剪贴板，而且拿不到 AX range，
那些应用里的提交会退化成只能复制。这是个需要单独拍板的取舍，不该顺手带进来。

---

## 2026-08-25 · 短语翻译弹出「无法自动检测语言」

Mini 模式下翻译短语（截图里的例子是 `TestHost`），Apple 引擎会弹出系统的
「无法自动检测语言。请选择要翻译的语言。」选择器。在气泡场景里那是个死胡同。

### 先量，别猜

直接跑 `NLLanguageRecognizer.languageHypotheses`：

| 文本 | 字母数 | 我们的判定 | 识别器前三 |
|---|---|---|---|
| `TestHost` | 8 | nil（置信度） | **cs:0.21** en:0.13 nl:0.09 |
| `Settings` | 8 | nil（置信度） | en:0.33 sv:0.16 is:0.11 |
| `Download` | 8 | nil（置信度） | en:0.19 nl:0.15 da:0.14 |
| `hello world` | 10 | nil（置信度） | en:0.31 id:0.10 hu:0.08 |
| `The quick brown fox jumps over` | 25 | **en** | en:0.85 nl:0.10 nb:0.03 |
| `设置` | 2 | nil（字数不足） | **zh-Hans:1.00** |
| `你好` | 2 | nil（字数不足） | **zh-Hant:0.80** zh-Hans:0.20 |
| `Привет` | 6 | nil（字数不足） | bg:0.53 ru:0.42 kk:0.04 |

两个结论：

**1. 识别器在短 Latin 词上基本没有价值。** `TestHost` 它排第一的是**捷克语 0.21**，
英语才 0.13。所以 `LanguageDetectionService` 那个 0.60 阈值挡掉它是**对的**，
**不要为了这种情况去调低阈值**——那会拿长文的准确率换短语，且短语依然测不准。

**2. 中文也中招，而且原因不同。** `设置` 识别器给的是 zh-Hans:1.00，
是被我们自己的字数门槛（compact script 要 ≥3 字）挡掉的。
**脚本本身已经把答案写在脸上了，我们却把它扔了。**

### 根因链

```
短语 → LanguageDetectionService 弃权 (nil)
     → MiniTranslationDirectionResolver 返回 sourceLanguageCode = auto
     → AppleTranslationCoordinator.translationContext 用同一个服务再测一次（同样 nil）
     → TranslationContext(sourceLanguage: nil)
     → Apple 自己的自动识别在短语上同样失败
     → 弹出语言选择器
```

### 修法：只补一层脚本兜底，不动识别器

新增 `LanguageScriptFallback`：识别器弃权后，用书写系统能确定的部分兜底。
接在 `translationContext` 交出 `sourceLanguage: nil` **之前**——那是所有调用方
（Mini、主窗口、截图 OCR、选区翻译）共同的最后一道口子，改一处全都覆盖。

顺序上假名和谚文必须排在汉字前面：日文正文里混着汉字、韩文里也可能出现汉字，
先判汉字会把这两种都认成中文。配了单测钉住（`한자漢字` → ko，`日本語のテスト` → ja）。

Latin 只能猜 `en`——八个字母分不出 en / es / nl。但**猜一个仍然远好过交 nil**：
猜错顶多译得不准，交 nil 是直接走不下去。而且兜底只在识别器弃权时才触发，
长文本（`Configuración` → es:0.98）根本走不到这里。

两条防止帮倒忙的闸门：

- 猜出来的语言对如果 `LanguageAvailability` 判定 `.unsupported`，**放弃兜底**，
  继续走原来的自动识别，别用一个必然抛错的语言对把路堵死。
- 纯数字 / 符号连脚本信息都没有，老老实实返回 nil，不瞎猜。

### 顺带

`ScriptPresence` 原本是 `MiniTranslationDirectionResolver` 的私有类型，
兜底逻辑要用同一套字符区间判断。抽成共享的 `TextScriptPresence` 并补上
Cyrillic / Latin，而不是再抄一份区间表——两份迟早会漂移。
原有的 Han / Kana / Hangul / Bopomofo 判断逐字未动，配了单测确认。

### 顺带：气泡页脚显示识别到的原文语种

修完之后有个副作用值得利用——现在我们**确实知道**送去翻译的是什么语言了。
在 Mini 气泡页脚右侧加一行 tertiary 的「原文 英语」，10pt，和已有的
「智能翻译识别已介入」共用同一行，所以高度只加一次（配了单测）。

取值顺序是刻意的：优先用我们**实际请求**的源语言，是 auto 时用和
`AppleTranslationCoordinator` **同一套** `LanguageScriptFallback` 推；
等引擎回报了 `detectedSourceLanguageCode` 再拿它盖掉。

关键是第二条——显示的必须是真正送去翻译的那个语言。
如果页脚自己另算一套，就会出现「页脚写着英语、引擎收到的是 auto」这种
比不显示更糟的情况。

### 没做

- **没有调低 `LanguageDetectionService` 的阈值或字数门槛。** 见上面第 1 条：
  那是拿长文准确率换短语，而且换不到。兜底解决同一个问题且不影响长文。
- **没有处理「源语言 == 目标语言」**（比如已经是目标语言的短语）。
  现在会被 `.unsupported` 闸门挡回原路径，行为和修改前一致，是另一个问题。

---

## 2026-08-23 · Input Assist（输入增强）完整实现

- 分支：`feature/input-assist`
- PRD：`Bagayalu_Input_Assist_PRD_v1.0.md`
- 调研产出：`InputAssist-reuse-notes.md`

按 PRD §55「不按页面开发，按风险开发」的排期走完 Phase 0 → 5：
先读开源、再做最稳的手动链路、最后才碰风险最高的自动触发。

```
中文正常上屏 → 检测到新增中文 → 停顿 / 强标点 → 并行翻译 → 候选浮层 → Enter 安全替换
```

---

### Phase 0：八个参考项目全部 clone 下来读过源码

结论写在 `InputAssist-reuse-notes.md`，这里只记两条影响了决策的：

**1. PRD §41 把 punto-switcher 列为 P0，但它没有 LICENSE 文件。** SwiftType 同样没有。
没有 LICENSE = 保留所有权利，按 §34「复用前必须确认 LICENSE」的规矩，
这两个只能读思路、不能抄代码。原本要参考它们的「capability-based replace」
改用 translate-kit（MIT），「候选状态机 / 键盘导航」改成自研 + 单测。
（traple 被 GitHub 标 NOASSERTION 是误报，正文是标准 MIT，尾部只是附了第三方词表说明。）

**2. Spike 2（Apple 拼音 composition 保护）在八个项目里零覆盖。**
全仓库检索 `markedText` / `AXMarkedRange` 只有 MacishType 命中，
而它是**作为输入法本身**读自己的组字状态；八个项目也没有一个用过 `AXObserver`。
PRD §29 设想的自动触发链路是整份 PRD 里唯一没有任何参考实现的架构。

---

### 三件从别人 bug 里直接拿到的答案

**AX → Cocoa 坐标翻转必须用主屏高度，不能用全局 maxY。**
两套坐标系都以主屏（`screens[0]`，origin 恒为 0,0）为基准互为上下翻转。
用「所有屏幕的全局 maxY」的话，只要副屏比主屏更高或更靠上，整体就偏移，
浮层直接落到另一台显示器上。这是 TypeTide issue #4 已经用 bug 换来的答案，配了单测钉住。

**`AXUIElementSetAttributeValue` 返回 `.success` 不代表文字真的被改了。**
translate-kit 源码里明确写着 Chromium 会接受调用然后悄悄忽略它。
所以写完必须读回 `kAXValue` 和写之前比一比，不同才算成功，否则退到粘贴兜底。
读不到 `kAXValue` 时（部分富文本控件不暴露）无法证伪，保守当作没生效——
多贴一次的代价远小于用户以为翻译了、实际什么都没发生。

**剪贴板快照要按 `item.types` 逐类型深拷贝。** 只存 `string(forType:)` 的话，
用户剪贴板里原本的图片、富文本、文件引用在 fallback 之后就没了。
粘贴后恢复的延迟：TypeTide 用 150ms、translate-kit 用 300ms，取中间值 250ms。

---

### 按键拦截：和上一条日志的结论正面相撞，但没有推翻它

PRD §14.1 要求浮层不抢焦点、却要吃掉 ↑ / ↓ / Enter / Esc / ⌘1–6 / ⌘C。
`NSEvent.addLocalMonitorForEvents` 只在本 App 是 key window 时才收得到，
`addGlobalMonitorForEvents` 收得到但不能吞。只有 `.defaultTap` 的 CGEvent tap 能做到。

上一条日志刚把全局快捷键从 `.defaultTap` 换成 `.listenOnly`。那条结论继续有效，
两者分工不同：

| | `GlobalHotkeyMonitor` | `CandidateKeyTap` |
|---|---|---|
| 存活时间 | 常驻 | 只在浮层可见的几秒 |
| 是否吞事件 | 从不 | 必须 |
| tap 选项 | `.listenOnly` | `.defaultTap` |

新 tap 背着三条硬约束（写在类注释里）：生命周期严格绑定浮层、
回调里绝不做任何 IO、处理 `tapDisabledByTimeout` 重新 enable。
UASwitcher 里是同样的取舍：`triggerConfig.isCapsLock ? .defaultTap : .listenOnly`。

顺带从 traple 学到**注入事件自标记**：替换时合成的 ⌘V 打上 `.eventSourceUserData` 标记，
回调开头直接跳过，免得自己发的按键再进自己的 tap。

**这套拦截和输入法候选框会不会打架？** 不会，因为浮层只在 commit 之后才出现，
那时原生候选框已经关了。浮层可见时用户再打字，第一个字符走
`dismissPassingEventThrough`（关浮层但**不吞**），tap 随即拆掉，
后面的 ↑↓ 自然回到输入法手里。

---

### Phase 3：自动触发怎么绕开「没有 API 能问出组字状态」

**不去问状态，只看新出现的文字长什么样。**
中文输入法开着、新增内容却全是 ASCII 字母 → 那是还没上屏的拼音，等着；
一旦出现汉字 → 已经 commit 了。判定落在
`InputAssistAutoTriggerPolicy.classify`，纯函数，有单测。
输入法是不是 CJK 用 TIS 的 `kTISPropertyInputSourceLanguages` 判断。

**PRD §32 建议的 TextDiffTracker 最后没做，而且不做更稳。**
拼音 `nihao`（5 字符）被 commit 成「你好」（2 字符）时**光标是往回跳的**，
任何「只往前增长」的 diff 在这里都会失效；放宽成通用 diff 又会把组字过程中的
每一次抖动都当成新输入。

换个锚点就没这个问题：只记住**这轮输入开始时光标在哪**，
新增内容永远是 `value[burstStart ..< caret]`。组字期间光标怎么来回跳都不影响这个区间的定义，
commit 之后它恰好就是那句中文。

**替换范围取「输入起点」和「当前句起点」里靠后的那个。**
用输入起点保证不改用户之前写好的内容（§9.1「不得修改前文」），
用句起点保证不跨多个完整句子（§9.3）。两条 PRD 要求一个 `max()` 就同时满足了。

**PRD §8.2 的 50–100ms 保护延迟被普通 debounce 覆盖了。**
时间基准本来就是「最后一次 AX 文本变化」，而原生候选框正是在 commit
（也就是那次文本变化）的瞬间关闭的，200–500ms 的 debounce 足够。
只有强标点绕过 debounce 那条路径单独留了 80ms。
因此**没有**为自动触发再挂一个常驻键盘 tap。

**替换期间必须暂停自动触发。** 我们写回去的译文同样会触发 `kAXValueChanged`，
不挡住的话会被当成「用户又输入了新内容」，转头对着自己的译文再弹一次。

---

### 本项目自身的硬约束：Apple 引擎必须一个语言一个 coordinator

PRD §18 要求所有目标语言并行翻译。但现有 `AppleTranslationCoordinator`
**同一时刻只能处理一个请求**——单个 `continuation` + `pendingRequest`，
新请求进来会 `cancelPending` 掉旧的，而且它依赖 SwiftUI `.translationTask` 送 session 进来。
一个 coordinator 做多语言，第二个语言会把第一个打断。

于是加了 `InputAssistAppleTranslationPool`：每个目标语言一个独立 coordinator，
各自挂一个 `.translationTask`，宿主是 1×1、`alphaValue = 0` 的常驻面板。
**用 alphaValue 0 而不是 orderOut**：窗口必须留在屏幕上 SwiftUI 才会持续驱动
`.translationTask`，但用户完全看不见。这样即使主窗口被关掉，Apple 翻译 session 依然活着。

Google / OpenAI 本身无状态、天然可并行，只是 `FrozenTranslationExecutor.execute`
的签名要求传一个 coordinator，给它一个闲置实例即可，不占用池子里的槽位。

---

### 几个不显眼但影响正确性的决定

**Enter 在没有译文时绝不能吞。** 候选还在转圈时用户按了 Enter，
如果我们吃掉又什么都不做，在 WhatsApp / 微信里就等于**消息发不出去**。
`InputAssistKeyRouter` 拿 `committableIndices` 判断：没有可提交的译文就放行 Enter 并关浮层。
⌘1–6 同理，⌘4 在只有 3 个候选时直接当作不认识（多半是原 App 自己的快捷键）。

**去掉了鼠标悬停改高亮。** 一开始跟着原生候选窗做了 hover 选中，
但浮层就贴在光标边上，鼠标恰好停在上面会**静默改变 Enter 会提交哪个语言**。
PRD §2.2「可预测」优先，改成只有 ↑↓ / ⌘数字 / 点击能改选中。

**缺语言包时不自动下载。** 首次遇到只显示「需要下载语言包 · 点击下载」，
用户点了才走 `prepareTranslation()`——否则会在用户打字打到一半时弹出系统下载对话框。

**终端类应用有两道闸。** 默认黑名单是第一道；
`InputAssistAppCompatibility` 的能力等级是第二道，
用户把黑名单清空也不会让自动触发去改写命令行（PRD §53）。

**`@AppStorage` 没有用在 `InputAssistSettings` 里。** 那个属性包装器是给 View 用的，
放进 `ObservableObject` 虽然能读能写，却不会触发 `objectWillChange`，
设置页上的开关点了不会立刻重绘。改成直接读写 `UserDefaults` + 每个 setter 显式发通知，
并补了一条测试钉住这个行为。

---

### 故意没做的事

| 没做 | 原因 |
|---|---|
| **把上下文送进翻译引擎** | PRD §9.1 希望引擎拿上下文理解、只替换目标句。但现有 `TranslationEngine` 是「一段文本进、一段译文出」，把上下文一起送进去，引擎会连上下文一起翻译，我们没有可靠办法从译文里切出对应那一句——切错就是替换错内容，踩 §44 红线。所以上下文照 §9.2 算好并冻进 `CandidateSession`，但这一版不喂给引擎。要真正做到需要一个 context-aware 的 prompt 接口，只有 OpenAI 引擎支持，跟 §17「不新增 Provider」冲突，留作单独决策 |
| **AXObserver 挂不上时的轮询兜底** | 有些 App 不发 `kAXValueChanged`。轮询能提高覆盖率，但要在全系统范围内定时读别人的 AX 树，代价和风险都不小。PRD §30 明确要求 best-effort：能力不足就退化成手动快捷键，不为覆盖率引入危险逻辑 |
| **语言级 Provider 配置** | PRD §17.1 第一版明确不做 |
| **双语插入（中文 + 译文）** | PRD §27.2 明确不做 |
| **CHANGELOG / README 的功能条目** | 等真机验证过一轮再写给用户看 |

---

### 怎么验证的

```
xcodebuild -project 大佐翻译官v1.xcodeproj -scheme dazuofanyiguan \
  -configuration Debug CODE_SIGNING_ALLOWED=NO test
```

`Test run with 168 tests in 2 suites passed`（改动前 57 条，本轮新增 111 条）。
UI 测试目标照例因为 runner 未签名而失败，与本次改动无关。

新增测试覆盖 PRD §54 点名的部分：SentenceBoundaryDetector、TriggerEngine、
TargetLanguageFilter、CacheKey、LRU、CandidateState、CandidateNavigation、
CandidateSessionValidation、AppFilter、SecureInputGuard，外加浮层定位、布局、
自动触发判定和应用兼容等级。

另外把 Debug 版直接跑起来做了启动冒烟（含首次推荐弹窗），10 秒稳定、无输出、无崩溃。

**没有覆盖到的：** AX 取词 / 事件 tap / 实际替换这三条路径需要辅助功能权限，
给 ad-hoc 签名的 Debug 版授权是系统安全设置，只能由使用者本人完成。
逻辑层单测和编译都过了，但「在 WhatsApp 里真的替换对了吗」必须开权限后用测试页实测。

几条值得单独提的用例：

- `commandDigitsMapToTheRightCandidateIncludingTheSwapped5And6` —— ANSI 键盘上
  5 是 keyCode 23、6 是 22，**不是连号**。
- `substringRefusesToSplitASurrogatePair` —— AX 的 CFRange 按 UTF-16 计数，
  emoji 占两个单元。切在中间必须返回 nil 而不是一个「差不多」的结果。
- `pinyinStillBeingComposedNeverTriggers` / `sameAsciiTextIsJustEnglishWhenNoCJKInputSourceIsActive`
  —— 同样是 `"hello there"`，中文输入法下是组字缓冲、英文输入法下就是普通英文，
  两种情况的结论必须不同。
- `autoTriggerNeverTouchesTextTypedBeforeThisRound` —— 直接把 PRD §9.1 的例子写成了断言。
- `commasNeverCutTheTranslationRange` —— 顺带澄清一件事：§9.1 里「只替换新增那一句」
  讲的是**自动触发的 diff**，不是句界。手动触发下
  `Hello John，关于…，我们可以提供16吨船吊` 整句都会进翻译范围，这是对的。
- `decimalPointIsNotASentenceBoundary` —— 半角句点夹在数字中间是小数点，不是句界。

---

### 踩的坑

**Swift Testing 的 `#expect` 会把子表达式拆进闭包。** 直接写
`#expect(index.value(for: "a") == "1")` 报
`Cannot use mutating member on immutable value: '$0' is immutable`，
因为 `value(for:)` 是 mutating（要更新 LRU 访问序号）。得先取到局部常量再断言。
同理，`#expect(x, someString)` 的第二个参数是 `Comment` 不是 `String`，
要写成字符串插值。

**`#expect(cgFloat == 900 - 200 - 18)` 会失败。** 报错写着
`(cocoa.origin.y → 682.0) == (900 - 200 - 18 → 682)`——值明明对得上，
但整数字面量表达式在宏展开里被独立推导成了整型，走了异构比较。
断言里的期望值要显式标 `CGFloat`。

**LRU 的字节统计不能每次插入都重算。** 第一版 `totalByteCount` 是
`entries.values.reduce`，被 `evictIfNeeded` 的 guard 每次插入都调一遍，
等于把插入变成 O(n)——正是上一条日志里那个二次复杂度的翻版。
改成维护一个运行总量，只有真正超限时才排序淘汰。

**Swift 的范围字面量不能把 `...` 换行放在行首。**
`in: Double(a)\n    ...Double(b)` 直接报 `expected ',' separator`，
得提到一个常量里写成一行。

**AXObserver 换 App 时要一起清掉 `focusedElement`。**
第一版 `detachObserver()` 只摘通知不清指针，
下一次 `updateFocusedElement` 会拿**新** observer 去摘一个属于**旧** App 的元素的通知。
返回错误码，无害，但属于自己给自己埋雷。

---

### Codex review 第一轮：四条全部属实（已修）

**1（P1）授权之后功能不会自己活过来。**
在没有辅助功能权限时打开开关，`applyEnabledState()` 走到权限那道 guard 就提前返回了。
用户接着去系统设置授权、切回 App——**此后再没有任何人调过 `applyEnabledState()`**。
于是卡在一个很别扭的状态：开关显示「已开启」、权限也给了，但快捷键和 AXObserver 一个都没注册上，
只能把开关关了再开，或者重启 App。

修法：协调器自己监听 `NSApplication.didBecomeActiveNotification`。
只在「开着 + 有权限 + 但没跑起来」这一种情况下重新装配，
不能每次切回 App 都把常驻的 AXObserver 和快捷键拆了重建。

顺带把 `AXIsProcessTrusted()` 做成可注入，这条路径才能写成单测
（`enablingWithoutPermissionRecoversOncePermissionArrives`）。
断言故意挑的是「不再提示缺权限」而不是「快捷键注册成功」——
⌥Space 可能被别的 App 占着，那样断言会假失败。

**2（P2）测试页收不到自动触发。**
`InputAssistFocusObserver` 原本一律不监听自己这个进程（避免主窗口原文框、
设置页黑名单编辑器里的输入也被当成待翻译内容）。但 PRD §48 要求测试页能就地验证自动触发，
而测试窗口的 bundle id 恰好就是我们自己。

修法：开一个窄口子——测试页用 `WindowAccessor` 把自己的窗口登记给协调器，
只有**它正好是当前 key window** 时才放行监听本进程。
光看「测试窗口开着」不够：用户可能同时开着设置页在编辑黑名单。

还得补一条：在自己 App 内部换窗口不会触发 `didActivateApplication`，
但「现在 key 的是不是测试窗口」恰恰会因此改变，所以又挂了 `NSWindow.didBecomeKeyNotification`。

**3（P2）粘贴兜底会把用户中途复制的内容覆盖掉。**
等粘贴落地的那 250ms 里，用户（或剪贴板管理器）完全可能写入新内容，
而旧实现无条件把快照还原回去——替换成功了、剪贴板却被毁了。

修法：放译文进剪贴板之后记下 `changeCount`，还原前比一比，
只有「还是我们放的那份」才还原。读取不改变 `changeCount`，所以目标 App 的粘贴不会误伤这个判断。

**4（P2）单条重试的任务不受取消控制。**
`retryCandidate` 里写的是 `_ = candidateService.retry(...)`，返回的 Task 直接丢了。
浮层关掉之后 `handleDismiss` 只 cancel 得到 `translationTask`，
那条重试还在继续发请求——OpenAI 那边是要计费的。
（UI 倒是不会错乱，回调里有 sessionID 校验。）

修法：把重试任务收进 `retryTasks`，和主任务一起在
`cancelInFlightTranslations()` 里取消；commit、换 session、deactivate 三条路径都走它。

### 这一轮自己踩的坑

**测试里 `NSWindow.close()` 会把测试进程带崩。**
程序化创建的 `NSWindow` 默认 `isReleasedWhenClosed = true`，
`close()` 在 ARC 还持有引用时就把它释放了。

更值得记的是**它伪装成了「通过」**：xcodebuild 打印
`Restarting after unexpected exit, crash, or test timeout`，
重启后只跑剩下的用例，最后照样输出
`Test run with 63 tests in 2 suites passed`——数字从 129 掉到 63，
但那一行写的是 passed。**只看最后那行 `passed` 会漏掉整整一半用例没跑。**
以后除了 grep `Test run with`，还要一起 grep `Restarting after` 和 `Failing tests`。

修完：`Test run with 133 tests in 2 suites passed`，无重启、无失败用例。


---

### Codex review 第二轮：又四条，同样全部属实（已修）

第一轮修完之后 Codex 换了个角度又扫出四条。逐条复现，全中。

**5（P1）替换前没校验「还是不是同一个输入框」。**
`validate()` 只检查了「有焦点元素」和「前台 App 没变」，
但**从头到尾没有和 `session.element` 比过**——读值和写值用的都是**当前**焦点元素。
于是：同一个 App 里 Tab 到另一个输入框，只要那里恰好有同样的文字
（搜索框和输入框都写着「好的」并不罕见），校验就会通过，译文被写进完全错误的控件。

`CandidateSession` 明明存了 `element`，只是没人用。修法就是 `CFEqual` 比一下，
不同就 abort，新增 `.focusedElementChanged` 这个原因。

**6（P1）验证不了的 AX 写入之后又补了一次粘贴，会把译文插两遍。**
这条最该记：**我上一轮的注释把错误的推理写得理直气壮。**

原话是「读不到 kAXValue 时无法证伪，保守当作没生效，转去走粘贴兜底：
多贴一次的代价，远小于用户以为翻译了、实际什么都没发生。」

问题在于「多贴一次」根本不是小代价。如果 AX 写其实**成功了**，
此刻选区已经塌缩到插入点之后，再补一次 ⌘V 就是把译文插进去**第二遍**，
直接把用户的文字弄坏——这正是 PRD §28 那条红线要防的事。

能走到这个状态：控件 settable `AXSelectedText` 但不暴露 `AXValue`
（部分富文本控件就是这样），手动选中一段文字触发即可。

修法把顺序倒过来：**只有在能验证结果的前提下才尝试 AX 直写。**
读不到 `kAXValue` 就干脆不写，直接粘贴——那时选区还原封不动地圈着原文，
粘贴正好替换掉它，只会发生一次插入。四种情况（调用失败 / 成功且生效 /
成功但被忽略 / 无法验证）现在都只会插入一次。

**7（P2）浮层会随着译文陆续返回一跳一跳地长高。**
loading 行占 1 行、译文行占 2 行（高亮行 4 行），
而 `updateRow` 每次都重算尺寸并重新定位。三个语言就跳三次。

我上一轮把 PRD §12.2 读成了「行数不能变」，但原文写的是
**「不得随着每个翻译返回而不断增加 Panel 高度」**——说的就是高度。我读松了。

修法：行高在浮层出现时从**原文**估算一次就定死，之后与译文无关。
原文在弹出那一刻就已经知道了，译文才是陆续到的。
估算带一个膨胀系数——中文译成拉丁语系字符数会明显变多
（「我们可以提供16吨船吊」11 个字 → 37 个字符），不算进去照样要长高。
超出预留高度的译文按 §13.1 用 `…` 截断。

现在只有**用户操作**才会改尺寸：移动高亮、按住 ⌥ 看调试信息。
`updateRow` 干脆不再碰尺寸。

**8（P2）手动触发快捷键改不了。**
设置页只是把 `settings.shortcut.displayString` 当文本显示，
仓库里没有任何地方写过 `settings.shortcut`。⌥Space 又特别容易被占
（Alfred、部分输入法切换器都在抢），一旦注册失败，手动触发就彻底没救了。

修法跟着仓库既有的截图快捷键写法走，用预设组合的 Picker
（⌥Space / ⌃Space / ⌥⇧Space / ⌥T / ⌥G / ⌘⌥J），改完立刻重新注册，
注册失败时在下面直接提示「换一个」。没上自定义录制器——
和现有 UI 风格一致、代码少得多，而这条意见要解决的是「完全没有退路」。

### 这一轮的教训

两条 P1 都源自**我自己写下的、听起来很有道理的注释**：
一条说「多贴一次代价小」，一条说「行数定死了就不会跳」。
注释写得越笃定，越没人（包括我自己）回头去验证它成不成立。


---

### Codex review 第三轮：三条新的，全部属实（已修）

前两轮的六条这一轮不再提（线程被重新锚到新提交上而已，代码里都在）。新提的三条：

**9（P1）选区沉降那 40ms 之后没有重新校验。**
`.replaceRange` 这条路是：校验 → `selectRange` → `sleep(40ms)` → 写。
校验发生在**睡之前**，睡醒直接写。这中间用户完全可能点到别的输入框或者 ⌘Tab 换 App。

粘贴兜底尤其危险：合成的 ⌘V 是发给**此刻**的前台 App 的——
睡醒时前台已经换人了的话，译文就直接贴进了另一个应用。

修法：把「读状态 + 跑校验」抽成 `evaluate(session:)`，睡醒后再跑一遍，
并且额外确认 `CFEqual` 前后是同一个元素，任何一项对不上就 abort。

**10（P1）光标往前移动不会作废待触发的那一轮。**
`handleSelectionChanged` 只在 `caret < startCaretUTF16Offset` 时重置。
于是：在已有文字中间打完中文、debounce 还没到点时按一下 →，
锚点还停在原处、待触发任务也还在，等 timer 到点算出来的 source range
会从锚点一路延伸到当前光标，**把用户原本就有的文字圈进去**。
接受那个候选等于替换掉这轮根本没输入过的内容。

具体到数字：已有 `前面内容XXXX`，在第 4 位插入「你好」，再按一下 →，
range 就从「你好」变成了「你好X」。

不能简单地「一见光标前进就重置」——**打字本身就会让光标前进**。
区分办法是看全文长度：伴随编辑的移动会改变长度，纯导航不会。
抽成了 `InputAssistCaretMovement.classify`（纯函数，可单测），
`followsEdit` 只更新追踪值，`navigation` 才重置锚点。

**11（P2）替换进行中还能再按手动快捷键。**
自动触发那条路查了 `isReplacing`，手动这条没查。
在选区沉降或 250ms 粘贴沉降的 await 期间按快捷键，会建出第二个 session 和浮层，
而旧的替换任务收尾时**无条件**清空 `currentSession` / `currentRequest`，
新浮层就变成一个既收不到结果、也提交不了的空壳。

两头都补上了：手动触发时拒绝（并记一条日志），
同时替换任务收尾改成只清理**自己这一轮**的 session（比对 sessionID）。

修完：`Test run with 142 tests in 2 suites passed`，无重启、无失败用例。


---

### Codex review 第四轮：两条新的，仍然属实（已修）

**12（P1）沉降之后只查了「原文还在不在」，没查「现在选中的是不是它」。**
第三轮加的复验解决了「换 App / 换控件」，但漏了**同一个控件内部**的选区变化：
用户在那 40ms 里点了一下，把我们刚设好的选区挪走或者收成了插入点。

此时文本本身没被改过，所以 `validate()` 照样给出 `.replaceRange`——
它只能证明「原文还在原来的位置」，**证明不了「现在选中的正是它」**。
接着 `writeSelection` 改的就是新选区，粘贴则落在新光标处。

这条不变式没法靠 `validate()` 拿到（它不接收当前选区），
所以在引擎里单独查一次：设好的 range 和此刻 `selectedRange` 必须一致，
否则 abort，新增 `.selectionChanged`。

**13（P2）⌘ 组合数字没有要求 ⌘ 是唯一修饰键。**
旧实现只判断 `hasCommand`，于是 **⌘⇧3 / ⌘⇧4 / ⌘⇧5 这三个系统截图快捷键**
会被当成 ⌘3 / ⌘4 / ⌘5 吞掉——用户想截个图，结果截不成，
还顺手把对应语言的译文替换了进去。⌘C 同理（⌘⇧C 在不少 App 里是别的功能）。

修法：数字键和 C 都要求 `!hasShift && !hasOption && !hasControl`，
否则落到「无关的 ⌘ 组合」那条分支——关浮层、但绝不拦截。

修完：`Test run with 146 tests in 2 suites passed`，无重启、无失败用例。

### 四轮下来的总结

Codex 一共提了 13 条，**13 条全部属实，零误报**。分布很有意思：

| 轮次 | 性质 |
|---|---|
| 第一轮 | 状态没有回到该有的样子（授权后不复活、任务没取消、剪贴板被覆盖） |
| 第二轮 | **我自己写的注释里的错误推理**（「多贴一次代价小」「行数定死就不会跳」） |
| 第三轮 | 异步空隙（sleep 前后、debounce 期间） |
| 第四轮 | 不变式的**边界**（复验查了 A 没查 B；修饰键判断只查了「有」没查「只有」） |

后两轮的共同点是：**我每次都只修到了被指出的那一层，没有把同一类问题问到底。**
第三轮补了「睡醒后重新校验」，但没问「校验够不够全」；
`hasCommand` 写下去的时候，也没问过「⌘ 是不是唯一的修饰键」。


---

### Codex review 第五轮：两条新的，还是属实（已修）

**14（P1）译文和原文相同时，成功的写入被判成了失败。**
`didWriteTakeEffect` 的判据是「写之后的全文和写之前不同」。
可是译文完全等于原文的情况一点都不罕见——产品名、型号、
引擎对专有名词原样返回（OpenAI、SQ16、OK）。

这时 AX 写入**成功**了，但全文一个字符都没变，判断只能报失败，
于是又补一次粘贴——而此时选区已经塌缩，同样的文字被插进去第二遍。
**又是「验证不了 → 补一次粘贴 → 插两遍」这条老路**，只是这次的成因不是读不到
`kAXValue`，而是判据本身在这种输入下失效了。

修法：在写之前就短路——译文和原文一样，目标位置本来就已经是想要的内容，
**什么都不做**，返回新的 `.alreadyMatching`。
顺带也让 `didWriteTakeEffect` 的判据重新成立：
进到那里时译文必定不等于原文，那么写成功就一定会让全文改变。

**15（P1）没有修饰键的那条路同样忽略了 ⇧ 和 ⌥。**
第四轮修的是 ⌘ 组合，但普通路径上还有同样的问题，而且更要命：

**⇧Enter 在聊天软件里是「换行但不发送」。** 当成普通 Enter 处理，
用户只是想另起一行，结果文字被替换掉了。
⌥↑/⌥↓（按段落移动）、⇧↑/⇧↓（扩展选区）同理，那是宿主 App 的编辑操作。

修法：候选操作只认**完全没有修饰键**的按键，带修饰键的一律关浮层并放行。

修完：`Test run with 150 tests in 2 suites passed`，无重启、无失败用例。

### 第五轮暴露出来的模式

这两条都不是新问题，是**同一个问题的第二个入口**：

- 14 和第二轮的 6 是同一条路（验证不了 → 补粘贴 → 插两遍），只是触发条件不同；
- 15 和第四轮的 13 是同一条规则（修饰键要判断「只有」而不是「有」），只是分支不同。

也就是说前两次我都**只修了报告里点名的那个入口，没有回头把同一类问题在别处的实例找一遍**。
下次修完一条应该先问：这个判据在什么输入下会失效？这条规则还有别的分支吗？

### 顺着这个教训自己扫了一遍（不是 review 提的）

**修饰键那一类：** 全仓库检索 `hasCommand` / `hasShift` / `hasOption` / `hasControl`，
剩下的只有 `showsDebugOverlay`，它本来就写的是「⌥ 且没有别的」。这一类清干净了。

**「⌘V 发给谁」那一类：** 发现 `pasteReplace` 自己不检查前台 App，
完全依赖调用方在几步之前校验过。窗口只有微秒级，实际很难踩到，
但**合成 ⌘V 是整个功能里最危险的一个动作**——它发给的是此刻的前台 App，
不是我们以为的那个。把这条不变式做成自守（进函数先比对 bundle id），
而不是指望每条调用路径都记得先查一遍。


---

### Codex review 第六 / 七轮：两条新的（已修），外加一次我自己的严重失误

**先说失误：我差点把「审核已通过」当成结论报出去。**

查 PR 评论用的是 `gh api .../pulls/4/comments`，**没有加 `--paginate`**。
GitHub 默认一页 30 条，而当时总共 32 条——最新的两条 Codex 意见正好在第二页，
我一条都没看到，于是得出「连续两轮零意见、可以合并」的结论并这么汇报了。
是用户说「他的评论我都看到了，你没看到吗」才发现的。

`gh api` 分页这件事本身是常识，但**危险的是它安静地成功**：
命令退出码 0、输出格式正常、只是少了几条。和前面那个
「测试崩溃后重启仍然打印 passed」是同一种形态——
**看起来正常的成功输出，其实是不完整的**。

以后凡是用 `gh api` 列表接口，一律加 `--paginate`；
更一般地，凡是「数量」类结论，先确认自己看到的是不是全集。

---

**16（P1）`selectRange` 会先把用户的光标覆盖掉，之后再校验就晚了。**

第四轮加的「写之前确认选区」是放在 `selectRange` **之后**的，
所以它验的只是「我刚设的那个选区还在不在」——恒为真。
用户在按 Enter 之前其实已经把光标挪走了这件事，那道检查永远发现不了。

光靠「浮层会在光标移动时自动收起」也堵不住：
有些 App 根本不发 `AXSelectedTextChanged`；**关掉自动触发时压根没有人在监听选区**；
点击关闭浮层还是异步的，和紧接着的 Enter 之间存在竞态。

修法：取词时把当时的选区记进 `CandidateSession.selectedRangeAtCapture`，
在 `selectRange` **之前**和当前选区比对，不一致就 abort。

**17（P1）前台 App 的检查放早了，中间隔着一次剪贴板深拷贝。**

这条最值得记——**它挑的正是我上一轮"主动加固"的那段代码**。
我把 `pasteReplace` 改成自守时，检查放在了函数开头，
但那之后还要 `InputAssistPasteboardSnapshot.snapshot` 把剪贴板里
每个 item 的每种类型都深拷贝一遍。用户剪贴板里躺着一张大图或 PDF 时，这是要花时间的，
那段时间足够切走一个 App，⌘V 就贴到别人那里去了。

修法：再查一次，而且必须是**紧贴着 `press` 的最后一件事**；
这时如果目标变了，还要先把剪贴板快照还原回去再 abort。

教训：加一道校验时，「放在哪一行」和「加不加」同等重要。
自守要守到真正危险的那一刻，不是守在函数入口。

修完：`Test run with 153 tests in 2 suites passed`，无重启、无失败用例。


---

### Codex review 第八轮：两条修了，一条**明确不改**

**18（P1）粘贴前只比 bundle id，同一个 App 里换输入框查不出来。**
上一轮补的「紧贴 press 再查一次」方向对了，但判据太弱：
**同一个 App 里点到另一个输入框，bundle id 是不变的。**
而替换期间自动监听是暂停的，前面那些元素 / 选区校验都覆盖不到「深拷贝」这段间隔。
改成把焦点元素（`CFEqual`）和选区一起重新确认。

这是同一处代码连着第三轮被挑：先是根本没查 → 查了但放在函数入口 → 查了但只比 bundle id。
每一轮我都只补到「被指出的那一点」为止。

**19（P2）深拷贝期间剪贴板被别人改了，会被 `clearContents` 毁掉。**
`ourChangeCount` 那道保护是**销毁之后**才记的，救不了这一段。
改成快照**之前**先记一次 `changeCount`，快照完对不上就直接放弃这次粘贴。

**20（P2）上下文没送进翻译请求——属实，但这一版不改。**

Codex 说得对：`capture.context` 按 PRD §9.2 算好了、冻进了 `CandidateSession`，
但 `CandidateService` 和任何 Provider 都没有消费它，
所以 §9.1 描述的上下文感知行为目前是缺失的。

不改的理由是这件事做不到「安全」而不是「麻烦」：
现有 `TranslationEngine` 是「一段文本进、一段译文出」。把上下文一起送进去，
引擎会把整段上下文都翻译出来，而我们**没有可靠办法从译文里切出对应目标句的那一段**——
切错就是替换错内容，正好踩 PRD §44 的红线。宁可不翻译，也不能替换错。

真正做对需要一个 context-aware 的 prompt 接口（「用周围文本理解，但只翻译标记的那一段」），
而这只有 OpenAI 引擎能支持，Apple / Google 做不到；
这同时和 §17「不新增 Provider 能力」、§33「Provider 只维护一套」冲突。
所以它是一个**产品决策**，不是一个可以顺手补上的实现缺口，已经交给用户定。

修完：`Test run with 155 tests in 2 suites passed`，无重启、无失败用例。


---

### Codex review 第九轮：两条，还是同一块代码

**21（P1）拿不到 range 的输入面，选区校验被整条跳过了。**

上一轮加的判据写成：

```swift
expectedSelectedRange == nil || selectedRange(element) == expectedSelectedRange
```

`nil` 时整个条件恒真。而**拿不到 `AXSelectedTextRange` 的输入面，恰恰就是走
`.replaceSelection` 粘贴兜底的那一批**——等于在最需要校验的地方跳过了校验。

「用 `|| x == nil` 给一个可选值兜底」这个写法太顺手了，顺手到我没停下来问一句：
**nil 在这里到底意味着什么？** 它意味着「这个面验不了 range」，
而不是「这个面不需要验」。

修法：range 拿不到时退一步比选中的**文本**（和 `session.sourceText` 比）；
两样都验不了就不放行。

**22（P2）abort 路径的剪贴板还原是无条件的。**
press 失败和沉降路径早就用的是 `restoreIfUntouched`，
新加的那几道 abort 分支却直接 `restore`，会把用户这期间复制的新内容覆盖掉。
同一个函数里两种写法并存，纯属加代码时没回头看隔壁几行。

顺带把那道复合 guard 拆成三条独立的，
abort 原因分别是 `.applicationChanged` / `.focusedElementChanged` / `.selectionChanged`，
日志和提示能说清到底是哪一环拦下的。

修完：`Test run with 157 tests in 2 suites passed`，无重启、无失败用例。

### 关于「同一块代码被挑了五轮」

`InputAssistTextReplaceEngine` 从第二轮到第九轮，每一轮都在被挑，累计 11 条。
不是同一个 bug 反复没修对，而是**同一段逻辑里一层套一层的前置条件**：

```
写之前要校验                     ← 第二轮
  校验要在 sleep 之后再来一次     ← 第三轮
    还要确认选区没被挪走          ← 第四轮
      比对要在 selectRange 之前   ← 第六轮
        ⌘V 之前还要再确认一次     ← 第七轮
          确认要紧贴 press        ← 第八轮
            光比 bundle id 不够   ← 第八轮
              range 为 nil 时也得验 ← 第九轮
```

每一条都是真的，每一条也都只有在上一条修好之后才暴露得出来。
教训不是「应该一次想全」——而是**这类"合成键盘事件 + 跨进程写文本"的代码，
本质上是在和用户的实时操作赛跑，任何一个 await、任何一次可能变慢的调用，
前后都得当成两个不同的世界**。


---

### Codex review 第十轮：两条

**23（P1）AX 直写之前也得重新确认。**
前面几轮把复验加在了 `sleep` 之后和 ⌘V 之前，唯独漏了 AX 写这条路。
而它前面横着一个 `isSettable`——**跨进程 AX 查询，目标 App 忙的时候默认超时能到数秒**。
这期间用户挪光标、换输入框、切 App 都来得及，之前那次校验早就过期了。

我一直把「可能变慢的地方」等同于「显式的 `await`」，
但同步的跨进程调用一样慢，而且因为它长得像普通函数调用，更容易被忽略。

顺手把三处校验合并成 `invalidTargetReason(element:expectedSelectedRange:expectedSelectedText:expectedBundleIdentifier:)`，
返回 nil 表示可以往下走，否则给出该报的 abort 原因。
现在 AX 写前和 ⌘V 前用的是同一份判定，不会再出现「这边补了那边没补」。

**24（P2）不带协议头的域名识别不出来。**
`example.com/产品` 既没有 `http://` 前缀也不含 `://`，
旧的 `looksLikeURL` 判不出它是 URL。而它路径里带中文，
自动触发会当成「新增了中文」——于是在一条 URL 中间弹候选、甚至替换掉其中一段。

新增 `looksLikeBareDomain`：顶级域必须是 2 个以上 ASCII 字母
（这样 `12.5` 这类小数不会中招），其余各段是 ASCII 字母数字或连字符
（于是「我们提供16吨船吊.价格面议」也不会被误判成域名）。

修完：`Test run with 160 tests in 2 suites passed`，无重启、无失败用例。


---

### Codex review 第十一轮（换了一位审核者）：四条

原来那个 Codex 用量到上限了，换成用仓库所有者账号跑的另一位。
**顺带暴露了我自己的一个查询习惯问题**：前十轮我一直按
`select(.user.login=="chatgpt-codex-connector[bot]")` 过滤评论，
换人之后新意见全部被这个过滤器挡掉，又是「看起来查过了，其实没查全」。
和之前漏 `--paginate` 是同一种错——**过滤条件写得太窄，把要找的东西筛掉了**。

**25（P1）AX 写返回成功、但读不回结果时，不能继续走粘贴。**

`verifyWrite` 的旧版本把「读 `kAXValue` 失败」和「读到了但没变」混成了同一个返回值 false。
可这两件事结论完全相反：后者说明文字确实没动过、粘贴安全；
前者只说明**验不了**——目标 App 忙、AX 超时、瞬时错误都会这样，
而这时写很可能已经生效、选区已经塌缩，再补一次粘贴就是把译文插进去第二遍。

这已经是同一个「插两遍」坑的**第三种成因**（前两次是读不到 `kAXValue`、译文等于原文）。
每次我都以为堵住了，其实只堵住了当时那一种。

修法：`WriteVerification` 拆成 `.applied` / `.didNotApply` / `.unverifiable` 三态，
`.unverifiable` 直接中止（新增 `.writeVerificationUnavailable`）。
读失败时先重试一次（隔 30ms），免得把本来好好的替换误判成验不了而白白放弃。

**26（P1）⌘V 之前还要复验剪贴板没被人改。**
记下 `ourChangeCount` 之后、按键之前还要跑好几次跨进程 AX 查询，
那段时间剪贴板管理器完全可能再写一次。不查这一下，
⌘V 粘的就是**别人刚写进来的东西**。这时也不能还原——对方的内容比我们的快照新。

**27（P2）裸域名只按 `/` 截主机名不够。**
`example.com?关键词`、`example.com#说明`、`example.com:8080/产品`
的顶级域会分别变成 `com?关键词` / `com#说明` / `com:8080`，
ASCII 字母校验失败，于是又漏回自动翻译那条路上。
改成截到 `/`、`?`、`#` 任意一个为止，再去掉端口。

**28（P2）我把不属于本 PR 的签名配置改动一起提交了。**

`project.pbxproj` 里主 App 的 Debug / Release / TestHost 三处
`DEVELOPMENT_TEAM` 从 `S336CKXSUQ` 被清成了空，混在 `694c00d` 里进来的。
**这不是我有意改的**——我全程没开过 Xcode，也没编辑过这个文件。

原因是我每次提交都用 `git add -A`，把工作区里**任何**变化一股脑收进来，
包括别的进程（多半是 Xcode 自己）在后台改的。已经整份恢复成 `origin/main` 的版本；
新文件靠 `PBXFileSystemSynchronizedRootGroup` 自动纳入，本来就不需要动 pbxproj。

后果不是小事：签名身份变了，本地 Debug 会退化成 ad-hoc「Sign to Run Locally」，
而**这个功能恰恰依赖辅助功能授权**——签名一变，权限授权和正式归档的行为都会偏离基线。

教训：`git add -A` 是"把我看到的所有东西都提交"，不是"提交我改的东西"。
提交前应该看一眼 `git status`，尤其是这种自己压根没碰过的文件。

修完：`Test run with 163 tests in 2 suites passed`，无重启、无失败用例。

---

### Codex review 第十二轮：两条，第二条打在要害上

**29（P1）「全文变了」不能当成写入成功的判据。**

`valueAfterWrite != valueBeforeWrite` 只能证明控件**发生过某种变化**，
证明不了这次写把**完整译文落在了预期范围**。

更糟的是这个判据和我上一轮加的 30ms 重试**互相放大**：
读回失败后要等 30ms 再读一次，这期间用户输入、宿主自动更正、异步编辑，
任何一样让全文变了，弱判据就会误报成 `.applied`——
哪怕 AX 写其实被忽略、只写进去一半、或者译文落错了位置。

修法：调用方本来就有写前全文、source range 和译文，
直接拼出「替换后应该长什么样」（新增纯函数 `InputAssistAXTextCapture.replacingRange`），
读回来的全文必须和它**逐字相同**才算 `.applied`；
和写之前一模一样是 `.didNotApply`（可以退到粘贴）；
**第三种样子一律 `.unverifiable`，中止。**

拿不到 source range 的那条路（`.replaceSelection`）算不出精确期望值，
于是干脆不走 AX 直写、直接粘贴——那时选区还圈着原文，只会插入一次。

**30（P2）我那两条"回归测试"其实什么都没测住。**

这条最该记。我给上一轮两个 P1 写的测试是：

- 一条断言 `AbortReason.writeVerificationUnavailable.rawValue == "writeVerificationUnavailable"`；
- 一条断言 `NSPasteboard.changeCount` 改了之后确实不一样。

**把三态分支删掉、把 `.unverifiable` 重新接回粘贴兜底，这两条照样全绿。**
第二条更离谱——它验的是 AppKit 自己的行为，一行生产代码都没调用。
也就是说「163 全绿」对本轮真正修的行为没有任何防护，是纯粹的数字好看。

修法：把判定抽成生产代码真正在用的纯函数，测试直接调它们——

- `WriteVerification.classify(valueAfterWrite:valueBeforeWrite:expectedValueAfterWrite:)`
- `WriteVerification.allowsPasteFallback`（**只有 `.didNotApply` 为真**）
- `shouldPostPaste(currentChangeCount:expectedChangeCount:)`

现在断言的是「读回 nil → 不许粘贴」「全文变成第三种样子 → 不许粘贴」
「changeCount 对不上 → 不许发按键」。谁把这些分支改回去，测试立刻红。

**教训：写回归测试时要先问一句「把我刚修的代码删掉，这条测试会不会失败？」**
如果不会，那它就不是回归测试，只是一条让计数变好看的断言。
我前面十几轮写了上百条测试，这个问题很可能不止这两处——
但这次至少把最关键的两条 P1 补上了真凭据。

修完：`Test run with 166 tests in 2 suites passed`，无重启、无失败用例。


---

### Codex review 第十三轮：两条，都在同一个「插两遍」上再深一层

**31（P1）不能拿 `AXError` 当"到底写没写"的判据。**
旧写法是 `if error == .success { 验证 } else { 直接粘贴 }`。
但非成功里包含 `.cannotComplete`——**跨进程消息没能完成，说明我们没拿到可靠回复，
不等于目标进程没收到、更不等于它没写。** 遇上「写已生效但回复超时」，
直接粘贴就是第二遍。

修法：返回码只当参考，不当判据。**无论返回什么都以实际读回的状态为准。**

**32（P1）第一次读到 before 就判"没生效"，同样会插两遍。**
有的 App 是异步落地的：写下去之后立刻读，读到的还是写之前的样子（stale-before）。
旧的 `verifyWrite` 只要读到非 nil 就立刻返回，于是判 `.didNotApply` 并马上补 ⌘V，
等原来那次 AX 写随后落地，译文就出现了两遍。

我上一轮加的 30ms 重试**只覆盖了 nil，没覆盖这个更危险的情况**。

修法：`.didNotApply` 也必须沉降后复验。抽出纯函数 `WriteVerification.resolve(first:second:)`：

| 第一次 | 第二次 | 结论 |
|---|---|---|
| `.applied` | — | 成功（短路，不再等） |
| `.didNotApply` | `.applied` | 成功（异步落地） |
| `.didNotApply` | `.didNotApply` | 确实没生效 → 允许粘贴 |
| 其余任意组合 | | 中止 |

**只有连续两次都精确读到「和写之前一模一样」才敢粘贴**，
中间夹一次读失败或第三种样子都不行。

### 「插两遍」这一条路，到此一共堵了五次

| 轮次 | 成因 |
|---|---|
| 二 | 读不到 `kAXValue` → 写之前就验不了 |
| 五 | 译文等于原文 → 判据在这种输入下失效 |
| 十一 | 写之后读回失败 → 被归进了「内容没动过」 |
| 十三 | `AXError` 非成功 → 被当成「肯定没写」 |
| 十三 | 第一次读到 stale-before → 被当成「没生效」 |

五次的形状完全一样：**「我判断不出写有没有生效」被当成了「写没有生效」。**
每次我只修了当时那一种成因，没有把这个命题本身立起来。
正确的不变式其实一句话就能说清：

> 除非能**证明**这次写没有生效，否则绝不允许执行第二种写入方式。

修完：`Test run with 168 tests in 2 suites passed`，无重启、无失败用例。


---

### 结论：上下文暂不送进翻译引擎（PRD §9.1）

这条 review 意见（第八轮 P2）属实，但**这一版明确不做**，理由记在这里备查。

**想要的效果**：用户已经打了「关于你昨天问的设备，」再输入「我们可以提供16吨船吊」，
引擎应当知道前文在说"设备"，从而把"船吊"译成 marine crane 而不是 boat crane；
但只替换新输入的那一句。短句更明显：「好的」单独看是 OK，
知道上一句是「明天下午三点见?」才会译成 "Sure, see you then."
而自动触发最常命中的恰恰就是这类短句。

**为什么不能直接送**：现有 `TranslationEngine` 的形状是「一段文本进、一段译文出」。
把上下文一起送进去，引擎会把整段都译出来，我们只想要其中一句，
就得从译文里**切一刀**。中英文句子结构不是一一对应的——语序会变、从句会合并、
标点对不上——切点找不准。切错就是把残缺或错位的内容写进用户的输入框，
正好踩 §44 红线，也和 §28「宁可不翻译，也不能替换错」冲突。

**做对需要什么**：给引擎一个新能力——「这段是上下文供你理解，只翻译我标记的那一句，
只返回那一句」。靠 prompt 能做到，但**只有 OpenAI 引擎能做**；
Apple Translation 和 Google 的 API 就是纯粹的文本进文本出，没有地方塞这种指令。
做了的话同一功能在三个引擎下行为不一致，而 §17 明确第一版不新增 Provider 能力、
§33 要求 Provider 只维护一套。

**决定**：先按「每句独立翻译」合并——三个引擎行为一致、完全安全。
上下文照 §9.2 算好并冻在 `CandidateSession` 里，接口留着。
真机用一段时间后如果短句翻译确实别扭，再单开 PR 给 OpenAI 引擎加 context-aware prompt，
届时要把 context 一并纳入 cache key（review 提醒过这点，是对的）。


## 2026-08-23 · 修复流式翻译主线程的二次复杂度开销

- 分支：`perf/streaming-and-hotpath-optimizations`
- PR：[#3](https://github.com/Achordchan/bagayalu-translate/pull/3)
- 版本：1.2.4 → 1.2.5（更新日志留待后续功能一并整理）

### 背景

排查性能问题时发现：流式译文的每个 SSE delta 都会在主线程上把**整段**工作重做一遍，
开销随译文长度呈二次增长。一次约 12000 字的流式翻译，仅"解析"这一段就要空烧约 2 秒主线程，
NSTextView 写入还要再花 4 秒以上。译文越长越卡，正是二次复杂度的典型表现。

同一条路径上叠了两个独立的二次项，缺一个都修不干净。

### 改了什么

**1. `OpenAIStreamingTranslationProjector` 改为增量解析**

旧实现每个 delta 都从头重扫整段累积文本，并新建一个 `JSONDecoder` 做全量解码
（还带一个失败后回溯重试的循环）。新增 `Session` 记住扫描停下的位置，只处理新增字符，
并自行增量解码 JSON 转义——包括被切在两个 delta 中间的代理对（emoji）。

原有的一次性入口 `visibleText(from:isAutoDetect:)` 保留不动，内部复用同一套实现，
语义完全不变。

**2. 新增 `StreamingNewlineRestorer`**

替代每个 delta 对全文跑两次 `replacingOccurrences`。只处理新增字符；末尾那一小段
可能还是 `[[DAZUO_NL]]` 前半截的字符留在"待定区"，每次按一次性规则重新渲染，
保证流式中途的显示也和整段替换逐字符一致。待定区长度不超过一个标记，所以是 O(1) 摊还。

**3. 译文视图不再整段替换 NSTextView 内容**

`OutputTextView`（主窗口）和 Mini 气泡的 `ScrollContainer` 在纯追加时只把新增部分
写进 `textStorage`，避免每个 delta 都让 TextKit 重排全文。

顺带修掉一个体验问题：旧实现每个 token 都重设 `textView.string`，会把滚动位置顶回顶部
并清掉用户选区——长译文在流式期间根本没法选中文字。

**4. 新增 `AppendOnlyStreamCheck`**

用"总长度 + 结尾 16 字节指纹"做 O(1) 的「这次是不是上次的延续」判断，
供上面三处共用。流被重开时（Responses 精简参数回退、译文疑似原文回显后的重试）
自动重置状态。

**5. 其它热路径**

| 改动 | 原因 |
|---|---|
| 全局快捷键 `.defaultTap` → `.listenOnly` | 回调从不修改或吞掉事件，主动 tap 只会把本进程插进全系统键盘输入的必经之路上，卡顿时还会被系统判定超时而停用 |
| 冻结背景截图复用同一次 `SCShareableContent` 查询 | 该查询要枚举全系统窗口，旧实现每块屏各查一次，且全在选区浮层显示**之前**，直接算进快捷键响应延迟 |
| `ContentView` / `SettingsView` 改用不订阅的 `logStore` 入口 | 两者只往下传日志、从不读 `entries`，但 `@EnvironmentObject` 会订阅整个对象，于是每写一条日志都要重新求值整个视图 |
| 缓存 Info.plist 版本号 | 旧写法每次 body 求值都查一遍字典 |
| 拖拽选区不再每帧重复给 `stage` 赋同值 | `@Published` 不做相等性检查，重复赋值也会发通知 |
| 日志 `print` 只在 DEBUG 下执行 | Release 版不该有同步 stdio |

### 实测数据

`-O` 编译，模拟逐 token 流式：

| delta 数 | 译文字数 | 解析开销 旧 → 新 | NSTextView 写入 旧 → 新 |
|---|---|---|---|
| 800 | 3126 | 129.1ms → **1.5ms** | 1040ms → **48.5ms** |
| 1600 | 6263 | 506.2ms → **2.9ms** | 4425ms → **101ms** |
| 3200 | 12526 | 2021.5ms → **6.0ms** | — |

两条路径都从二次变为线性：delta 数翻倍，耗时也只翻倍。

### 踩的坑

**`String.hasPrefix` 是个陷阱。** 最初用它做「是不是上次的延续」的校验，结果整体仍然是
二次复杂度，只快了 2.6 倍。原因是 `hasPrefix` 会走 Unicode 规范化比较，不是 memcmp。
实测 3200 个 delta：

```
String.hasPrefix        : 504.9 ms
String != String        :  12.9 ms   （有二进制快速路径，可以用）
长度 + 指纹 O(1) 判定   :   1.7 ms
```

换成 O(1) 判定后提速从 2.6 倍变成 335 倍。同样的写法在两个 NSTextView 里也犯了一次，
一并改掉了。**以后在每个 delta 的路径上，一律不要用 `hasPrefix`。**

### Codex review 第一轮：候选键回退回归（已修）

**问题**：auto-detect 响应里同时出现两个字段、且驼峰字段不是字符串时——
例如 `{"translatedText":null,"translated_text":"译文"}`——流式期间不再显示译文。

旧实现是无状态的：每个 delta 都按 `translatedText` → `translated_text` 的顺序把两个键各试一遍，
谁先给出字符串值就用谁。我第一版把 Session 做成「锁定第一个找到的键」，于是驼峰键一旦不可用
就永久失败，再也退不回下划线键。

**修法**：给每个候选键配一个**互相独立**的增量解析器，Session 每次按优先级取第一个有结果的。
结构上就是旧实现那个 for 循环，只是把「重新试一遍」换成「各自往前推进新增的那几个字符」。
正常情况下第一个解析器就命中并提前返回，第二个根本不会推进，所以性能没有变化
（3200 delta 仍是 6.1ms）。

**顺带发现的更深一层**：优先级在旧实现里是**每次调用都重新裁决**的——
已经在读 `translated_text`，后到的 `translatedText` 依然会把它顶掉；
`translatedText` 结构还没读全（`:` 还没到）时，这一次调用会先用 `translated_text` 的值。
独立解析器的结构天然覆盖了这些情况，不需要额外特判。

**教训：模糊测试的输入分布决定了它能发现什么。** 第一版的 fuzzer 是字符级随机拼接，
根本生成不出「两个不同的键 + 非字符串值」这种结构，所以 4000 组全绿也没能发现问题。
补了一个**结构感知**的 fuzzer——按真实形状拼 JSON 片段（随机键、随机值类型、随机分隔空格、
随机截断），6000 组结构化 + 4000 组字符级，才真正覆盖住。以后改解析器请两种都跑。

### Codex review 第二轮：不要用近似判断代替明确信号（已修）

**问题**：`AppendOnlyStreamCheck` 用「长度不小于上次 + 上次结尾那 16 字节相同」来判断是不是纯追加。
只要替换后的文本长度不变、结尾 16 字节又恰好相同，中间不一样也会被当成追加，
于是什么都不追加，界面上留着过期内容——而且是**静默**的。

Codex 点名了两个真实的替换点，复查后确认都成立：

1. `OpenAIResponsesStreamAccumulator` 收到 `.outputText(.done)` 时是
   `finalText = done.text`，**整体替换**，服务端给的权威全文未必等于逐个 delta 拼出来的；
2. `OpenAICompatibleEngine` 的兼容性回退和原文回显重试会**复用同一个回调**开新流。

视图侧还有第三个：翻译结束时 `outputText` 被赋成最终结果，那是独立于流式投影的另一次完整
JSON 解析，和最后一帧流式文本未必逐字节相同。

**修法**：能拿到明确信号的地方就不要猜。

- 累加器改为返回 `Update(text:replacesPreviousText:)`，`.done` / `.completed` 标记为权威替换；
  传输层再把「新流的第一次回调」也标成替换。标志一路透传到投影器和还原器，
  收到就重建增量状态。
- 视图侧没有这个信号，改用精确的 `utf8.starts(with:)` 逐字节前缀比对。
  它比 `String.hasPrefix` 快得多（后者要做 Unicode 规范化），相对 TextKit 排版开销可以忽略。
- `AppendOnlyStreamCheck` 整个删掉，投影器和还原器只保留「文本变短一定不是追加」这一条 O(1) 兜底。

**代价**：视图写入路径慢了约 20%（1600 delta：101ms → 121ms），仍是旧实现的 1/35。
解析路径反而更快了（6.1ms → 4.8ms），因为省掉了每次算指纹。用这点开销换掉一类静默错误，值得。

**教训：性能优化里引入的「快速判断」，如果判断错了会静默出错，就不要用近似的。**
先找有没有明确信号可用；实在没有，就选精确但便宜的做法，并确认它相对该路径的主要开销可以忽略。

### 怎么验证的

改动集中在解析逻辑，所以对旧实现做了**逐字符流式差分测试**：把译文一个字符一个字符喂进去，
每一步的结果都和旧实现比对。

- 投影器：4000 组随机输入，逐字符全部一致
- 还原器：4000 组随机输入，逐字符全部一致
- 新增 11 个单元测试（逐字符等价、跨 delta 代理对、候选键回退、`.done` 替换语义、换键即替换、链路串测等）
- 57 个单元测试全部通过；构建通过；应用实际启动运行正常

差分测试抓到一处真实差异并已对齐：非法 JSON 转义（如 `\d`）时旧实现返回 nil，
新实现最初宽松放行，已改成按 JSON 规范同样判为失败。**这类改写一定要做差分测试，
光看着"逻辑等价"是不够的。**

### 故意没做的

- **剪贴板轮询间隔保持 0.18s 不变。** 双复制判定依赖这个间隔，调大有漏判风险。
  只加了 0.03s 容差让系统合并唤醒，属于缓解不是根治——macOS 没有剪贴板变更通知 API，只能轮询。
- **没有拆分 `ContentView`。** 它订阅了 10 个 `@EnvironmentObject`，任一变化都会重算整个 body。
  彻底解决要把译文面板拆成独立视图，但 `.onChange` 处理器都挂在 ContentView 上，
  改动面大、回归风险高，而主要开销已经在 1/3 里解决了。留作后续。
- **没有给流式更新加节流。** 合并多个 delta 再刷 UI 是常见做法，但会改变更新节奏（观感），
  与"不改变主要功能"的要求冲突。增量化之后每次更新已经足够便宜。

### Codex review 第三轮：残留的 COW 拷贝（实测后决定不改）

**问题**：`StreamingNewlineRestorer` 返回的 `display` 会被 `vm.outputText` 持有，
于是下一次 `removeLast`/`append` 时缓冲区是共享的，Swift 必须先整段拷贝再改（copy-on-write）。
也就是说尽管只解析新增字符，每个 delta 仍有一次 O(全文长度) 的拷贝，理论上还是二次的。

**机制属实**，实测也能看到二次项（deltas 每翻倍，耗时从 2.0x 慢慢涨到 3.5x）。
但常数是**纯 memcpy**，和旧实现里每个 delta 新建 JSONDecoder 完全不是一个量级：

| 译文字数 | 投影器 | 还原器 | 合计 | 旧实现同段 |
|---|---|---|---|---|
| 2400 | 0.4ms | 1.2ms | 1.6ms | 54ms |
| 4800 | 0.7ms | 2.1ms | 2.8ms | 197ms |
| 9600 | 1.7ms | 4.8ms | 6.5ms | 778ms |
| 19200 | 4.6ms | 10.5ms | 15.1ms | — |

参照系：同一次更新里 NSTextView 的写入与排版，4800 字时是 121ms。
也就是说这段解析开销只占整次更新的 2% 左右，而 19200 字已经远超真实译文长度。

**为什么不改**：要消掉这个二次项，只能把契约从「每次给全文」改成「给全文 + 增量」，
一路改到 ViewModel、Mini 控制器和两个文本视图。而且只有在**没有任何其他持有者**时才真正省得下来——
`@Published` 加 SwiftUI 很难保证这一点，很可能改了一大圈仍然在拷贝。
用这个代价换 2% 的开销不划算。

**顺带**：投影器原本也有同样的问题（旧版把入参存进 `previousInput` 跨调用持有），
在上一轮删掉指纹判定时一并消失了。现在只有还原器还有这个特性。

**教训：二次复杂度也分量级。** 判断该不该修，要看常数和它在整条路径里的占比，
而不是只看阶。这次每个 delta 是一次 memcpy，和之前每个 delta 一次 JSONDecoder 全量解码，
阶相同但差了两个数量级。

### Codex review 第四轮：换候选键时忘了标记为替换（已修）

**问题**：第一轮修复引入的「每个候选键各自增量解析、按优先级取第一个有结果的」设计，
带来一个新情况——**换键时输出不是追加**。但引擎当时只把「原始流是否替换」透传给还原器，
换键这件事没有告诉下游。

实测复现（先读到 `translated_text` 的「备用」，随后 `translatedText` 出现并接管）：

```
投影="备用"          → 界面显示="备用"
投影="优先且更长"     → 界面显示="备用且更长"   ← 错误，把两段拼在一起了
```

**修法**：`Session.project` 改为返回 `Projection(text:replacesPreviousText:)`。
单个候选键的解析结果只增不减，所以判据很简单：**这次给出结果的候选键下标变了，就是替换**。
引擎把它和原始流的替换标志一起 OR 后传给下游。

**教训：分段都对，不代表串起来对。** 前几轮的模糊测试都是单独测投影器、单独测还原器，
两边各自都与旧实现完全一致，却测不出「拼接」这一类错误——因为错误发生在两段的**接缝**上。
补了一个**链路级**模糊测试：把投影器和还原器串起来，与「旧投影器 + 一次性 restoreNewlines」
逐字符对比，8000 组结构化 + 500 组双键定向用例。以后改这条链路，请跑链路级的那个。

### 已知环境问题

本地跑 `xcodebuild test` 必须加 `CODE_SIGNING_ALLOWED=NO`，且一定会以 `** TEST FAILED **` 结尾：
UI 测试 runner 未签名无法 bootstrap。这在 `main` 分支上同样发生，**不是改动引入的**。
判断通过与否只看单元测试那行：`Test run with N tests in 1 suite passed`。
