# 更新日志

## 1.4.1 - 2026-09-26

- **更新后不再弹「想要访问你的钥匙串」密码框。** 以前每次更新完第一次翻译，系统都会要求输入登录密码才能读取保存的 API Key，点「始终允许」也只对当前版本有效。从这一版起，发布包改用 Apple 签发的开发证书签名，以后的更新钥匙串会直接放行。
- **升级到这一版时，需要最后再授权一次。** 1.3.1 换上的证书保住了辅助功能授权，却挡不住钥匙串弹框，所以这次又换了一张证书，旧的授权要重新确认一次：
  - 升级后第一次翻译时如果弹出钥匙串密码框，输入登录密码后点「始终允许」。
  - 打开「系统设置 → 隐私与安全性」，在「辅助功能」和「录屏与系统录音」里分别选中旧的「大佐翻译官v1」，点「−」删掉，再重新添加并打开开关（也可以在应用的权限引导里把图标拖进列表）。

  完成这一次之后，以后的更新两样都不用再做。
- **控制台换了新样式**，和主界面统一：可以按信息 / 警告 / 错误筛选、按关键字搜索，一键复制当前显示的日志，反馈问题时直接贴出来即可；窗口可以拖大。
- 日志内容统一使用界面上的叫法（「选区翻译」「钥匙串」等），不再中英混杂。

## 1.4.0 - 2026-09-26

- **截图翻译的译文改为直接贴回原图**：保留原截图，只抹掉原文，再按原来的位置、字号、颜色和粗细画上译文，不再用一张卡片盖住整个选区。译文比原文长时，会先往旁边的空白处延伸，再折行、缩小字号，实在放不下才截断；旁边的分隔线、图标和不需要翻译的文字不会被盖住。翻译过程中翻好一批就先显示一批。
- **截图翻译识别更准**：源语言默认「自动检测」（跟随主界面的设置），也可以在截图工具栏里手动指定。修复中文、日文、韩文截图被当成英文识别、结果变成乱码的问题。
- 修复截图翻译把认对的文字改坏的问题：英文里带 trusted、scholar 这类单词时，下面几行开头被替换成「¿」；俄语截图里的英文单词被换成形近的俄文字母；两个相同的词或相邻的相似行被吞掉一个。
- 截图里折行的句子现在会拼回整段再翻译，双栏排版不会再把左右两栏拼成一句；已经是目标语言的段落保持原样，不再重复翻译。
- 修复截图框得很高时，相邻两行的顺序偶尔颠倒的问题；修复识别出错时可能导致应用崩溃的问题。
- 截图翻译的「钉到屏幕」和「完成」现在拿的是贴了译文的图（翻译之前仍然是原截图）。识别、翻译进行中这两个按钮暂时不可点，免得拿走只翻了一半的图。
- 选区翻译在替换原文前会更严格地确认目标应用没有换：以前在两个没有标准应用标识的程序之间切换时可能察觉不到。

## 1.3.1 - 2026-09-24

- **从这一版起，更新后不用再重新授权辅助功能和屏幕录制。** 发布包改用固定证书签名，macOS 会把以后的每个版本都认作同一个应用，授权自动沿用。
- **升级到这一版时，需要最后授权一次。** 打开「系统设置 → 隐私与安全性」，在「辅助功能」和「录屏与系统录音」里分别选中旧的「大佐翻译官v1」，点「−」删掉，再重新添加并打开开关（也可以在应用的权限引导里把图标拖进列表）。完成这一次之后，以后的更新都不用再授权。
- 修复授权屏幕录制后，系统设置弹出的「退出并重新打开」点了没有反应的问题：此前应用开着权限引导时，会拦下系统发来的退出请求。
- 修复在权限引导里拖动图标授权时，每拖一次都会在 `~/Library/Caches` 下留下一整份应用副本（约 31MB）的问题。旧版本已经留下的副本在 `~/Library/Caches/com.apple.SwiftUI.Drag-*` 文件夹里，里面只有「大佐翻译官v1.app」的那几个可以直接删除。

## 1.3.0 - 2026-09-20

- 新增翻译服务「微软翻译」：在线、免配置、不需要 API Key，在设置的「翻译服务」里或主窗口顶部的服务菜单中切换即可。主窗口、Mini 气泡、选区翻译、截图翻译都可以使用。
- 微软翻译支持自动检测源语言，译文返回后会显示识别出的语种；单次最多翻译 30000 字，超出会提前提示而不是等服务端报错。

## 1.2.6 - 2026-09-09

- 修复 Apple 本地翻译的会话泄漏导致的 CPU 飙升：此前每翻译一次都会向系统申请一个新的翻译会话且从不回收，连续使用（尤其选区翻译）后应用与系统翻译扩展各堆积十几个隐形会话，系统翻译扩展陷入持续布局循环，CPU 可达 90%，只能重启应用恢复。现在同一语言对全程复用同一个翻译会话；翻译进行中提交新请求仍会立即打断旧翻译开始新的，不泄漏、不堆积。

## 1.2.5 - 2026-08-25

- 新增「选区翻译」：在任意应用里选中文字后按 ⌥⇧Space 弹出候选浮层，↑↓ 切换、回车把原文替换成译文、⌘C 复制译文、Esc 关闭。可配置最多 6 个目标语言，与原文相同的语言会自动隐去。**默认关闭**，需在设置中开启并授予辅助功能权限。
- 选区翻译在能精确定位原文时执行原位替换，宿主应用的撤销（⌘Z）仍然可用；无法安全确认替换生效时自动退回复制译文，不会静默失败。
- 选区翻译支持 Chrome、VS Code、Slack、Discord、Notion 等基于 Chromium / Electron 的应用。这类应用默认不提供辅助功能数据，首次在某个应用中使用时可能需要按两次快捷键。
- 选区翻译遵循应用范围设置，默认排除终端、iTerm2、钥匙串访问与常见密码管理器；系统安全输入开启时不工作。被排除的应用现在会明确提示，不再毫无反应。
- 选区翻译新增「选中后自动显示候选」实验选项，默认关闭。
- 修复流式翻译在长文本下拖慢主窗口的问题：9600 字的解析耗时从 778ms 降到 6.5ms。
- 修复流式翻译在少数情况下界面停留在过期内容，以及两段译文被拼接在一起的问题。
- 修复翻译短词短语（例如 `TestHost`、`设置`）时 Apple 翻译弹出「无法自动检测语言，请选择要翻译的语言」的问题。
- 修复日文、韩文中夹带汉字的文本被当成中文翻译的问题；注音符号文本现在按繁体中文处理。
- Mini 翻译气泡新增原文语种显示，在译文返回后展示实际使用的源语言。

## 1.2.4 - 2026-07-31

- 修复 1.2.3 通过 Sparkle 更新后因 ad-hoc hardened runtime 签名与 Sparkle 动态库 Team ID 不一致而无法启动的问题。
- 发布流程增加真实启动检查，避免仅通过静态签名验证但运行时被 macOS 拒绝加载的安装包再次发布。

## 1.2.3 - 2026-07-31

- OpenAI 通用接口改用 MacPaw/OpenAI SDK，移除原有手写请求与响应解析主路径。
- Responses 支持真实 SSE 流式译文，主窗口和 Mini 翻译气泡会随服务端增量实时更新。
- Responses 标准参数被拒绝时自动使用精简参数重试，并统一处理限流与不兼容错误。
- 增加常见第三方 Responses 响应结构的兼容解析，保留 Chat Completions 模式。
- 修复接口验证成功但 API Key 写入失败时仍保存部分配置的问题。
- 修复 Mini 窗口关闭后残留悬停状态，导致后续结果不再自动关闭的问题。
- 发布包改为无 sandbox 权限的完整 ad-hoc bundle 签名，确保 Sparkle 可生成并验证更新包。
- 增加本地 `start.sh`、锁定 Swift Package 版本并补充第三方许可声明。

## 1.2.2 - 2026-07-17

- 修复无 Developer ID 环境下 Sparkle 更新安装失败问题。
- 改进 arm64 与 x86_64 发布包的签名检查与 appcast 生成。

## 1.2.1 - 2026-07-17

- Mini 模式支持智能翻译方向。
- 增加原文、译文和 Mini 窗口独立字号。
- 改进全局快捷键、权限刷新与服务商切换。
